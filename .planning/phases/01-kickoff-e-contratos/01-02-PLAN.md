---
phase: 01-kickoff-e-contratos
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/__init__.py
  - src/features.py
  - docs/feature_contract.md
autonomous: true
requirements:
  - KICK-01
  - KICK-04

must_haves:
  truths:
    - "from src.features import PRE_DELIVERY_FEATURES executa sem erro a partir da raiz do projeto"
    - "PRE_DELIVERY_FEATURES contem exatamente as colunas pre-entrega acordadas em CONTEXT.md"
    - "FORBIDDEN_FEATURES contem obrigatoriamente order_delivered_customer_date, review_score, review_comment_message, review_creation_date, review_answer_timestamp"
    - "TARGET_COLUMN e a string 'bad_review'"
    - "docs/feature_contract.md tem tabela com todas as colunas tageadas como pre-entrega, pos-entrega ou target"
    - "O corte temporal order_approved_at esta documentado como ancora em ambos os arquivos"
  artifacts:
    - path: "src/__init__.py"
      provides: "Torna src/ importavel como pacote Python"
    - path: "src/features.py"
      provides: "Constantes PRE_DELIVERY_FEATURES, FORBIDDEN_FEATURES, TARGET_COLUMN"
      exports:
        - PRE_DELIVERY_FEATURES
        - FORBIDDEN_FEATURES
        - TARGET_COLUMN
    - path: "docs/feature_contract.md"
      provides: "Tabela de colunas legivel por humanos com tags pre/pos-entrega/target"
      contains: "order_approved_at"
  key_links:
    - from: "src/features.py"
      to: "notebooks/FASE4-P4-ml-pipeline.ipynb"
      via: "from src.features import PRE_DELIVERY_FEATURES"
      pattern: "PRE_DELIVERY_FEATURES"
    - from: "src/features.py"
      to: "app/pages/03_modelo.py"
      via: "from src.features import PRE_DELIVERY_FEATURES"
      pattern: "PRE_DELIVERY_FEATURES"
    - from: "docs/feature_contract.md"
      to: "src/features.py"
      via: "sincronizacao manual — colunas em PRE_DELIVERY_FEATURES devem ter linha na tabela"
      pattern: "\\[pre-entrega\\]"
---

<objective>
Criar o contrato de features em dois formatos: o arquivo Python `src/features.py` (importavel por codigo ML e pelo app Streamlit) e o documento `docs/feature_contract.md` (legivel por todos os membros do time).

Purpose: Este e o guardrail tecnico central do projeto — qualquer notebook ou app que importar `PRE_DELIVERY_FEATURES` de `src/features.py` esta automaticamente protegido contra data leakage pos-entrega. O documento Markdown garante que membros nao-tecnicos tambem possam auditar as decisoes.
Output: `src/__init__.py`, `src/features.py` com tres constantes, `docs/feature_contract.md` com tabela completa.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kickoff-e-contratos/01-CONTEXT.md
@.planning/phases/01-kickoff-e-contratos/01-RESEARCH.md
@.planning/phases/01-kickoff-e-contratos/01-01-SUMMARY.md

<interfaces>
<!-- Constantes a serem criadas em src/features.py — esta e a spec definitiva -->
<!-- Baseada em CONTEXT.md (decisoes bloqueadas) e RESEARCH.md (Code Examples) -->

```python
# src/features.py — spec completa

PRE_DELIVERY_FEATURES: list[str] = [
    # Frete e preco
    "freight_value",
    "price",
    "freight_ratio",              # engineered: freight_value / price
    # Prazo estimado
    "estimated_delivery_days",    # engineered: order_estimated_delivery_date - order_approved_at
    # Geografia
    "seller_state",
    "customer_state",
    "seller_customer_distance_km",  # engineered Phase 2: Haversine(seller_zip, customer_zip)
    # Produto
    "product_weight_g",
    "product_volume_cm3",         # engineered: length * width * height
    "product_category_name_english",
    # Pedido
    "order_item_count",
    "payment_type",
    "payment_installments",
]

FORBIDDEN_FEATURES: list[str] = [
    "order_delivered_customer_date",   # pos-entrega
    "review_score",                    # o proprio target / pos-entrega
    "review_comment_message",          # pos-entrega
    "review_creation_date",            # pos-entrega
    "review_answer_timestamp",         # pos-entrega
    "order_delivered_carrier_date",    # parcialmente pos-aprovacao, pouco confiavel
]

TARGET_COLUMN: str = "bad_review"
# bad_review = 1 se review_score in {1, 2}, else 0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar src/__init__.py e src/features.py</name>
  <files>src/__init__.py, src/features.py</files>
  <behavior>
    - Test 1: `from src.features import PRE_DELIVERY_FEATURES` deve funcionar a partir da raiz do projeto sem erro
    - Test 2: `PRE_DELIVERY_FEATURES` deve ser uma lista com exatamente 13 strings
    - Test 3: Todas as colunas proibidas obrigatorias devem estar em `FORBIDDEN_FEATURES`: order_delivered_customer_date, review_score, review_comment_message, review_creation_date, review_answer_timestamp
    - Test 4: `TARGET_COLUMN == "bad_review"`
    - Test 5: Nenhuma coluna de `FORBIDDEN_FEATURES` deve aparecer em `PRE_DELIVERY_FEATURES`
    - Test 6: `seller_customer_distance_km` deve estar em `PRE_DELIVERY_FEATURES` (computada na Phase 2, mas declarada aqui como contrato)
  </behavior>
  <action>
    **`src/__init__.py`** — Arquivo vazio (apenas comentario). Obrigatorio para que `from src.features import ...` funcione sem `sys.path` manual:

    ```python
    # src/__init__.py
    # Makes src/ a Python package.
    # Required for: from src.features import PRE_DELIVERY_FEATURES
    ```

    **`src/features.py`** — Implementar exatamente conforme a spec em `<interfaces>` acima. Usar o template completo do RESEARCH.md (secao "Code Examples"). O arquivo deve incluir:

    1. Docstring de modulo descrevendo:
       - TEMPORAL ANCHOR: order_approved_at
       - Regra: nenhuma feature pos-entrega
       - Quem importa este arquivo (FASE4-P4-ml-pipeline.ipynb e app/pages/03_modelo.py)
       - Instrucao: "If you add a column to PRE_DELIVERY_FEATURES, also add a row to docs/feature_contract.md"

    2. Constante `PRE_DELIVERY_FEATURES: list[str]` com comentarios agrupando as colunas por categoria (frete/preco, prazo, geografia, produto, pedido). Para `seller_customer_distance_km`, adicionar comentario: `# PHASE 2 — computed via Haversine; not available until gold table is built`

    3. Constante `FORBIDDEN_FEATURES: list[str]` com comentario inline em cada coluna explicando por que e proibida

    4. Constante `TARGET_COLUMN: str = "bad_review"` com comentario: `# bad_review = 1 if review_score in {1, 2}, else 0`

    IMPORTANTE: Nao usar `order_purchase_timestamp` como ancora temporal — a ancora e `order_approved_at` (decisao bloqueada em CONTEXT.md).
  </action>
  <verify>
    <automated>python -c "
import sys
sys.path.insert(0, '.')
from src.features import PRE_DELIVERY_FEATURES, FORBIDDEN_FEATURES, TARGET_COLUMN

# Test: lista nao vazia
assert len(PRE_DELIVERY_FEATURES) > 0, 'PRE_DELIVERY_FEATURES vazia'
print(f'OK: PRE_DELIVERY_FEATURES tem {len(PRE_DELIVERY_FEATURES)} colunas')

# Test: colunas obrigatorias proibidas
required_forbidden = [
    'order_delivered_customer_date',
    'review_score',
    'review_comment_message',
    'review_creation_date',
    'review_answer_timestamp',
]
for col in required_forbidden:
    assert col in FORBIDDEN_FEATURES, f'FAIL: {col} nao esta em FORBIDDEN_FEATURES'
    print(f'OK: {col} esta em FORBIDDEN_FEATURES')

# Test: sem vazamento (nenhuma coluna proibida em PRE_DELIVERY_FEATURES)
leakage = [c for c in FORBIDDEN_FEATURES if c in PRE_DELIVERY_FEATURES]
assert not leakage, f'LEAKAGE DETECTADO: {leakage}'
print('OK: nenhuma coluna proibida em PRE_DELIVERY_FEATURES')

# Test: target
assert TARGET_COLUMN == 'bad_review', f'TARGET_COLUMN incorreto: {TARGET_COLUMN}'
print(f'OK: TARGET_COLUMN = {TARGET_COLUMN!r}')

# Test: coluna Phase 2
assert 'seller_customer_distance_km' in PRE_DELIVERY_FEATURES
print('OK: seller_customer_distance_km esta em PRE_DELIVERY_FEATURES')

print('ALL TESTS PASSED')
"
    </automated>
  </verify>
  <done>
    - src/__init__.py existe (vazio com comentario)
    - src/features.py exporta PRE_DELIVERY_FEATURES (13 colunas pre-entrega), FORBIDDEN_FEATURES (6 colunas proibidas) e TARGET_COLUMN = "bad_review"
    - Nenhuma coluna proibida aparece na lista de features permitidas
    - Docstring documenta ancora temporal order_approved_at
    - Todos os testes automatizados passam
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar docs/feature_contract.md</name>
  <files>docs/feature_contract.md</files>
  <action>
    Criar o documento human-readable que e a contrapartida do `src/features.py`. Deve ser legivel por qualquer membro do time, inclusive os nao-tecnicos. Estrutura obrigatoria:

    **Header prominente:**
    ```
    # Feature Contract — Olist Pre-Delivery Risk Model

    **Temporal anchor:** `order_approved_at`
    **Target:** `bad_review` = 1 se `review_score` in {1, 2}, 0 caso contrario
    **Regra de ouro:** Apenas colunas marcadas `[pre-entrega]` podem entrar na matriz de features do modelo.
    **Arquivo de referencia:** `src/features.py` — unica fonte de verdade para codigo
    ```

    **Tabela de colunas** — incluir TODAS as colunas abaixo com as colunas: `Coluna | Origem CSV | Derivacao | Tag | Notas`. Tags possiveis: `[pre-entrega]`, `[pos-entrega]`, `[target]`, `[join-key]`, `[temporal-anchor]`.

    Colunas a incluir na tabela:

    Join keys e ancora temporal:
    - order_id | orders.csv | raw | [join-key] |
    - order_approved_at | orders.csv | raw | [temporal-anchor] | Ancora de corte — features calculadas relative a esta data

    Target:
    - review_score | order_reviews.csv | raw | [pos-entrega] | Usado APENAS para derivar bad_review — nunca como feature
    - bad_review | derivada | review_score in {1,2} -> 1, else 0 | [target] |

    Features pre-entrega (de PRE_DELIVERY_FEATURES):
    - freight_value | order_items.csv | soma por pedido | [pre-entrega] |
    - price | order_items.csv | soma por pedido | [pre-entrega] |
    - freight_ratio | engineered | freight_value / price | [pre-entrega] |
    - estimated_delivery_days | orders.csv | order_estimated_delivery_date - order_approved_at | [pre-entrega] | Usa order_approved_at como ancora (nao order_purchase_timestamp)
    - seller_state | sellers.csv | raw | [pre-entrega] |
    - customer_state | customers.csv | raw | [pre-entrega] |
    - seller_customer_distance_km | geolocation.csv | Haversine(seller_zip, customer_zip) em km | [pre-entrega] | Computada na Phase 2 — esperado 0-4000 km
    - product_weight_g | products.csv | raw | [pre-entrega] |
    - product_volume_cm3 | products.csv | length * width * height | [pre-entrega] |
    - product_category_name_english | category_translation.csv | join via product_category_name | [pre-entrega] |
    - order_item_count | order_items.csv | contagem por order_id | [pre-entrega] |
    - payment_type | order_payments.csv | moda por pedido | [pre-entrega] |
    - payment_installments | order_payments.csv | soma por pedido | [pre-entrega] |

    Colunas proibidas (FORBIDDEN — nunca entram em X):
    - order_delivered_customer_date | orders.csv | raw | [pos-entrega] | **PROIBIDA** — so disponivel apos entrega
    - review_comment_message | order_reviews.csv | raw | [pos-entrega] | **PROIBIDA** — texto pos-entrega (NLP opcional e fora do escopo v1)
    - review_creation_date | order_reviews.csv | raw | [pos-entrega] | **PROIBIDA**
    - review_answer_timestamp | order_reviews.csv | raw | [pos-entrega] | **PROIBIDA**
    - order_delivered_carrier_date | orders.csv | raw | [pos-entrega] | **PROIBIDA** — parcialmente pos-aprovacao, pouco confiavel

    **Secao final: Como manter este documento sincronizado**
    Adicionar nota: "Se uma coluna e adicionada a `PRE_DELIVERY_FEATURES` em `src/features.py`, uma linha correspondente DEVE ser adicionada a esta tabela. As duas fontes devem estar sempre em sincronia."
  </action>
  <verify>
    <automated>python -c "
import os
content = open('docs/feature_contract.md').read()

# Header critico
assert 'order_approved_at' in content, 'FAIL: ancora temporal ausente'
assert 'bad_review' in content, 'FAIL: target ausente'
assert 'pre-entrega' in content.lower(), 'FAIL: tag pre-entrega ausente'
assert 'pos-entrega' in content.lower(), 'FAIL: tag pos-entrega ausente'
assert 'PROIBIDA' in content or 'FORBIDDEN' in content, 'FAIL: colunas proibidas nao destacadas'

# Colunas criticas
required_cols = [
    'freight_value', 'price', 'freight_ratio',
    'estimated_delivery_days', 'seller_state', 'customer_state',
    'seller_customer_distance_km', 'product_weight_g',
    'order_item_count', 'payment_type', 'payment_installments',
    'order_delivered_customer_date', 'review_score',
]
for col in required_cols:
    assert col in content, f'FAIL: {col} ausente em feature_contract.md'

print(f'OK: feature_contract.md tem {len(content)} chars')
print('OK: todas as colunas criticas presentes')
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <done>
    - docs/feature_contract.md existe com header mostrando ancora temporal (order_approved_at) e target (bad_review)
    - Tabela tem todas as 13 features pre-entrega de PRE_DELIVERY_FEATURES
    - Colunas proibidas estao claramente marcadas como PROIBIDAS
    - Documento tem nota de sincronizacao com src/features.py
  </done>
</task>

</tasks>

<verification>
Verificacao integrada apos conclusao do plano:

```python
import sys
sys.path.insert(0, '.')

# Import funciona
from src.features import PRE_DELIVERY_FEATURES, FORBIDDEN_FEATURES, TARGET_COLUMN

# Sem leakage
leakage = [c for c in FORBIDDEN_FEATURES if c in PRE_DELIVERY_FEATURES]
assert not leakage, f"LEAKAGE: {leakage}"

# Target correto
assert TARGET_COLUMN == "bad_review"

# Documento sincronizado
doc = open("docs/feature_contract.md").read()
for col in PRE_DELIVERY_FEATURES:
    assert col in doc, f"Coluna {col!r} em features.py mas ausente em feature_contract.md"

print("PASS: contrato de features completo e consistente")
```
</verification>

<success_criteria>
- `from src.features import PRE_DELIVERY_FEATURES, FORBIDDEN_FEATURES, TARGET_COLUMN` funciona da raiz do projeto
- PRE_DELIVERY_FEATURES lista exatamente as colunas pre-entrega acordadas (sem order_purchase_timestamp como ancora)
- FORBIDDEN_FEATURES inclui obrigatoriamente: order_delivered_customer_date, review_score, review_comment_message, review_creation_date, review_answer_timestamp
- TARGET_COLUMN == "bad_review"
- Nenhuma coluna proibida aparece em PRE_DELIVERY_FEATURES (zero leakage)
- docs/feature_contract.md tem tabela completa com todas as colunas de PRE_DELIVERY_FEATURES
- Ancora temporal order_approved_at documentada com destaque em ambos os arquivos
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-kickoff-e-contratos/01-02-SUMMARY.md`
</output>
