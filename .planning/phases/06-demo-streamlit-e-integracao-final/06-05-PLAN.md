---
phase: 06-demo-streamlit-e-integracao-final
plan: "05"
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - .streamlit/secrets.toml
  - docs/demo_checklist.md
autonomous: false
requirements:
  - PRES-07

must_haves:
  truths:
    - "App inicia com streamlit run app.py sem nenhum erro em menos de 3 segundos (cold start local)"
    - "Todas as 3 paginas funcionais (Preditor, Mapa, EDA) carregam sem erro de ImportError, FileNotFoundError ou AttributeError"
    - "Preditor retorna score de risco ao submeter formulario com valores validos"
    - "Mapa renderiza choropleth com estados coloridos ao carregar a pagina"
    - "EDA exibe pelo menos uma figura ao carregar (se reports/figures/ populado)"
    - "Simulacao completa da demo executada: 5 cenarios no Preditor, 2 filtros no Mapa, 3 graficos no EDA — sem timeout ou erro"
    - "URL publica do Streamlit Cloud acessivel via celular (ou plano B local documentado)"
  artifacts:
    - path: "docs/demo_checklist.md"
      provides: "Roteiro de simulacao completa com passos verificados e plano B documentado"
  key_links:
    - from: "Streamlit Cloud deploy"
      to: "app.py"
      via: "streamlit run app.py no entrypoint do Cloud"
      pattern: "streamlit run app.py"
    - from: "demo_checklist.md"
      to: "5 cenarios do Preditor"
      via: "inputs especificos com scores esperados documentados"
      pattern: "Cenario"
---

<objective>
Deploy no Streamlit Cloud, simulacao completa da demo em maquina local + URL publica, e criacao do checklist de apresentacao com plano B documentado.

Purpose: Criterio 5 do sucesso da fase — "simulacao completa da demo feita sem timeout, erro ou carregamento lento". Este plano prove que tudo funciona ponta-a-ponta antes da apresentacao real. O checklist e o artefato que o apresentador usa no dia.

Output: App deployed no Streamlit Cloud (URL publica), docs/demo_checklist.md com 5 cenarios testados e plano B local documentado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-demo-streamlit-e-integracao-final/06-CONTEXT.md
@.planning/phases/06-demo-streamlit-e-integracao-final/06-RESEARCH.md

<interfaces>
<!-- Estado do repositorio ao entrar na Wave 3 -->
<!-- Todos os arquivos abaixo devem existir apos Wave 1 + Wave 2 -->

Arquivos que devem existir:
- app.py                          (06-01)
- utils/loaders.py                (06-01)
- pages/1_Home.py                 (06-01)
- pages/2_Preditor.py             (06-02)
- pages/3_Mapa.py                 (06-03)
- pages/4_EDA.py                  (06-04)
- requirements.txt                (06-01)
- data/geo/brazil-states.geojson  (06-01)
- .streamlit/config.toml          (06-01)

Artefatos de fases anteriores (devem existir antes do deploy):
- models/final_pipeline.joblib    (Phase 4, ML-07)
- data/processed/geo_aggregated.parquet (Phase 3, EDA-03)
- reports/figures/*.png           (Phase 3, EDA-01 a EDA-05)
- models/threshold.json           (Phase 4, ML-05 — opcional, fallback 0.5 se nao existir)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke test local completo + docs/demo_checklist.md</name>
  <files>docs/demo_checklist.md</files>
  <action>
Executar smoke test local do app e criar o checklist de apresentacao.

**Passo 1 — Verificar pre-requisitos:**
```bash
# Na raiz do projeto
python -c "
import sys
sys.path.insert(0, '.')
from utils.loaders import load_pipeline, load_geo_data, load_brazil_geojson, list_eda_figures

print('Pipeline:', 'OK' if True else 'MISSING')
try:
    p = load_pipeline(); print('load_pipeline: OK')
except FileNotFoundError as e:
    print(f'load_pipeline: MISSING — {e}')

try:
    df = load_geo_data(); print(f'load_geo_data: OK ({len(df)} rows, cols: {df.columns.tolist()})')
except FileNotFoundError as e:
    print(f'load_geo_data: MISSING — {e}')

figs = list_eda_figures()
print(f'EDA figures: {len(figs)} PNGs encontrados')

import json
try:
    g = json.load(open('data/geo/brazil-states.geojson'))
    print(f'GeoJSON: OK ({len(g[\"features\"])} estados)')
except: print('GeoJSON: MISSING')
"
```

**Passo 2 — Verificar sintaxe de todos os arquivos:**
```bash
python -m py_compile app.py utils/loaders.py pages/1_Home.py pages/2_Preditor.py pages/3_Mapa.py pages/4_EDA.py
echo "Todos os arquivos: OK"
```

**Passo 3 — Criar docs/demo_checklist.md** com roteiro completo para o dia da apresentacao:

```markdown
# Checklist de Demo — Olist Risk Dashboard

**Ultima simulacao:** [DATA]
**App URL (Streamlit Cloud):** [URL apos deploy]
**Plano B (local):** streamlit run app.py

---

## Pre-Apresentacao (30 min antes)

- [ ] Abrir URL do Streamlit Cloud no celular — confirmar que carrega
- [ ] Abrir `streamlit run app.py` localmente como Plano B
- [ ] Confirmar que pipeline carregou (sem spinner permanente no Preditor)
- [ ] Confirmar que mapa renderizou (estados coloridos, nao cinzas)
- [ ] Ter pelo menos 1 aba com URL do Cloud e 1 com localhost:8501 abertas

---

## Roteiro da Demo (5 minutos)

### Pagina Home (30 segundos)
- [ ] Abrir pagina Home — mostrar estrutura do dashboard e premissa

### Pagina Preditor (2 minutos — peca central)

**Cenario 1 — Risco Baixo (vermelho esperado: NAO)**
- Frete: R$ 15, Pedido: R$ 200, Prazo: 7 dias, Categoria: housewares, SP → SP
- Score esperado: < 35% (verde)
- [ ] Verificado — score: ____%

**Cenario 2 — Risco Alto (vermelho esperado: SIM)**
- Frete: R$ 80, Pedido: R$ 50, Prazo: 30 dias, Categoria: furniture_decor, SP → AM
- Score esperado: > 65% (vermelho)
- [ ] Verificado — score: ____%

**Cenario 3 — Risco Medio**
- Frete: R$ 35, Pedido: R$ 120, Prazo: 15 dias, Categoria: sports_leisure, MG → BA
- Score esperado: 35-65% (amarelo)
- [ ] Verificado — score: ____%

**Cenario 4 — Input do publico (improviso controlado)**
- Pedir para alguem da plateia sugerir valores
- [ ] Nenhum erro ao submeter

**Cenario 5 — Rotas Norte/Nordeste**
- Frete: R$ 60, Pedido: R$ 80, Prazo: 25 dias, Categoria: bed_bath_table, SP → PA
- Score esperado: alto (vermelho)
- [ ] Verificado — score: ____%

### Pagina Mapa (1 minuto)
- [ ] Mapa carregou com estados coloridos
- [ ] Aplicar filtro: UF Destino = AM, PA, RR — mostrar concentracao no Norte
- [ ] Hover em AM — mostrar % bad review + atraso medio + volume
- [ ] Remover filtros — mapa volta ao estado completo

### Pagina EDA (1 minuto)
- [ ] Selecionar boxplot atraso vs nota — mostrar correlacao visual
- [ ] Selecionar heatmap rotas — conectar com mapa anterior
- [ ] Navegar com botoes prev/next

---

## Plano B — Se Streamlit Cloud Falhar

```bash
# Na maquina de apresentacao
cd [caminho/do/projeto]
streamlit run app.py
# Abrir: http://localhost:8501
```

**Pre-requisitos do Plano B (verificar antes do evento):**
- [ ] Python e streamlit instalados na maquina
- [ ] `pip install -r requirements.txt` executado
- [ ] Todos os artefatos presentes localmente (models/, data/, reports/)
- [ ] `streamlit run app.py` testado — abre sem erro

---

## Problemas Conhecidos e Solucoes

| Problema | Solucao |
|---------|---------|
| Streamlit Cloud em "sleep" — spinner por 30-60s | Acessar URL 15 min antes para fazer warm-up |
| Estados cinzas no mapa | Verificar featureidkey='properties.sigla' e siglas uppercase no parquet |
| Feature names mismatch no Preditor | Verificar src/features.py PRE_DELIVERY_FEATURES vs nomes no formulario |
| Pipeline nao carrega — versao mismatch | Verificar requirements.txt vs versoes do ambiente de treino |
| reports/figures/ vazio | Executar Phase 3 antes do deploy |
```

Criar o arquivo com `Path("docs").mkdir(exist_ok=True)` e salvar o conteudo acima.
  </action>
  <verify>
    <automated>cd "C:/Users/Wey/Desktop/Alpha/Projetos/python" && python -m py_compile app.py utils/loaders.py pages/1_Home.py pages/2_Preditor.py pages/3_Mapa.py pages/4_EDA.py && echo "Todos os arquivos compilam OK" && python -c "from pathlib import Path; assert Path('docs/demo_checklist.md').exists(), 'checklist nao criado'; content=Path('docs/demo_checklist.md').read_text(); assert 'Cenario' in content; assert 'Plano B' in content; print('demo_checklist.md OK')"</automated>
  </verify>
  <done>Todos os arquivos Python compilam sem SyntaxError. docs/demo_checklist.md existe com 5 cenarios documentados, roteiro de 5 minutos e plano B local.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
App Streamlit completo com 4 paginas (Home, Preditor, Mapa, EDA), utils/loaders.py com cache correto, GeoJSON local, requirements.txt pinado, e docs/demo_checklist.md. Claude executou smoke test local e verificou sintaxe de todos os arquivos.

Para deploy no Streamlit Cloud e simulacao final da demo, e necessaria verificacao humana porque:
1. Deploy no Streamlit Cloud requer conta e repositorio no GitHub — nao pode ser automatizado sem credenciais
2. Simulacao real da demo requer artefatos das Phases 3 e 4 (pipeline.joblib, geo_aggregated.parquet, PNGs) que podem nao existir ainda
3. Verificacao visual do gauge e do choropleth exige olho humano
  </what-built>
  <how-to-verify>

**Passo 1 — Teste local basico (sem artefatos de Phase 3/4):**
```bash
cd [caminho/do/projeto]
pip install -r requirements.txt
streamlit run app.py
```
- Abrir http://localhost:8501
- Home carrega? (sim/nao)
- Preditor mostra formulario? (sim/nao) — mesmo sem pipeline.joblib, deve mostrar erro claro
- Mapa abre? (sim/nao) — mesmo sem parquet, deve mostrar erro claro
- EDA abre com aviso "nenhuma figura"? (sim/nao)

**Passo 2 — Teste completo (apos Phases 3 e 4 executadas):**
- Seguir docs/demo_checklist.md — 5 cenarios no Preditor
- Confirmar que Cenario 2 (frete alto, prazo longo, SP→AM) retorna risco alto (vermelho)
- Confirmar que mapa renderiza 27 estados coloridos
- Confirmar que EDA exibe PNGs dos graficos da Phase 3

**Passo 3 — Deploy Streamlit Cloud (opcional — INFRA-01):**
- Fazer push do repositorio para GitHub
- Acessar share.streamlit.io — conectar repo — deploy
- Testar URL publica no celular
- Se deploy falhar com versao mismatch: ajustar requirements.txt e redeploy

**Notas importantes:**
- Se Phases 3 e 4 ainda nao foram executadas: o app deve abrir com mensagens de erro informativas (FileNotFoundError tratado), nao com traceback nu
- O deploy no Streamlit Cloud e desejavel mas nao bloqueante — Plano B local e suficiente para a apresentacao
  </how-to-verify>
  <resume-signal>
Digite "aprovado" se o app abre sem traceback e as paginas respondem conforme esperado.
Ou descreva os erros encontrados para que Claude corrija antes da apresentacao.
  </resume-signal>
</task>

</tasks>

<verification>
- `python -m py_compile app.py utils/loaders.py pages/*.py` — todos compilam
- `docs/demo_checklist.md` existe com 5 cenarios e plano B
- App abre em `streamlit run app.py` sem ImportError
- Cada pagina navega sem traceback (erros de FileNotFoundError sao capturados e exibidos como st.error/warning)
- (Pos-Phases-3-4) Preditor retorna score ao submeter formulario com valores validos
- (Pos-Phases-3-4) Mapa renderiza 27 estados coloridos
</verification>

<success_criteria>
- Todos os arquivos Python compilam sem erro de sintaxe
- App inicia com `streamlit run app.py` em menos de 3 segundos (cold start local, sem artefatos pesados carregados ate que pagina seja visitada)
- Erros de artefatos ausentes (pipeline, parquet, PNGs) sao tratados com mensagem amigavel — nao com traceback nu
- docs/demo_checklist.md contem roteiro de 5 minutos com 5 cenarios especificos e plano B documentado
- (Opcional) URL publica Streamlit Cloud acessivel no celular
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/06-demo-streamlit-e-integracao-final/06-05-SUMMARY.md`
</output>
