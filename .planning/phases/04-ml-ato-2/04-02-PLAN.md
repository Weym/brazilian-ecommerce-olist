---
phase: 04-ml-ato-2
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - notebooks/FASE4-P4-ml-pipeline.ipynb
  - models/final_pipeline.joblib
  - reports/figures/shap_beeswarm.png
autonomous: true
requirements:
  - ML-03
  - ML-04

must_haves:
  truths:
    - "O pipeline XGBoost usa as mesmas NUMERIC_FEATURES e CATEGORICAL_FEATURES do baseline — zero features pos-entrega"
    - "scale_pos_weight e calculado em y_train (nao em y completo) — sem leakage de hiperparametro"
    - "PR-AUC do XGBoost e superior ao PR-AUC do baseline (assert final_pr_auc > baseline_pr_auc)"
    - "SHAP TreeExplainer roda em amostra de ate 5000 linhas do test set transformado — sem timeout"
    - "reports/figures/shap_beeswarm.png existe com top-15 features, salvo em 150 dpi"
    - "models/final_pipeline.joblib existe e contem o Pipeline completo (preprocessor + XGBClassifier), nao apenas o estimador"
  artifacts:
    - path: "notebooks/FASE4-P4-ml-pipeline.ipynb"
      provides: "Secoes 3 (XGBoost) e 4 (SHAP) adicionadas ao notebook"
    - path: "models/final_pipeline.joblib"
      provides: "Pipeline XGBoost completo serializado para uso no Streamlit"
      exports:
        - final_pipeline
    - path: "reports/figures/shap_beeswarm.png"
      provides: "Beeswarm SHAP top-15 features para slides e Streamlit"
  key_links:
    - from: "notebooks/FASE4-P4-ml-pipeline.ipynb"
      to: "models/final_pipeline.joblib"
      via: "joblib.dump(final_pipeline, '../models/final_pipeline.joblib')"
      pattern: "final_pipeline.joblib"
    - from: "notebooks/FASE4-P4-ml-pipeline.ipynb"
      to: "reports/figures/shap_beeswarm.png"
      via: "plt.savefig('../reports/figures/shap_beeswarm.png')"
      pattern: "shap_beeswarm.png"
    - from: "models/final_pipeline.joblib"
      to: "app/pages/03_modelo.py"
      via: "joblib.load('models/final_pipeline.joblib')"
      pattern: "final_pipeline.joblib"
---

<objective>
Adicionar as secoes 3 e 4 ao notebook: treinamento do XGBoost (com scale_pos_weight correto) e analise SHAP com beeswarm salvo em PNG.

Purpose: O XGBoost e o modelo final da apresentacao — precisa superar o baseline em PR-AUC e ter explicabilidade via SHAP para o Ato 2 dos slides. O beeswarm e o grafico que responde "quais fatores preveem risco antes da entrega?".
Output: Secoes 3-4 no notebook, models/final_pipeline.joblib, reports/figures/shap_beeswarm.png.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ml-ato-2/04-CONTEXT.md
@.planning/phases/04-ml-ato-2/04-RESEARCH.md
@.planning/phases/04-ml-ato-2/04-01-SUMMARY.md

<interfaces>
<!-- Variaveis e objetos disponiveis no notebook apos execucao do Plan 01 -->
<!-- O executor pode assumir que estas variaveis existem no kernel ao continuar o notebook -->

```python
# Disponiveis apos Secao 1:
X           # DataFrame[PRE_DELIVERY_FEATURES], todos os pedidos
y           # Series[int], TARGET_COLUMN = "bad_review"
X_train     # 80% estratificado
X_test      # 20% estratificado
y_train     # target treino
y_test      # target teste
NUMERIC_FEATURES    # list[str], 9 colunas numericas
CATEGORICAL_FEATURES  # list[str], 4 colunas categoricas

# Disponiveis apos Secao 2:
preprocessor        # ColumnTransformer (fitted em X_train)
baseline_pipeline   # Pipeline completo (fitted)
baseline_pr_auc     # float — PR-AUC do baseline no test set
eval_model          # funcao helper para reportar PR-AUC + classification_report
```

<!-- Hiperparametros XGBoost (Claude's Discretion per CONTEXT.md): -->
```python
XGBClassifier(
    n_estimators=300,
    max_depth=4,
    learning_rate=0.05,
    scale_pos_weight=neg/pos,  # calculado em y_train
    eval_metric="aucpr",
    random_state=42,
    n_jobs=-1,
)
```

<!-- SHAP: salvar em amostra de 5000 (blocker documentado em STATE.md) -->
<!-- feature_names: NUMERIC_FEATURES + OHE get_feature_names_out(CATEGORICAL_FEATURES) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Secao 3 — XGBoost com scale_pos_weight</name>
  <files>notebooks/FASE4-P4-ml-pipeline.ipynb, models/final_pipeline.joblib</files>
  <behavior>
    - Test 1: scale_pos_weight calculado em y_train (nao em y) — neg/pos onde neg=(y_train==0).sum(), pos=(y_train==1).sum()
    - Test 2: final_pipeline e um sklearn Pipeline com steps "preprocessor" e "classifier"
    - Test 3: O preprocessor do final_pipeline e o mesmo objeto do baseline (mesmas transformacoes, fitted em X_train)
    - Test 4: XGBClassifier tem n_estimators=300, max_depth=4, learning_rate=0.05, eval_metric="aucpr"
    - Test 5: final_pr_auc > baseline_pr_auc (assert embutido no notebook)
    - Test 6: models/final_pipeline.joblib existe e round-trip: predict_proba(X_test.head(3)) funciona
  </behavior>
  <action>
    Adicionar celulas da Secao 3 ao notebook, apos as celulas da Secao 2.

    **Celula 3.1 — scale_pos_weight e construcao do pipeline XGBoost:**
    ```python
    # scale_pos_weight DEVE ser calculado em y_train — nao em y completo
    neg = (y_train == 0).sum()
    pos = (y_train == 1).sum()
    scale_pos_weight = neg / pos
    print(f"scale_pos_weight: {scale_pos_weight:.2f} (neg={neg:,}, pos={pos:,})")

    final_pipeline = Pipeline(steps=[
        ("preprocessor", preprocessor),  # mesmo ColumnTransformer do baseline (ja fitted em X_train)
        ("classifier", XGBClassifier(
            n_estimators=300,
            max_depth=4,
            learning_rate=0.05,
            scale_pos_weight=scale_pos_weight,
            eval_metric="aucpr",
            random_state=42,
            n_jobs=-1,
            # NOTA: nao incluir use_label_encoder — removido no XGBoost 1.6+
        )),
    ])

    final_pipeline.fit(X_train, y_train)
    print("XGBoost pipeline treinado.")
    ```

    **Celula 3.2 — Avaliacao e comparacao com baseline:**
    ```python
    final_pr_auc = eval_model(final_pipeline, X_test, y_test, "XGBoost Final")

    # Garantia de que o XGBoost supera o baseline
    assert final_pr_auc > baseline_pr_auc, \
        f"XGBoost ({final_pr_auc:.4f}) deve superar baseline ({baseline_pr_auc:.4f})"
    print(f"\nMelhora sobre baseline: +{final_pr_auc - baseline_pr_auc:.4f} PR-AUC")
    ```

    **Celula 3.3 — Serializar pipeline final:**
    ```python
    import os
    os.makedirs("../models", exist_ok=True)

    # Serializar o Pipeline COMPLETO (preprocessor + estimador) — nao apenas o XGBClassifier
    joblib.dump(final_pipeline, "../models/final_pipeline.joblib")
    print("Salvo: models/final_pipeline.joblib")

    # Round-trip verification — critico para garantir que o Streamlit conseguira carregar
    loaded_final = joblib.load("../models/final_pipeline.joblib")
    sample_scores = loaded_final.predict_proba(X_test.head(3))[:, 1]
    print(f"Round-trip OK: {sample_scores.round(3)}")
    ```

    ATENCAO: Ao construir `final_pipeline`, usar o mesmo objeto `preprocessor` que foi criado na Secao 2. O `preprocessor` ja esta fitted em X_train — quando o `Pipeline` chama `fit(X_train, y_train)`, o sklearn refaz o fit do preprocessor no X_train novamente (comportamento correto do Pipeline). NAO criar um novo ColumnTransformer para o XGBoost — usar a mesma definicao.

    PROIBIDO: `use_label_encoder=False` no XGBClassifier — esse parametro foi removido no XGBoost 1.6+ e causara TypeError.
  </action>
  <verify>
    <automated>cd /c/Users/Wey/Desktop/Alpha/Projetos/python && python -c "
import os, json, joblib

# Notebook contem Secao 3
nb = json.load(open('notebooks/FASE4-P4-ml-pipeline.ipynb'))
src = ' '.join(''.join(c['source']) for c in nb['cells'] if c['cell_type'] == 'code')

assert 'XGBClassifier' in src, 'FAIL: XGBClassifier ausente'
assert 'scale_pos_weight' in src, 'FAIL: scale_pos_weight ausente'
assert 'y_train' in src and 'neg' in src and 'pos' in src, 'FAIL: calculo de scale_pos_weight em y_train ausente'
assert 'n_estimators' in src, 'FAIL: n_estimators ausente'
assert 'final_pr_auc > baseline_pr_auc' in src, 'FAIL: assert de superioridade do XGBoost ausente'
assert 'final_pipeline.joblib' in src, 'FAIL: serializacao do final_pipeline ausente'
assert 'use_label_encoder' not in src, 'FAIL: use_label_encoder presente (parametro removido no XGBoost 1.6+)'
print('OK: Secao 3 presente e correta no notebook')

# Joblib existe
assert os.path.exists('models/final_pipeline.joblib'), 'FAIL: models/final_pipeline.joblib nao encontrado'
loaded = joblib.load('models/final_pipeline.joblib')
assert hasattr(loaded, 'predict_proba'), 'FAIL: objeto carregado nao tem predict_proba'
assert hasattr(loaded, 'named_steps'), 'FAIL: nao e um sklearn Pipeline'
assert 'preprocessor' in loaded.named_steps, 'FAIL: preprocessor ausente no pipeline serializado'
assert 'classifier' in loaded.named_steps, 'FAIL: classifier ausente no pipeline serializado'
print('OK: final_pipeline.joblib carrega como Pipeline completo com preprocessor + classifier')
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <done>
    - Secao 3 do notebook tem celulas: calculo de scale_pos_weight em y_train, Pipeline XGBoost (n_estimators=300, max_depth=4, lr=0.05), avaliacao com assert final_pr_auc > baseline_pr_auc, serializacao com round-trip
    - models/final_pipeline.joblib existe, e um sklearn Pipeline completo (named_steps: preprocessor + classifier), carregavel via joblib.load
    - use_label_encoder NAO aparece no codigo
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Secao 4 — SHAP TreeExplainer e beeswarm PNG</name>
  <files>notebooks/FASE4-P4-ml-pipeline.ipynb, reports/figures/shap_beeswarm.png</files>
  <behavior>
    - Test 1: X_test_transformed usa pipeline.named_steps["preprocessor"].transform(X_test) — nao o DataFrame X_test bruto
    - Test 2: feature_names combina NUMERIC_FEATURES + OHE.get_feature_names_out(CATEGORICAL_FEATURES)
    - Test 3: Amostra SHAP e min(5000, len(X_test_transformed)) — nao o test set completo
    - Test 4: shap.TreeExplainer(xgb_model) onde xgb_model = final_pipeline.named_steps["classifier"]
    - Test 5: shap.summary_plot com max_display=15 e show=False (salvo em arquivo, nao exibido inline)
    - Test 6: reports/figures/shap_beeswarm.png existe com tamanho > 10KB
  </behavior>
  <action>
    Adicionar celulas da Secao 4 ao notebook, apos as celulas da Secao 3.

    **Celula 4.1 — Transformar dados e extrair nomes de features:**
    ```python
    # Extrair classificador e transformar dados — OBRIGATORIO antes do SHAP
    xgb_model = final_pipeline.named_steps["classifier"]
    X_test_transformed = final_pipeline.named_steps["preprocessor"].transform(X_test)

    # Nomes de features apos OneHotEncoder
    ohe = final_pipeline.named_steps["preprocessor"].named_transformers_["cat"]
    feature_names = NUMERIC_FEATURES + list(ohe.get_feature_names_out(CATEGORICAL_FEATURES))

    print(f"Features transformadas: {len(feature_names)}")
    print(f"Dimensao X_test_transformed: {X_test_transformed.shape}")
    ```

    **Celula 4.2 — SHAP em amostra de ate 5000 linhas (performance):**
    ```python
    # Amostra maxima de 5000 — TreeExplainer pode levar 10+ min em 100k linhas
    # 5000 amostras sao suficientes para um beeswarm estavel (padrao de importancia converge acima de 2k)
    sample_size = min(5000, len(X_test_transformed))
    X_shap = X_test_transformed[:sample_size]

    print(f"SHAP em {sample_size:,} amostras...")
    explainer = shap.TreeExplainer(xgb_model)
    shap_values = explainer.shap_values(X_shap)
    print(f"SHAP calculado. Shape: {shap_values.shape}")
    ```

    **Celula 4.3 — Beeswarm PNG top-15 features:**
    ```python
    import os
    os.makedirs("../reports/figures", exist_ok=True)

    plt.figure(figsize=(10, 8))
    shap.summary_plot(
        shap_values,
        X_shap,
        feature_names=feature_names,
        max_display=15,
        show=False,  # CRITICO: show=False para salvar em arquivo sem abrir janela
    )
    plt.title("SHAP — Top 15 Features: Pre-Delivery Risk Model", fontsize=13, pad=12)
    plt.tight_layout()
    plt.savefig("../reports/figures/shap_beeswarm.png", dpi=150, bbox_inches="tight")
    plt.close()
    print("Salvo: reports/figures/shap_beeswarm.png")
    ```

    **Celula 4.4 — Exibir top features numericamente:**
    ```python
    import pandas as pd
    import numpy as np

    mean_abs_shap = np.abs(shap_values).mean(axis=0)
    shap_df = (
        pd.DataFrame({"feature": feature_names, "mean_abs_shap": mean_abs_shap})
        .sort_values("mean_abs_shap", ascending=False)
        .head(15)
        .reset_index(drop=True)
    )
    print("Top 15 features por importancia SHAP (mean |SHAP value|):")
    print(shap_df.to_string(index=False))
    ```

    CRITICO — ANTI-PATTERNS A EVITAR:
    1. NUNCA chamar `explainer.shap_values(X_test)` com o DataFrame pandas bruto — XGBoost espera array numerico; sempre usar `X_test_transformed`
    2. NUNCA `shap.summary_plot(..., show=True)` — em ambiente sem display grafico isso trava. Sempre `show=False` + `plt.savefig` + `plt.close()`
    3. NUNCA usar `shap.Explainer(xgb_model)` — usar especificamente `shap.TreeExplainer` para XGBoost (~10x mais rapido)
  </action>
  <verify>
    <automated>cd /c/Users/Wey/Desktop/Alpha/Projetos/python && python -c "
import os, json

# Notebook contem Secao 4
nb = json.load(open('notebooks/FASE4-P4-ml-pipeline.ipynb'))
src = ' '.join(''.join(c['source']) for c in nb['cells'] if c['cell_type'] == 'code')

assert 'TreeExplainer' in src, 'FAIL: shap.TreeExplainer ausente'
assert 'preprocessor' in src and 'transform(X_test)' in src, 'FAIL: transformacao de X_test ausente (deve usar preprocessor.transform)'
assert 'get_feature_names_out' in src, 'FAIL: nomes de features OHE ausentes'
assert 'min(5000' in src or 'min(5_000' in src, 'FAIL: amostragem de 5000 ausente'
assert 'show=False' in src, 'FAIL: show=False ausente (necessario para salvar sem display)'
assert 'max_display=15' in src, 'FAIL: max_display=15 ausente'
assert 'shap_beeswarm.png' in src, 'FAIL: caminho shap_beeswarm.png ausente'
print('OK: Secao 4 presente e correta no notebook')

# PNG existe
assert os.path.exists('reports/figures/shap_beeswarm.png'), 'FAIL: reports/figures/shap_beeswarm.png nao encontrado'
size_kb = os.path.getsize('reports/figures/shap_beeswarm.png') / 1024
assert size_kb > 10, f'FAIL: shap_beeswarm.png muito pequeno ({size_kb:.1f} KB) — pode estar corrompido'
print(f'OK: reports/figures/shap_beeswarm.png existe ({size_kb:.1f} KB)')
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <done>
    - Secao 4 do notebook tem celulas: transformacao de X_test via preprocessor, feature_names com OHE.get_feature_names_out, SHAP TreeExplainer em amostra de min(5000, len), beeswarm salvo com show=False + plt.savefig, tabela numerica de top-15 features
    - reports/figures/shap_beeswarm.png existe com tamanho > 10KB
    - Nenhum uso de shap.Explainer (auto) ou de X_test pandas bruto no SHAP
  </done>
</task>

</tasks>

<verification>
Verificacao integrada apos conclusao do plano:

```python
import os, json, joblib

# Notebook existe
nb = json.load(open("notebooks/FASE4-P4-ml-pipeline.ipynb"))
src = " ".join("".join(c["source"]) for c in nb["cells"] if c["cell_type"] == "code")

# XGBoost presente
assert "XGBClassifier" in src
assert "scale_pos_weight" in src
assert "y_train" in src  # calculado em train, nao full dataset

# SHAP presente e correto
assert "TreeExplainer" in src
assert "transform(X_test)" in src
assert "show=False" in src

# Artefatos existem
assert os.path.exists("models/final_pipeline.joblib")
assert os.path.exists("reports/figures/shap_beeswarm.png")

# Pipeline e completo (nao apenas estimador)
loaded = joblib.load("models/final_pipeline.joblib")
assert "preprocessor" in loaded.named_steps
assert "classifier" in loaded.named_steps

print("PASS: XGBoost + SHAP completos, artefatos gerados")
```
</verification>

<success_criteria>
- Secao 3 do notebook treina XGBoost com scale_pos_weight calculado em y_train, reporta PR-AUC > baseline (assert embutido), serializa models/final_pipeline.joblib como Pipeline completo
- Secao 4 do notebook calcula SHAP via TreeExplainer em amostra de ate 5000 linhas do test set TRANSFORMADO (nao DataFrame bruto), salva beeswarm top-15 em reports/figures/shap_beeswarm.png com 150 dpi
- models/final_pipeline.joblib carregavel via joblib.load, contem named_steps: preprocessor + classifier
- reports/figures/shap_beeswarm.png existe com tamanho > 10KB
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/04-ml-ato-2/04-02-SUMMARY.md`
</output>
