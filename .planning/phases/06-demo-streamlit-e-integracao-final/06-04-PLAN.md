---
phase: 06-demo-streamlit-e-integracao-final
plan: "04"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - pages/4_EDA.py
autonomous: true
requirements:
  - PRES-05

must_haves:
  truths:
    - "Pagina EDA exibe PNGs de reports/figures/ como imagens estaticas sem reprocessamento"
    - "Navegacao entre graficos via st.selectbox funciona sem rebuild ou rerun pesado"
    - "Se reports/figures/ estiver vazio (Phase 3 ainda nao executada), pagina mostra mensagem amigavel em vez de crashar"
    - "Cada grafico ocupa largura total da pagina com use_container_width=True"
    - "Nomes dos graficos no selectbox sao legiveis (stem do arquivo, sem extensao)"
  artifacts:
    - path: "pages/4_EDA.py"
      provides: "Pagina Streamlit com selectbox de navegacao e exibicao de PNGs estaticos"
      min_lines: 40
  key_links:
    - from: "pages/4_EDA.py"
      to: "utils/loaders.list_eda_figures()"
      via: "import no topo"
      pattern: "from utils.loaders import list_eda_figures"
    - from: "st.image()"
      to: "reports/figures/*.png"
      via: "path string do Path object"
      pattern: "st\\.image"
---

<objective>
Construir a pagina de EDA: galeria navegavel de PNGs exportados na Phase 3, com selectbox para selecionar o grafico e exibicao em largura total. Simples, instantanea, sem reprocessamento.

Purpose: O Ato 1 da apresentacao precisa de evidencias visuais navegaveis ao vivo. Em vez de abrir slides separados, o apresentador mostra os graficos diretamente no app, mantendo o contexto do dashboard.

Output: pages/4_EDA.py completo, leve e resiliente (nao quebra se Phase 3 ainda nao foi executada).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-demo-streamlit-e-integracao-final/06-CONTEXT.md
@.planning/phases/06-demo-streamlit-e-integracao-final/06-RESEARCH.md

<interfaces>
<!-- Contrato de utils/loaders.py (criado no plano 06-01) -->

```python
from utils.loaders import list_eda_figures

list_eda_figures() -> list[Path]
# Retorna: lista de Path objects para *.png em reports/figures/, ordenados por nome
# Retorna lista vazia [] se o diretorio nao existir (nao levanta excecao)
# Ex: [PosixPath('reports/figures/01_atraso_vs_nota.png'), ...]

# Para exibir uma figura:
# st.image(str(figura_path), use_container_width=True)
# st.image aceita path string diretamente — PIL nao necessario se sem resize
```

Figuras esperadas da Phase 3 (EDA-01 a EDA-05) em reports/figures/:
- Algo como: atraso_vs_nota_boxplot.png, frete_vs_nota.png, choropleth_uf.png, heatmap_rotas.png
- Nomes exatos desconhecidos ate Phase 3 ser executada — usar stem do arquivo como label
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pagina EDA — selectbox + st.image navegavel</name>
  <files>pages/4_EDA.py</files>
  <action>
Criar pages/4_EDA.py com navegacao por selectbox entre PNGs de reports/figures/. Pagina deve ser resiliente: se o diretorio nao existir ou estiver vazio (Phase 3 ainda nao executada), mostrar mensagem clara em vez de KeyError ou FileNotFoundError.

**Nao usar PIL:** st.image() aceita path string diretamente. PIL adicionaria dependencia desnecessaria.

**Nao usar st.cache_data em st.image:** Streamlit ja faz cache de imagens internamente. list_eda_figures() ja esta cacheada em loaders.py.

**Labels legiveis:** Converter stem do arquivo em titulo humanizado — substituir underscores por espacos, title case.

```python
import streamlit as st
from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).parent.parent))

from utils.loaders import list_eda_figures

st.set_page_config(
    page_title="Painel de EDA — Olist",
    page_icon="📊",
    layout="wide",
)

st.title("Painel de EDA — Ato 1: Logistica Degrada Satisfacao")
st.markdown(
    "Graficos exportados da Phase 3 que provam o impacto logistico nas avaliacoes. "
    "Use o seletor abaixo para navegar entre as analises."
)

# Carregar lista de figuras via loader (cacheado em loaders.py)
figures = list_eda_figures()

if not figures:
    st.warning(
        "Nenhuma figura encontrada em reports/figures/. "
        "Execute a Phase 3 (EDA — Ato 1) para gerar os graficos."
    )
    st.info(
        "Figuras esperadas: boxplot atraso vs nota, scatter frete vs nota, "
        "choropleth UF, heatmap rotas origem-destino."
    )
    st.stop()

# Criar labels legiveis a partir do stem do arquivo
def make_label(path: Path) -> str:
    """Converte nome de arquivo em label humanizado para o selectbox."""
    stem = path.stem
    # Remover prefixos numericos tipo "01_", "02_"
    if stem[:3].replace("_", "").isdigit():
        stem = stem[3:]
    return stem.replace("_", " ").replace("-", " ").title()

fig_labels = [make_label(f) for f in figures]
fig_map = dict(zip(fig_labels, figures))

# Selectbox de navegacao
col_select, col_info = st.columns([3, 1])
with col_select:
    selected_label = st.selectbox(
        "Selecione o Grafico",
        options=fig_labels,
        help=f"{len(figures)} graficos disponíveis",
    )
with col_info:
    st.metric("Graficos Disponíveis", len(figures))

selected_path = fig_map[selected_label]

# Exibir figura selecionada
st.subheader(selected_label)
st.image(str(selected_path), use_container_width=True)

# Contexto narrativo por grafico
CONTEXT_MAP = {
    # Mapeamento parcial — adicionar conforme Phase 3 define nomes
    "atraso": "Dias de atraso vs nota de avaliacao: atrasos acima de 5 dias correlacionam fortemente com avaliacoes 1-2 estrelas.",
    "frete": "Frete alto relativo ao valor do pedido aumenta insatisfacao — especialmente em categorias de baixo ticket.",
    "choropleth": "Concentracao geografica de avaliacoes ruins por UF — Norte e Nordeste apresentam maiores taxas.",
    "heatmap": "Rotas com maior concentracao de atrasos: pares de UF origem-destino mais criticos.",
    "rota": "Corredores logisticos criticos — pares origem-destino com maior risco de atraso.",
}

# Buscar contexto relevante pelo nome do arquivo
label_lower = selected_label.lower()
context_text = None
for keyword, text in CONTEXT_MAP.items():
    if keyword in label_lower:
        context_text = text
        break

if context_text:
    st.caption(f"Interpretacao: {context_text}")

# Navegacao rapida com botoes prev/next
st.divider()
col_prev, col_idx, col_next = st.columns([1, 2, 1])
current_idx = fig_labels.index(selected_label)

with col_prev:
    if current_idx > 0:
        if st.button("← Anterior", use_container_width=True):
            st.session_state["eda_selected"] = fig_labels[current_idx - 1]
            st.rerun()
with col_idx:
    st.caption(f"Grafico {current_idx + 1} de {len(figures)}")
with col_next:
    if current_idx < len(figures) - 1:
        if st.button("Proximo →", use_container_width=True):
            st.session_state["eda_selected"] = fig_labels[current_idx + 1]
            st.rerun()
```

Nota sobre navegacao por botoes: Os botoes prev/next sao um bonus de usabilidade para apresentacao ao vivo (apresentador pode avan car sem usar o selectbox). O selectbox ja funciona independentemente.
  </action>
  <verify>
    <automated>cd "C:/Users/Wey/Desktop/Alpha/Projetos/python" && python -c "import ast; ast.parse(open('pages/4_EDA.py').read()); print('Syntax OK')" && python -c "content=open('pages/4_EDA.py').read(); assert 'st.set_page_config' in content; assert 'st.selectbox' in content; assert 'st.image' in content; assert 'use_container_width=True' in content; assert 'list_eda_figures' in content; assert 'st.stop()' in content; print('Estrutura OK')"</automated>
  </verify>
  <done>pages/4_EDA.py existe com sintaxe valida. Contem: st.set_page_config, st.selectbox para navegar entre PNGs, st.image com use_container_width=True, tratamento gracioso para diretorio vazio (st.warning + st.stop()), botoes de navegacao prev/next. Nenhuma chamada direta a open() ou Path.glob() — tudo via list_eda_figures() de loaders.</done>
</task>

</tasks>

<verification>
- `python -c "import ast; ast.parse(open('pages/4_EDA.py').read())"` — sem SyntaxError
- `grep "st.set_page_config" pages/4_EDA.py` — primeira chamada Streamlit
- `grep "use_container_width=True" pages/4_EDA.py` — imagem em largura total
- `grep "st.stop()" pages/4_EDA.py` — tratamento gracioso para diretorio vazio
- `grep "list_eda_figures" pages/4_EDA.py` — importando via loaders
</verification>

<success_criteria>
- Pagina renderiza sem erro mesmo com reports/figures/ vazio (mostra warning em vez de crashar)
- Quando PNGs existem, selectbox lista todos com labels humanizados
- Imagem selecionada ocupa largura total com use_container_width=True
- Botoes prev/next permitem navegacao sequencial para apresentacao ao vivo
- Nenhum processamento de imagem ao vivo — apenas st.image(path_string)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/06-demo-streamlit-e-integracao-final/06-04-SUMMARY.md`
</output>
