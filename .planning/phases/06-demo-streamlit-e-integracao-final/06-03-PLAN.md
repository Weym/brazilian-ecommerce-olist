---
phase: 06-demo-streamlit-e-integracao-final
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - pages/3_Mapa.py
autonomous: true
requirements:
  - PRES-04

must_haves:
  truths:
    - "Choropleth dos estados brasileiros renderiza com 27 estados coloridos por % bad reviews"
    - "Filtros de UF origem, UF destino, categoria e faixa de risco sao interativos e atualizam o mapa sem reload de pagina"
    - "Hover sobre cada estado exibe 3 metricas: % bad review, atraso medio em dias, volume de pedidos"
    - "GeoJSON carregado de arquivo local data/geo/brazil-states.geojson (nao via HTTP ao vivo)"
    - "Dados filtrados vem de geo_aggregated.parquet via load_geo_data() — nenhuma query SQL ou join ao vivo"
  artifacts:
    - path: "pages/3_Mapa.py"
      provides: "Pagina Streamlit com choropleth interativo, filtros e hover informativo"
      min_lines: 80
  key_links:
    - from: "pages/3_Mapa.py"
      to: "utils/loaders.load_geo_data()"
      via: "import no topo"
      pattern: "from utils.loaders import load_geo_data"
    - from: "pages/3_Mapa.py"
      to: "utils/loaders.load_brazil_geojson()"
      via: "import no topo"
      pattern: "from utils.loaders import load_brazil_geojson"
    - from: "px.choropleth"
      to: "properties.sigla"
      via: "featureidkey='properties.sigla'"
      pattern: "featureidkey.*sigla"
---

<objective>
Construir a pagina do Mapa Geografico: choropleth Plotly dos estados brasileiros colorizado por % de avaliacoes ruins, com 4 filtros interativos e hover com 3 metricas por estado.

Purpose: O mapa cria a ponte visual entre EDA (onde a dor esta concentrada geograficamente) e o ML (faixas de risco por regiao). E a visualizacao mais impactante para plateia — mostra imediatamente quais estados tem maior concentracao de problemas.

Output: pages/3_Mapa.py completo, dependendo de utils/loaders.py (Wave 1) e rodando em Wave 2.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-demo-streamlit-e-integracao-final/06-CONTEXT.md
@.planning/phases/06-demo-streamlit-e-integracao-final/06-RESEARCH.md

<interfaces>
<!-- Contratos de utils/loaders.py (criado no plano 06-01, disponivel na Wave 2) -->

```python
# utils/loaders.py — funcoes disponíveis
from utils.loaders import load_geo_data, load_brazil_geojson

load_geo_data() -> pd.DataFrame
# Colunas esperadas (OPEN QUESTION — verificar com df.columns.tolist() em runtime):
# uf_destino      str  — sigla UF destino ("SP", "RJ") — usado como 'locations' no choropleth
# uf_origem       str  — sigla UF origem
# pct_bad_review  float — percentual de reviews 1-2 estrelas (0.0-1.0 ou 0-100, verificar)
# atraso_medio_dias float — atraso medio em dias
# volume_pedidos  int   — quantidade de pedidos
# categoria       str   — categoria do produto (nome pode variar)
# faixa_risco     str   — "Baixo"/"Medio"/"Alto" (pode nao existir — calcular se necessario)
# AVISO: nomes podem ser diferentes. Adicionar celula de debug: st.write(df.columns.tolist())

load_brazil_geojson() -> dict
# GeoJSON com 27 features (estados)
# featureidkey correto: "properties.sigla" — string uppercase "SP", "RJ", etc.
# NAO usar "id" ou "properties.id" — campo errado causa estados em branco
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pagina Mapa — choropleth + filtros interativos + hover</name>
  <files>pages/3_Mapa.py</files>
  <action>
Criar pages/3_Mapa.py com choropleth Plotly dos estados brasileiros, 4 filtros interativos em colunas horizontais e hover informativo.

**CRITICO — featureidkey:** Usar `featureidkey="properties.sigla"` para casar com o GeoJSON codeforamerica. Se estados ficarem cinza (mismatch), o problema e aqui.

**CRITICO — nomes de colunas:** geo_aggregated.parquet foi criado na Phase 3 e os nomes exatos sao desconhecidos ate Phase 3 ser executada. O codigo deve detectar os nomes das colunas em runtime com fallbacks gracious. Adicionar bloco de debug expansivel mostrando `df.columns.tolist()` para facilitar ajuste se necessario.

**CRITICO — pct_bad_review:** Verificar se o valor esta em escala 0-1 ou 0-100. O choropleth deve exibir como percentual legivel (multiplicar por 100 se necessario).

```python
import streamlit as st
import pandas as pd
import plotly.express as px
from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).parent.parent))

from utils.loaders import load_geo_data, load_brazil_geojson

st.set_page_config(
    page_title="Mapa Geografico — Olist",
    page_icon="🗺️",
    layout="wide",
)


def detect_column(df: pd.DataFrame, candidates: list, default: str) -> str:
    """Detecta nome de coluna tentando candidatos em ordem. Retorna default se nenhum encontrado."""
    for c in candidates:
        if c in df.columns:
            return c
    return default


st.title("Mapa de Risco Logistico por Estado")
st.markdown("Concentracao de avaliacoes ruins (1-2 estrelas) por UF de destino.")

# Carregamento via loaders (cache_data — instantaneo apos primeira visita)
df_raw = load_geo_data()
geojson = load_brazil_geojson()

# Detectar colunas (OPEN QUESTION — dependem de Phase 3)
COL_UF_DEST   = detect_column(df_raw, ["uf_destino", "customer_state", "estado_destino"], "uf_destino")
COL_UF_ORIG   = detect_column(df_raw, ["uf_origem", "seller_state", "estado_origem"], "uf_origem")
COL_PCT_BAD   = detect_column(df_raw, ["pct_bad_review", "bad_review_rate", "taxa_bad_review", "pct_ruim"], "pct_bad_review")
COL_ATRASO    = detect_column(df_raw, ["atraso_medio_dias", "delay_avg_days", "dias_atraso_medio"], "atraso_medio_dias")
COL_VOLUME    = detect_column(df_raw, ["volume_pedidos", "total_orders", "n_pedidos"], "volume_pedidos")
COL_CATEGORIA = detect_column(df_raw, ["categoria", "product_category_name_english", "category"], "categoria")
COL_FAIXA     = detect_column(df_raw, ["faixa_risco", "risk_level", "nivel_risco"], "faixa_risco")

# Normalizar pct_bad_review para escala 0-100 se necessario
df = df_raw.copy()
if COL_PCT_BAD in df.columns and df[COL_PCT_BAD].max() <= 1.01:
    df["pct_bad_review_pct"] = df[COL_PCT_BAD] * 100
    col_color = "pct_bad_review_pct"
    color_label = "% Aval. Ruins"
else:
    col_color = COL_PCT_BAD
    color_label = "% Aval. Ruins"

# Calcular faixa de risco se coluna nao existir no parquet
if COL_FAIXA not in df.columns:
    threshold_bad = df[col_color].quantile(0.67)
    threshold_low = df[col_color].quantile(0.33)
    df["faixa_risco"] = pd.cut(
        df[col_color],
        bins=[-1, threshold_low, threshold_bad, float("inf")],
        labels=["Baixo", "Medio", "Alto"],
    )
    COL_FAIXA = "faixa_risco"

# --- Filtros ---
st.subheader("Filtros")
col1, col2, col3, col4 = st.columns(4)

with col1:
    opts_orig = sorted(df[COL_UF_ORIG].dropna().unique().tolist()) if COL_UF_ORIG in df.columns else []
    uf_orig_filter = st.multiselect("UF Origem", options=opts_orig, default=[],
                                    help="Estado do vendedor")
with col2:
    opts_dest = sorted(df[COL_UF_DEST].dropna().unique().tolist()) if COL_UF_DEST in df.columns else []
    uf_dest_filter = st.multiselect("UF Destino", options=opts_dest, default=[],
                                    help="Estado do comprador")
with col3:
    opts_cat = sorted(df[COL_CATEGORIA].dropna().unique().tolist()) if COL_CATEGORIA in df.columns else []
    cat_filter = st.multiselect("Categoria", options=opts_cat, default=[],
                                help="Categoria do produto")
with col4:
    faixa_filter = st.multiselect("Faixa de Risco", options=["Baixo", "Medio", "Alto"], default=[],
                                   help="Faixa de risco calculada por % bad reviews")

# Aplicar filtros sem mutar df cacheado
df_filtered = df.copy()
if uf_orig_filter and COL_UF_ORIG in df_filtered.columns:
    df_filtered = df_filtered[df_filtered[COL_UF_ORIG].isin(uf_orig_filter)]
if uf_dest_filter and COL_UF_DEST in df_filtered.columns:
    df_filtered = df_filtered[df_filtered[COL_UF_DEST].isin(uf_dest_filter)]
if cat_filter and COL_CATEGORIA in df_filtered.columns:
    df_filtered = df_filtered[df_filtered[COL_CATEGORIA].isin(cat_filter)]
if faixa_filter and COL_FAIXA in df_filtered.columns:
    df_filtered = df_filtered[df_filtered[COL_FAIXA].isin(faixa_filter)]

# --- Agregacao por UF destino para o choropleth ---
# O choropleth precisa de uma linha por UF de destino
agg_dict = {col_color: "mean"}
if COL_ATRASO in df_filtered.columns:
    agg_dict[COL_ATRASO] = "mean"
if COL_VOLUME in df_filtered.columns:
    agg_dict[COL_VOLUME] = "sum"

if COL_UF_DEST not in df_filtered.columns or df_filtered.empty:
    st.warning("Sem dados para os filtros selecionados.")
    st.stop()

df_map = df_filtered.groupby(COL_UF_DEST, as_index=False).agg(agg_dict)

# --- Hover customizado ---
hover_data = {col_color: ":.1f"}
if COL_ATRASO in df_map.columns:
    hover_data[COL_ATRASO] = ":.1f"
if COL_VOLUME in df_map.columns:
    hover_data[COL_VOLUME] = ":,"

# --- Choropleth ---
fig = px.choropleth(
    df_map,
    geojson=geojson,
    locations=COL_UF_DEST,
    featureidkey="properties.sigla",   # CRITICO: campo correto no GeoJSON codeforamerica
    color=col_color,
    color_continuous_scale="Reds",
    hover_name=COL_UF_DEST,
    hover_data=hover_data,
    labels={
        col_color: color_label,
        COL_ATRASO: "Atraso Medio (dias)",
        COL_VOLUME: "Volume de Pedidos",
    },
    title="% Avaliacoes Ruins por UF de Destino",
)
fig.update_geos(fitbounds="locations", visible=False)
fig.update_layout(
    margin={"r": 0, "t": 40, "l": 0, "b": 0},
    height=500,
    coloraxis_colorbar=dict(title=color_label),
)

st.plotly_chart(fig, use_container_width=True)

# Metricas resumo
if not df_map.empty:
    m1, m2, m3 = st.columns(3)
    with m1:
        if col_color in df_map.columns:
            worst_uf = df_map.loc[df_map[col_color].idxmax(), COL_UF_DEST]
            st.metric("UF com Mais Risco", worst_uf)
    with m2:
        if COL_ATRASO in df_map.columns:
            st.metric("Atraso Medio Geral", f"{df_map[COL_ATRASO].mean():.1f} dias")
    with m3:
        if COL_VOLUME in df_map.columns:
            st.metric("Pedidos no Filtro", f"{int(df_map[COL_VOLUME].sum()):,}")

# Debug expandivel — facilita ajuste de nomes de colunas apos Phase 3 ser executada
with st.expander("Debug: colunas do geo_aggregated.parquet"):
    st.write("Colunas detectadas:", df_raw.columns.tolist())
    st.write("Mapeamento usado:", {
        "UF destino": COL_UF_DEST, "UF origem": COL_UF_ORIG,
        "% bad review": COL_PCT_BAD, "Atraso": COL_ATRASO,
        "Volume": COL_VOLUME, "Categoria": COL_CATEGORIA,
    })
    st.dataframe(df_raw.head(5))
```
  </action>
  <verify>
    <automated>cd "C:/Users/Wey/Desktop/Alpha/Projetos/python" && python -c "import ast; ast.parse(open('pages/3_Mapa.py').read()); print('Syntax OK')" && python -c "content=open('pages/3_Mapa.py').read(); assert 'st.set_page_config' in content; assert 'featureidkey' in content; assert 'properties.sigla' in content; assert 'load_geo_data' in content; assert 'load_brazil_geojson' in content; assert 'st.multiselect' in content; print('Estrutura OK')"</automated>
  </verify>
  <done>pages/3_Mapa.py existe com sintaxe valida. Contem: st.set_page_config, 4 filtros st.multiselect, px.choropleth com featureidkey="properties.sigla", hover_data com 3 metricas, deteccao de colunas com fallbacks, e bloco de debug para ajuste pos-Phase-3. Nenhuma chamada a pd.read_parquet() ou json.load() diretamente — tudo via loaders.</done>
</task>

</tasks>

<verification>
- `python -c "import ast; ast.parse(open('pages/3_Mapa.py').read())"` — sem SyntaxError
- `grep "properties.sigla" pages/3_Mapa.py` — featureidkey correto presente
- `grep "load_geo_data\|load_brazil_geojson" pages/3_Mapa.py` — importando via loaders
- `grep -c "st.multiselect" pages/3_Mapa.py` — deve ser 4 (4 filtros)
- `grep "df_raw.copy\|df_filtered = df" pages/3_Mapa.py` — nao mutando df cacheado
</verification>

<success_criteria>
- Choropleth renderiza com GeoJSON local (sem HTTP request ao vivo)
- 4 filtros multiselect aparecem em colunas horizontais e filtram os dados antes do choropleth
- Hover mostra 3 metricas por estado
- Deteccao automatica de nomes de colunas com fallbacks — nao quebra se Phase 3 usou nomes diferentes
- Bloco de debug expansivel mostra colunas reais para diagnostico rapido
- Nenhuma linha chama pd.read_parquet() ou json.load() diretamente
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/06-demo-streamlit-e-integracao-final/06-03-SUMMARY.md`
</output>
