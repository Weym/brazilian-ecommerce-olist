---
phase: 02-data-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - notebooks/FASE2-P1-data-foundation.ipynb
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-05
must_haves:
  truths:
    - "A coluna seller_customer_distance_km existe e todos os valores estao no range 0-4000 km (nao graus decimais)"
    - "A coluna estimated_days existe (diferenca em dias entre order_approved_at e order_estimated_delivery_date)"
    - "A coluna freight_ratio existe (freight_value / total_payment_value), sem divisao por zero"
    - "A coluna product_volume_cm3 existe (length * height * width)"
    - "A coluna actual_delay_days existe (order_delivered_customer_date - order_estimated_delivery_date em dias)"
    - "A coluna bad_review existe como inteiro binario (1 se review_score in {1,2}, else 0)"
    - "Cada coluna da tabela gold tem uma tag explícita [pre-entrega | pos-entrega | target] documentada em celula Markdown"
  artifacts:
    - path: "notebooks/FASE2-P1-data-foundation.ipynb"
      provides: "Secoes de feature engineering, calculo Haversine validado, target criado e coluna-tagging documentada"
      exports: ["gold_tagged DataFrame"]
  key_links:
    - from: "seller_lat, seller_lng, customer_lat, customer_lng"
      to: "seller_customer_distance_km"
      via: "haversine library ou formula manual vetorizada com numpy"
      pattern: "haversine|arcsin.*sin.*cos"
    - from: "review_score"
      to: "bad_review"
      via: "gold_tagged['bad_review'] = (gold_tagged['review_score'].isin([1,2])).astype(int)"
      pattern: "isin.*1.*2.*astype.*int"
---

<objective>
Construir todas as features derivadas da tabela gold, calcular a distancia Haversine vendedor-comprador em km, criar a coluna target `bad_review`, e documentar o tagging de colunas como [pre-entrega | pos-entrega | target].

Purpose: Este plano entrega o contrato de dados que os tracks de EDA e ML vao consumir. Sem as features derivadas corretas e sem o tagging explicito de colunas, qualquer analise downstream pode introduzir vazamento de dados. A distancia em km e especialmente critica — se sair em graus decimais (0-10), e feature corrompida.

Output: `gold_with_geo` transformado em `gold_tagged` — DataFrame com todas as features do contrato, target criado, e comentarios de tagging por coluna.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-kickoff-e-contratos/01-CONTEXT.md
@.planning/phases/02-data-foundation/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Contrato de entrada: gold_with_geo do Plan 01 -->
<!-- Colunas esperadas em gold_with_geo: -->
<!--   order_id, order_status, order_approved_at, order_estimated_delivery_date -->
<!--   order_delivered_customer_date, freight_value, payment_value (soma dos itens), -->
<!--   total_payment_value (soma dos pagamentos), seller_id, seller_state, -->
<!--   seller_lat, seller_lng, customer_id, customer_unique_id, customer_state, -->
<!--   customer_lat, customer_lng, review_score, product_category_name, -->
<!--   product_category_name_english, product_weight_g, product_photos_qty, -->
<!--   product_length_cm, product_height_cm, product_width_cm -->

<!-- Contrato de features definido em Phase 1 (src/features.py): -->
<!--   PRE_DELIVERY_FEATURES = [                                    -->
<!--     "order_id", "order_approved_at", "order_estimated_delivery_date", -->
<!--     "estimated_days", "payment_value", "freight_value", "freight_ratio", -->
<!--     "product_weight_g", "product_volume_cm3", "product_photos_qty", -->
<!--     "seller_customer_distance_km", "seller_state", "customer_state", -->
<!--     "product_category_name_english", "seller_id", "customer_id",   -->
<!--   ]                                                              -->
<!--   FORBIDDEN_FEATURES (pos-entrega): order_delivered_customer_date, -->
<!--     review_score, review_comment_message, actual_delay_days        -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Calcular distancia Haversine seller-customer em km e validar range</name>
  <files>notebooks/FASE2-P1-data-foundation.ipynb</files>
  <action>
Adicionar celulas no notebook (continuando apos o final do Plan 01) para calcular a distancia entre seller e customer usando a formula Haversine. Usar a biblioteca `haversine` (listada no stack — mais segura que formula manual) OU implementar com numpy vetorizado.

**Opcao preferida — haversine library (mais simples e testada):**

```python
# Celula 14: calcular distancia Haversine em km
# haversine(ponto1, ponto2) = (lat1, lon1), (lat2, lon2) — retorna km por padrao
from haversine import haversine_vector, Unit

# Criar arrays de coordenadas (remover linhas sem lat/lon para o calculo)
has_geo = (
    gold_with_geo["seller_lat"].notna() &
    gold_with_geo["seller_lng"].notna() &
    gold_with_geo["customer_lat"].notna() &
    gold_with_geo["customer_lng"].notna()
)

# Inicializar coluna com NaN
gold_with_geo["seller_customer_distance_km"] = np.nan

# Calcular apenas onde lat/lon existe
seller_coords = gold_with_geo.loc[has_geo, ["seller_lat", "seller_lng"]].values
customer_coords = gold_with_geo.loc[has_geo, ["customer_lat", "customer_lng"]].values

gold_with_geo.loc[has_geo, "seller_customer_distance_km"] = haversine_vector(
    seller_coords, customer_coords, unit=Unit.KILOMETERS
)
```

**Fallback se haversine nao estiver instalado — formula numpy:**

```python
# Alternativa: Haversine manual vetorizada com numpy
def haversine_km(lat1, lon1, lat2, lon2):
    R = 6371  # raio da Terra em km
    lat1, lon1, lat2, lon2 = map(np.radians, [lat1, lon1, lat2, lon2])
    dlat = lat2 - lat1
    dlon = lon2 - lon1
    a = np.sin(dlat/2)**2 + np.cos(lat1) * np.cos(lat2) * np.sin(dlon/2)**2
    return 2 * R * np.arcsin(np.sqrt(a))

gold_with_geo.loc[has_geo, "seller_customer_distance_km"] = haversine_km(
    gold_with_geo.loc[has_geo, "seller_lat"].values,
    gold_with_geo.loc[has_geo, "seller_lng"].values,
    gold_with_geo.loc[has_geo, "customer_lat"].values,
    gold_with_geo.loc[has_geo, "customer_lng"].values,
)
```

**Celula de validacao — CRITICA (verificar range km, nao graus):**

```python
# Celula 15: validar distribuicao de distancias
dist_stats = gold_with_geo["seller_customer_distance_km"].describe()
print(dist_stats)
print(f"\nMin: {dist_stats['min']:.1f} km")
print(f"Max: {dist_stats['max']:.1f} km")
print(f"Media: {dist_stats['mean']:.1f} km")

# GUARDRAIL: valores em graus decimais ficam entre 0-10, km ficam entre 0-4000
assert dist_stats["max"] > 100, f"Distancia maxima {dist_stats['max']} parece em graus, nao km — verificar formula Haversine"
assert dist_stats["max"] < 6000, f"Distancia maxima {dist_stats['max']} excede diametro do Brasil (5000km) — verificar coordenadas"
assert dist_stats["min"] >= 0, "Distancia nao pode ser negativa"

# Exemplo concreto: SP para AM deve ser ~2700 km
# Se seller_state SP e customer_state AM produzirem ~2700, formula esta correta
sp_am = gold_with_geo[
    (gold_with_geo["seller_state"] == "SP") &
    (gold_with_geo["customer_state"] == "AM")
]["seller_customer_distance_km"].median()
print(f"\nMediana distancia SP->AM: {sp_am:.0f} km (esperado ~2700 km)")
```
  </action>
  <verify>
    <automated>cd "C:/Users/Wey/Desktop/Alpha/Projetos/python" && jupyter nbconvert --to notebook --execute notebooks/FASE2-P1-data-foundation.ipynb --output notebooks/FASE2-P1-data-foundation.ipynb 2>&1 | grep -E "AssertionError|Error|km|Max:" | head -15</automated>
  </verify>
  <done>
    - Coluna seller_customer_distance_km existe no DataFrame
    - assert dist_stats["max"] > 100 passa (confirma km, nao graus)
    - assert dist_stats["max"] < 6000 passa
    - Mediana SP->AM entre 1500-4000 km (razoavel para Brazil)
    - Notebook executa sem AssertionError nesta secao
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derivar features, criar target bad_review e taguear todas as colunas</name>
  <files>notebooks/FASE2-P1-data-foundation.ipynb</files>
  <behavior>
    - estimated_days = (order_estimated_delivery_date - order_approved_at).dt.days — inteiro positivo
    - freight_ratio = freight_value / total_payment_value — float entre 0 e 1 (ou maior se frete > pedido)
    - freight_ratio deve ser NaN quando total_payment_value == 0 (evitar divisao por zero)
    - product_volume_cm3 = product_length_cm * product_height_cm * product_width_cm
    - actual_delay_days = (order_delivered_customer_date - order_estimated_delivery_date).dt.days — pode ser negativo (entrega antecipada)
    - bad_review = 1 quando review_score in {1,2}, 0 caso contrario — inteiro (0 ou 1), nunca NaN quando review_score nao e NaN
  </behavior>
  <action>
Adicionar celulas no notebook para:

**Secao 5 — Feature Engineering:**

```python
# Celula 16: features derivadas pre-entrega
gold_features = gold_with_geo.copy()

# estimated_days: dias entre aprovacao e prazo estimado
gold_features["estimated_days"] = (
    gold_features["order_estimated_delivery_date"] - gold_features["order_approved_at"]
).dt.days

# freight_ratio: proporcao do frete no pedido total (usar total_payment_value como denominador)
gold_features["freight_ratio"] = np.where(
    gold_features["total_payment_value"] > 0,
    gold_features["freight_value"] / gold_features["total_payment_value"],
    np.nan
)

# product_volume_cm3: volume do produto
gold_features["product_volume_cm3"] = (
    gold_features["product_length_cm"] *
    gold_features["product_height_cm"] *
    gold_features["product_width_cm"]
)

# Celula 17: features pos-entrega (incluir na gold table com tag, mas proibidas no ML)
gold_features["actual_delay_days"] = (
    gold_features["order_delivered_customer_date"] - gold_features["order_estimated_delivery_date"]
).dt.days  # negativo = entregue antes do prazo

# Celula 18: target binario
gold_features["bad_review"] = gold_features["review_score"].isin([1, 2]).astype(int)

# Validacoes inline
assert gold_features["bad_review"].isin([0, 1]).all(), "bad_review deve ser 0 ou 1"
assert gold_features["bad_review"].isna().sum() == 0, "bad_review nao pode ter NaN (review_score ja filtrado)"
print(f"Taxa de bad_review: {gold_features['bad_review'].mean():.1%} (esperado ~15-20%)")
print(f"estimated_days negativos: {(gold_features['estimated_days'] < 0).sum()} (deve ser 0)")
print(f"freight_ratio > 1: {(gold_features['freight_ratio'] > 1).sum()} pedidos (frete > total)")
```

**Secao 6 — Tagging de colunas:**

```python
# Celula 19: documentar tag de cada coluna — CONTRATO IMUTAVEL
COLUMN_TAGS = {
    # [pre-entrega] — disponivel ate order_approved_at
    "order_id":                         "pre-entrega | identificador",
    "order_approved_at":                "pre-entrega | ancora temporal",
    "order_estimated_delivery_date":    "pre-entrega",
    "estimated_days":                   "pre-entrega | derivada",
    "payment_value":                    "pre-entrega | soma de itens",
    "freight_value":                    "pre-entrega",
    "freight_ratio":                    "pre-entrega | derivada",
    "total_payment_value":              "pre-entrega | soma de pagamentos",
    "product_weight_g":                 "pre-entrega",
    "product_volume_cm3":               "pre-entrega | derivada",
    "product_photos_qty":               "pre-entrega",
    "seller_customer_distance_km":      "pre-entrega | derivada | Haversine km",
    "seller_id":                        "pre-entrega | identificador",
    "seller_state":                     "pre-entrega",
    "seller_city":                      "pre-entrega",
    "seller_zip_code_prefix":           "pre-entrega",
    "seller_lat":                       "pre-entrega",
    "seller_lng":                       "pre-entrega",
    "customer_id":                      "pre-entrega | identificador",
    "customer_unique_id":               "pre-entrega | identificador",
    "customer_state":                   "pre-entrega",
    "customer_city":                    "pre-entrega",
    "customer_zip_code_prefix":         "pre-entrega",
    "customer_lat":                     "pre-entrega",
    "customer_lng":                     "pre-entrega",
    "product_id":                       "pre-entrega | identificador",
    "product_category_name":            "pre-entrega",
    "product_category_name_english":    "pre-entrega",
    "product_length_cm":                "pre-entrega",
    "product_height_cm":                "pre-entrega",
    "product_width_cm":                 "pre-entrega",
    "order_status":                     "pre-entrega",
    "order_purchase_timestamp":         "pre-entrega",
    # [pos-entrega] — PROIBIDO NO MODELO ML
    "order_delivered_customer_date":    "pos-entrega | PROIBIDO NO MODELO",
    "order_delivered_carrier_date":     "pos-entrega | PROIBIDO NO MODELO",
    "actual_delay_days":                "pos-entrega | PROIBIDO NO MODELO | derivada",
    "review_score":                     "pos-entrega | PROIBIDO NO MODELO",
    # [target]
    "bad_review":                       "target | 1=insatisfacao (1-2 estrelas), 0=ok",
}

# Celula 20: imprimir tabela de tagging
print("=== COLUMN TAGS — CONTRATO DA TABELA GOLD ===\n")
for col, tag in COLUMN_TAGS.items():
    status = "[OK]" if col in gold_features.columns else "[AUSENTE]"
    print(f"  {status} {col:45s} | {tag}")

# Renomear gold_features para gold_tagged (nome final)
gold_tagged = gold_features.copy()
print(f"\nTabela gold_tagged: {gold_tagged.shape}")
print(f"Colunas: {list(gold_tagged.columns)}")
```

**Secao 7 — Validacao do target:**

```python
# Celula 21: distribuicao do target
bad_rate = gold_tagged["bad_review"].mean()
print(f"bad_review=1 (insatisfacao): {gold_tagged['bad_review'].sum()} pedidos ({bad_rate:.1%})")
print(f"bad_review=0 (ok): {(gold_tagged['bad_review']==0).sum()} pedidos ({1-bad_rate:.1%})")

# Se proporcao for muito diferente de 15-20%, documentar — afeta scale_pos_weight do XGBoost
if bad_rate < 0.10 or bad_rate > 0.35:
    print(f"AVISO: proporcao de bad_review ({bad_rate:.1%}) fora do range esperado 15-20%")
    print("Verificar criterio de filtragem de pedidos acima")
```
  </action>
  <verify>
    <automated>cd "C:/Users/Wey/Desktop/Alpha/Projetos/python" && jupyter nbconvert --to notebook --execute notebooks/FASE2-P1-data-foundation.ipynb --output notebooks/FASE2-P1-data-foundation.ipynb 2>&1 | grep -E "AssertionError|Error|bad_review|taxa" | head -10</automated>
  </verify>
  <done>
    - bad_review existe, e inteiro (0 ou 1), sem NaN
    - Taxa de bad_review impressa entre 10-35% (range aceitavel para documentacao)
    - estimated_days: nenhum valor negativo (ou minimo documentado)
    - freight_ratio: nenhum NaN onde total_payment_value > 0
    - product_volume_cm3: existe e e positivo onde dimensoes existem
    - actual_delay_days: existe com valores negativos aceitaveis (entrega antecipada)
    - Celula de COLUMN_TAGS impressa sem "[AUSENTE]" para colunas criticas
  </done>
</task>

</tasks>

<verification>
- `gold_tagged["seller_customer_distance_km"].max()` entre 100 e 6000 (km valido, nao graus)
- `gold_tagged["bad_review"].value_counts()` mostra ~15-20% de 1s
- `gold_tagged["order_id"].is_unique == True`
- `gold_tagged["estimated_days"].min() >= 0` (prazo estimado nao pode ser negativo)
- Todas as colunas listadas em COLUMN_TAGS com tag "pre-entrega" ou "pos-entrega" ou "target" existem no DataFrame
</verification>

<success_criteria>
- Notebook executa sem erro com todos os asserts passando
- seller_customer_distance_km em km (max > 100, max < 6000)
- bad_review criado como binario (0/1) sem NaN
- Todas as features pre-entrega do contrato existem: estimated_days, freight_ratio, product_volume_cm3, seller_customer_distance_km
- Colunas pos-entrega existem mas estao marcadas como PROIBIDAS no tagging
- gold_tagged pronto para ser exportado no Plan 03
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/02-data-foundation/02-02-SUMMARY.md` com:
- Taxa real de bad_review (proporcao de 1-2 estrelas no dataset filtrado)
- Estatisticas de seller_customer_distance_km (min, max, media, mediana)
- Quantidade de NaNs em seller_customer_distance_km (CEPs sem cobertura geo)
- Lista final de colunas de gold_tagged com seus tags
</output>
