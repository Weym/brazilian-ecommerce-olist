---
phase: 01-kickoff-e-contratos
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - docs/kickoff.md
autonomous: true
requirements:
  - KICK-04
  - KICK-05

must_haves:
  truths:
    - "docs/kickoff.md existe e documenta o target binario (1-2 estrelas = positivo)"
    - "O nome da coluna target 'bad_review' e a logica de derivacao estao documentados"
    - "A janela de dados (order_approved_at disponivel, excluindo canceled/unavailable) esta escrita"
    - "As regras de outlier para frete e prazo estao escritas antes da primeira linha de codigo de join"
    - "O rationale do target (insatisfacao real vs neutros) esta documentado"
  artifacts:
    - path: "docs/kickoff.md"
      provides: "Documento unificado de kickoff: target, ancora temporal, regras de outlier"
      contains: "bad_review"
  key_links:
    - from: "docs/kickoff.md"
      to: "notebooks/FASE2-P1-data-foundation.ipynb"
      via: "referencia antes de construir a tabela gold"
      pattern: "bad_review"
    - from: "docs/kickoff.md"
      to: "src/features.py"
      via: "TARGET_COLUMN = 'bad_review' definido aqui"
      pattern: "bad_review"
---

<objective>
Criar o documento de kickoff que registra formalmente o target do modelo e as regras de construcao de dados — o que e uma avaliacao ruim, como derivar o target, quais pedidos incluir, e o que fazer com outliers.

Purpose: Este documento e lido antes de qualquer notebook da Phase 2 ser aberto. Sem ele, P1 (Data Lead) pode fazer escolhas diferentes sobre filtros e outliers, invalidando o contrato da tabela gold.
Output: docs/kickoff.md com target definition, recorte temporal e regras de outlier completos.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kickoff-e-contratos/01-CONTEXT.md
@.planning/phases/01-kickoff-e-contratos/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar docs/kickoff.md</name>
  <files>docs/kickoff.md</files>
  <action>
    Criar o documento de kickoff com cinco secoes obrigatorias. Todo o conteudo e derivado diretamente das decisoes bloqueadas em CONTEXT.md — nenhuma interpretacao adicional.

    **Secao 1: Target do Modelo (KICK-04)**
    ```
    # Kickoff Document — Olist Pre-Delivery Risk Model

    ## 1. Target do Modelo

    **Definicao:** Avaliacao ruim = `review_score` in {1, 2}
    **Nome da coluna na tabela gold:** `bad_review`
    **Tipo:** Binario (int8)

    | review_score | bad_review | Interpretacao |
    |-------------|------------|---------------|
    | 1 | 1 | Insatisfacao severa |
    | 2 | 1 | Insatisfacao moderada |
    | 3 | 0 | Neutro |
    | 4 | 0 | Satisfacao moderada |
    | 5 | 0 | Satisfacao plena |

    **Derivacao (Python):**
    df["bad_review"] = df["review_score"].isin([1, 2]).astype(int)

    **Rationale:**
    - Estrelas 1-2 representam insatisfacao real e defensavel — nao e ambiguo como estrela 3 (neutro)
    - A separacao {1,2} vs {3,4,5} produz um sinal binario limpo para o modelo de classificacao
    - Estrela 3 e deliberadamente excluida do positivo para evitar ruido de clientes "mornos"
    ```

    **Secao 2: Ancora Temporal (parte de KICK-05)**
    - Variavel: `order_approved_at` (nao `order_purchase_timestamp`)
    - Rationale: usar purchase_timestamp causaria vazamento de pedidos com aprovacao atrasada, distorcendo `estimated_delivery_days`
    - Todas as features pre-entrega devem ser calculadas como: `valor - order_approved_at` ou `valor no momento da aprovacao`
    - Codigo de referencia: `df["estimated_delivery_days"] = (df["order_estimated_delivery_date"] - df["order_approved_at"]).dt.days`

    **Secao 3: Janela de Dados e Filtros de Inclusao (KICK-05)**
    Documentar exatamente o que entra na tabela gold:
    - Incluir: todos os pedidos com `order_approved_at` nao-nulo
    - Incluir: todos os pedidos com review disponivel (inner join com order_reviews)
    - **EXCLUIR**: pedidos com `order_status` in {'canceled', 'unavailable'}
    - **EXCLUIR**: pedidos sem `order_approved_at` (nao foram aprovados)
    - Resultado: somente pedidos entregues com review disponivel entram no modelo

    Adicionar nota: "Nao filtrar por data de compra — usar todo o historico disponivel. O dataset Olist abrange set/2016 a ago/2018."

    **Secao 4: Regras de Outlier (KICK-05)**
    Duas regras obrigatorias, documentadas antes da construcao da tabela gold:

    Outlier de frete:
    - Politica: NAO remover outliers de frete
    - Acao: flaggar com coluna booleana `high_freight_flag = (freight_value > media + 3*std)` para analise exploratoria
    - Motivo: fretes extremos podem ser o sinal mais forte de insatisfacao — remover perderia informacao

    Outlier de prazo:
    - Pedidos com atraso > 30 dias: incluir na EDA (Ato 1 precisa desses dados)
    - Para o ML (Phase 4): P4 decide se capeia esses valores ou trata separadamente — DOCUMENTAR a decisao no notebook FASE4-P4
    - Calcular atraso como: `delay_days = (order_delivered_customer_date - order_estimated_delivery_date).dt.days` (valores negativos = entrega antecipada)

    **Secao 5: Resumo para Revisao Rapida**
    Criar uma tabela de resumo de uma pagina com todas as decisoes:

    | Decisao | Valor | Responsavel |
    |---------|-------|-------------|
    | Target | bad_review = 1 se review_score in {1,2} | P4 — ML Lead |
    | Ancora temporal | order_approved_at | P1 — Data Lead |
    | Pedidos excluidos | status canceled, unavailable | P1 — Data Lead |
    | Outlier frete | Flaggar (nao remover), threshold = +3 std | P1 — Data Lead |
    | Outlier prazo | >30 dias incluido na EDA; ML decide separadamente | P4 — ML Lead |
    | Distribuicao esperada | ~15-20% positivos (bad_review=1) | Confirmar na Phase 2 |

    **Rodape:**
    ```
    ---
    *Documento criado na Phase 1 (Kickoff). Deve ser lido antes de abrir qualquer notebook da Phase 2.*
    *Referencia de codigo: src/features.py | Metricas: docs/metrics_agreement.md | Features: docs/feature_contract.md*
    ```
  </action>
  <verify>
    <automated>python -c "
content = open('docs/kickoff.md').read()

checks = {
    'bad_review definido': 'bad_review' in content,
    'review_score {1,2} documentado': '{1, 2}' in content or 'in [1, 2]' in content or 'in {1,2}' in content or 'isin([1, 2])' in content or '{1,2}' in content,
    'order_approved_at como ancora': 'order_approved_at' in content,
    'order_purchase_timestamp proibido ou nao recomendado': 'order_purchase_timestamp' in content,
    'canceled excluido': 'canceled' in content,
    'unavailable excluido': 'unavailable' in content,
    'outlier frete documentado': 'frete' in content.lower() and ('std' in content or 'desvio' in content.lower()),
    'outlier prazo documentado': '30 dias' in content or '30-day' in content or '30 day' in content,
    'rationale target presente': 'rationale' in content.lower() or 'motivo' in content.lower() or 'Rationale' in content,
}

all_ok = True
for check, result in checks.items():
    status = 'OK' if result else 'FAIL'
    if not result:
        all_ok = False
    print(f'{status}: {check}')

assert all_ok, 'Alguns checks falharam'
print(f'OK: docs/kickoff.md tem {len(content)} chars')
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <done>
    - docs/kickoff.md existe com todas as cinco secoes
    - Target documentado: bad_review = 1 se review_score in {1,2}, com tabela e codigo Python de derivacao
    - Ancora temporal documentada: order_approved_at (nao order_purchase_timestamp)
    - Filtros de inclusao documentados: excluir canceled, unavailable, pedidos sem review
    - Regras de outlier documentadas: frete (flaggar, nao remover), prazo (>30 dias incluido na EDA)
    - Tabela de resumo de decisoes com responsaveis
  </done>
</task>

</tasks>

<verification>
Verificacao apos conclusao:

```python
content = open("docs/kickoff.md").read()
required = ["bad_review", "order_approved_at", "canceled", "unavailable", "30"]
for term in required:
    assert term in content, f"Termo ausente em kickoff.md: {term}"
print("PASS: kickoff.md completo")
```
</verification>

<success_criteria>
- docs/kickoff.md existe com todas as decisoes de KICK-04 e KICK-05
- Target binario documentado: bad_review = 1 se review_score in {1,2}
- Ancora temporal documentada: order_approved_at (nao order_purchase_timestamp)
- Pedidos excluidos documentados: canceled, unavailable, sem review
- Regras de outlier de frete e prazo escritas antes da Phase 2
- Tabela de resumo de decisoes legivel em 30 segundos
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-kickoff-e-contratos/01-04-SUMMARY.md`
</output>
