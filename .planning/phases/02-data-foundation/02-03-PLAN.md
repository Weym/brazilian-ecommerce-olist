---
phase: 02-data-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - notebooks/FASE2-P1-data-foundation.ipynb
  - data/gold/olist_gold.parquet
  - docs/data_quality.md
autonomous: true
requirements:
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "O arquivo data/gold/olist_gold.parquet existe e pd.read_parquet() carrega sem erro"
    - "O checklist de qualidade documenta: contagem de nulos por coluna, verificacao de duplicatas, CEPs invalidos identificados, datas inconsistentes verificadas"
    - "A tabela gold carrega com as colunas corretas e tipos corretos (datas como datetime64, bad_review como int)"
    - "Qualquer notebook pode fazer pd.read_parquet('data/gold/olist_gold.parquet') e comecar analise sem joins adicionais"
    - "O arquivo docs/data_quality.md existe com o checklist preenchido e valores reais do dataset"
  artifacts:
    - path: "data/gold/olist_gold.parquet"
      provides: "Tabela gold exportada — contrato imutavel para todas as fases downstream"
    - path: "docs/data_quality.md"
      provides: "Checklist de qualidade preenchido com valores reais"
      contains: "nulos | duplicatas | CEPs | datas"
    - path: "notebooks/FASE2-P1-data-foundation.ipynb"
      provides: "Secao de qualidade e export executada com outputs visiveis"
  key_links:
    - from: "gold_tagged DataFrame"
      to: "data/gold/olist_gold.parquet"
      via: "gold_tagged.to_parquet('data/gold/olist_gold.parquet', index=False)"
      pattern: "to_parquet.*olist_gold"
    - from: "data/gold/olist_gold.parquet"
      to: "qualquer notebook de EDA ou ML"
      via: "pd.read_parquet('data/gold/olist_gold.parquet')"
      pattern: "read_parquet.*olist_gold"
---

<objective>
Executar checklist de qualidade de dados na tabela gold_tagged, exportar como olist_gold.parquet e produzir documentacao de qualidade em docs/data_quality.md.

Purpose: A tabela gold e o contrato imutavel de toda a Phase 2 em diante. Antes de "congelar" o arquivo, a qualidade deve ser auditada e documentada — nulos, duplicatas, CEPs invalidos e inconsistencias de datas. Sem este passo, problemas descobertos durante EDA ou ML forcam retrabalho em todos os tracks paralelos.

Output: `data/gold/olist_gold.parquet` (contrato congelado) + `docs/data_quality.md` (checklist preenchido).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-data-foundation/02-02-SUMMARY.md
</context>

<interfaces>
<!-- gold_tagged — DataFrame produzido pelo Plan 02 -->
<!-- Colunas obrigatorias que devem existir antes do export: -->
<!--   pre-entrega: order_id, order_approved_at, order_estimated_delivery_date,         -->
<!--     estimated_days, payment_value, freight_value, freight_ratio, total_payment_value, -->
<!--     product_weight_g, product_volume_cm3, product_photos_qty,                        -->
<!--     seller_customer_distance_km, seller_id, seller_state, customer_id,               -->
<!--     customer_state, product_category_name_english                                    -->
<!--   pos-entrega: order_delivered_customer_date, actual_delay_days                     -->
<!--   target: bad_review                                                                 -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Executar checklist de qualidade de dados e imprimir relatorio no notebook</name>
  <files>notebooks/FASE2-P1-data-foundation.ipynb</files>
  <action>
Adicionar Secao 8 ao notebook com checklist completo de qualidade. Cada verificacao deve printar resultado claro e marcacao de STATUS: OK / AVISO / PROBLEMA.

```python
# Celula 22: === CHECKLIST DE QUALIDADE DE DADOS ===
print("=" * 60)
print("CHECKLIST DE QUALIDADE — TABELA GOLD")
print("=" * 60)

# --- 1. Nulos por coluna ---
print("\n[1] NULOS POR COLUNA")
null_counts = gold_tagged.isnull().sum()
null_pct = (null_counts / len(gold_tagged) * 100).round(2)
null_report = pd.DataFrame({"nulos": null_counts, "pct": null_pct})
null_report = null_report[null_report["nulos"] > 0].sort_values("nulos", ascending=False)
if null_report.empty:
    print("  STATUS: OK — sem nulos")
else:
    print(null_report.to_string())

# Colunas criticas nao podem ter nulos
CRITICAS = ["order_id", "order_approved_at", "review_score", "bad_review",
            "seller_id", "customer_id", "seller_state", "customer_state"]
for col in CRITICAS:
    if col in gold_tagged.columns:
        n = gold_tagged[col].isna().sum()
        status = "OK" if n == 0 else f"PROBLEMA: {n} nulos"
        print(f"  {col}: {status}")

# --- 2. Duplicatas ---
print("\n[2] DUPLICATAS")
dup_order = gold_tagged["order_id"].duplicated().sum()
print(f"  order_id duplicados: {dup_order} — STATUS: {'OK' if dup_order == 0 else 'PROBLEMA'}")

# --- 3. CEPs invalidos ---
print("\n[3] CEPs INVALIDOS (formato esperado: string de 5 digitos)")
# CEP invalido = nao numerico ou diferente de 5 chars
def check_cep_valid(series):
    return series.astype(str).str.match(r"^\d{5}$")

seller_cep_ok = check_cep_valid(gold_tagged["seller_zip_code_prefix"]).sum()
customer_cep_ok = check_cep_valid(gold_tagged["customer_zip_code_prefix"]).sum()
total = len(gold_tagged)
print(f"  seller_zip validos: {seller_cep_ok}/{total} ({seller_cep_ok/total:.1%})")
print(f"  customer_zip validos: {customer_cep_ok}/{total} ({customer_cep_ok/total:.1%})")

seller_sem_geo = gold_tagged["seller_lat"].isna().sum()
customer_sem_geo = gold_tagged["customer_lat"].isna().sum()
print(f"  Sellers sem lat/lon (CEP nao encontrado no geo): {seller_sem_geo} ({seller_sem_geo/total:.1%})")
print(f"  Customers sem lat/lon (CEP nao encontrado no geo): {customer_sem_geo} ({customer_sem_geo/total:.1%})")

# --- 4. Datas inconsistentes ---
print("\n[4] DATAS INCONSISTENTES")
# order_approved_at deve existir (ja filtrado)
approved_nulos = gold_tagged["order_approved_at"].isna().sum()
print(f"  order_approved_at nulos: {approved_nulos} — STATUS: {'OK' if approved_nulos == 0 else 'PROBLEMA'}")

# estimated_delivery deve ser posterior a approved_at
bad_dates = (gold_tagged["order_estimated_delivery_date"] < gold_tagged["order_approved_at"]).sum()
print(f"  estimated_delivery < approved_at: {bad_dates} pedidos — STATUS: {'OK' if bad_dates == 0 else f'AVISO: {bad_dates}'}")

# estimated_days negativos
neg_days = (gold_tagged["estimated_days"] < 0).sum()
print(f"  estimated_days negativos: {neg_days} — STATUS: {'OK' if neg_days == 0 else f'AVISO: {neg_days}'}")

# Data range da ancora temporal
print(f"\n  Range order_approved_at: {gold_tagged['order_approved_at'].min().date()} a {gold_tagged['order_approved_at'].max().date()}")
print(f"  AVISO: dataset Olist termina abruptamente em Set 2018 — se analise de tendencia, excluir Set 2018")

# --- 5. Distribuicao do target ---
print("\n[5] DISTRIBUICAO DO TARGET")
bad_rate = gold_tagged["bad_review"].mean()
print(f"  bad_review=1: {gold_tagged['bad_review'].sum()} ({bad_rate:.1%})")
print(f"  bad_review=0: {(gold_tagged['bad_review']==0).sum()} ({1-bad_rate:.1%})")
print(f"  scale_pos_weight sugerido para XGBoost: {(gold_tagged['bad_review']==0).sum() / gold_tagged['bad_review'].sum():.1f}")

# --- 6. Outliers de frete ---
print("\n[6] OUTLIERS DE FRETE")
frete_std = gold_tagged["freight_value"].std()
frete_mean = gold_tagged["freight_value"].mean()
outliers_frete = (gold_tagged["freight_value"] > frete_mean + 3*frete_std).sum()
print(f"  Pedidos com frete > media+3std: {outliers_frete} ({outliers_frete/total:.1%})")
print(f"  STATUS: incluidos no gold mas flagados — nao remover (decisao Phase 1)")

print("\n" + "=" * 60)
print("CHECKLIST CONCLUIDO")
print("=" * 60)
```
  </action>
  <verify>
    <automated>cd "C:/Users/Wey/Desktop/Alpha/Projetos/python" && jupyter nbconvert --to notebook --execute notebooks/FASE2-P1-data-foundation.ipynb --output notebooks/FASE2-P1-data-foundation.ipynb 2>&1 | grep -E "CHECKLIST|Error|PROBLEMA" | head -10</automated>
  </verify>
  <done>
    - Celula de checklist executa sem erro
    - "CHECKLIST CONCLUIDO" aparece no output
    - Nenhum "PROBLEMA" em colunas criticas (order_id, bad_review, seller_id, customer_id)
    - Porcentagem de bad_review impressa (usada no SUMMARY)
  </done>
</task>

<task type="auto">
  <name>Task 2: Exportar olist_gold.parquet e criar docs/data_quality.md</name>
  <files>notebooks/FASE2-P1-data-foundation.ipynb, data/gold/olist_gold.parquet, docs/data_quality.md</files>
  <action>
**Parte A — Exportar parquet no notebook:**

```python
# Celula 23: selecionar colunas finais e exportar
# Manter todas as colunas (pre-entrega + pos-entrega + target) — tags no COLUMN_TAGS definem uso
# Nao filtrar colunas aqui — o contrato e a tabela completa

DATA_GOLD = Path("data/gold")
DATA_GOLD.mkdir(parents=True, exist_ok=True)

gold_tagged.to_parquet(DATA_GOLD / "olist_gold.parquet", index=False)
print(f"Exportado: data/gold/olist_gold.parquet")
print(f"Shape: {gold_tagged.shape}")

# Celula 24: verificacao de round-trip (carregar e confirmar que bate)
gold_check = pd.read_parquet(DATA_GOLD / "olist_gold.parquet")
assert gold_check.shape == gold_tagged.shape, "Round-trip falhou: shapes divergem"
assert "bad_review" in gold_check.columns, "Coluna target bad_review ausente apos round-trip"
assert "seller_customer_distance_km" in gold_check.columns, "Coluna de distancia ausente"
assert gold_check["order_id"].is_unique, "order_id duplicado apos round-trip"
print(f"Round-trip OK — {gold_check.shape[0]} linhas, {gold_check.shape[1]} colunas")
print(f"Dtypes:\n{gold_check.dtypes.to_string()}")
```

**Parte B — Criar docs/data_quality.md:**

Criar o arquivo `docs/data_quality.md` com conteudo derivado dos resultados do checklist. O executor deve preencher os valores reais do output do notebook.

Estrutura do arquivo:

```markdown
# Checklist de Qualidade — Tabela Gold

**Arquivo:** `data/gold/olist_gold.parquet`
**Gerado em:** {data de execucao}
**Notebook:** `notebooks/FASE2-P1-data-foundation.ipynb`

## Dimensoes

| Metrica | Valor |
|---------|-------|
| Total de pedidos (linhas) | {N} |
| Total de colunas | {N} |
| Pedidos excluidos (canceled/unavailable/sem review) | {N} |

## [1] Nulos por Coluna

| Coluna | Nulos | % | Impacto |
|--------|-------|---|---------|
| seller_customer_distance_km | {N} | {%} | Feature de distancia indisponivel para esses pedidos |
| product_weight_g | {N} | {%} | Feature de peso indisponivel |
| product_volume_cm3 | {N} | {%} | Feature de volume indisponivel |
| ... demais colunas com nulos ... | | | |

Colunas criticas (order_id, bad_review, seller_id, customer_id, seller_state, customer_state): SEM NULOS.

## [2] Duplicatas

- order_id duplicados: 0 (OK)
- Tabela gold tem exatamente 1 linha por pedido

## [3] CEPs e Cobertura Geografica

| Metrica | Valor |
|---------|-------|
| Sellers sem lat/lon (CEP ausente no geo) | {N} ({%}) |
| Customers sem lat/lon (CEP ausente no geo) | {N} ({%}) |
| Pedidos sem distancia calculada | {N} ({%}) |

**Tratamento:** CEPs nao encontrados na tabela de geolocation resultam em seller_customer_distance_km = NaN. Pipeline de ML deve tratar NaN com imputer.

## [4] Datas

| Verificacao | Resultado |
|-------------|-----------|
| order_approved_at nulos | 0 (OK — filtrado antes do export) |
| estimated_delivery < approved_at | {N} pedidos |
| estimated_days negativos | {N} |
| Range temporal | {data_min} a {data_max} |

**AVISO:** Dataset Olist encerra abruptamente em setembro de 2018. Analises de tendencia temporal devem excluir ou anotar esse mes.

## [5] Target

| Classe | Pedidos | Proporcao |
|--------|---------|-----------|
| bad_review = 1 (1-2 estrelas) | {N} | {%} |
| bad_review = 0 (3-5 estrelas) | {N} | {%} |

**scale_pos_weight sugerido para XGBoost:** {ratio}

## [6] Outliers

- Pedidos com frete > media+3*desvio: {N} ({%})
- Decisao (Phase 1): manter no gold, nao remover — flagar para EDA

## Colunas por Tag

### Pre-entrega (permitidas no modelo ML)
order_id, order_approved_at, order_estimated_delivery_date, estimated_days,
payment_value, freight_value, freight_ratio, total_payment_value,
product_weight_g, product_volume_cm3, product_photos_qty,
seller_customer_distance_km, seller_id, seller_state, seller_city,
customer_id, customer_unique_id, customer_state, customer_city,
product_category_name_english, order_status, order_purchase_timestamp,
seller_lat, seller_lng, customer_lat, customer_lng

### Pos-entrega (PROIBIDO no modelo ML — vazamento de dados)
order_delivered_customer_date, order_delivered_carrier_date, actual_delay_days, review_score

### Target
bad_review (1 = insatisfacao 1-2 estrelas, 0 = ok 3-5 estrelas)

---
*Qualidade validada: {data}*
*Proxima verificacao recomendada: antes de iniciar Phase 4 (ML)*
```

O executor deve substituir todos os {placeholders} com os valores reais do output do checklist no notebook.
  </action>
  <verify>
    <automated>cd "C:/Users/Wey/Desktop/Alpha/Projetos/python" && python -c "import pandas as pd; df = pd.read_parquet('data/gold/olist_gold.parquet'); print(f'Shape: {df.shape}'); print(f'bad_review ok: {\"bad_review\" in df.columns}'); print(f'distance ok: {\"seller_customer_distance_km\" in df.columns}'); print(f'order_id unique: {df[\"order_id\"].is_unique}'); print(f'bad_review values: {df[\"bad_review\"].unique().tolist()}')"</automated>
  </verify>
  <done>
    - `data/gold/olist_gold.parquet` existe e carrega com pd.read_parquet sem erro
    - Shape impressa: (N_linhas, N_colunas) — N_linhas esperado entre 80000-100000
    - "bad_review ok: True"
    - "distance ok: True"
    - "order_id unique: True"
    - "bad_review values: [0, 1]" (apenas esses dois valores)
    - `docs/data_quality.md` existe com valores reais preenchidos (sem {placeholders})
  </done>
</task>

</tasks>

<verification>
Verificacao final de toda a Phase 2:

```python
# Script de smoke test — rodar apos todos os 3 plans
import pandas as pd
gold = pd.read_parquet("data/gold/olist_gold.parquet")

# Contrato de colunas
assert "bad_review" in gold.columns
assert "seller_customer_distance_km" in gold.columns
assert "estimated_days" in gold.columns
assert "freight_ratio" in gold.columns
assert "product_volume_cm3" in gold.columns
assert "actual_delay_days" in gold.columns
assert "product_category_name_english" in gold.columns

# Integridade
assert gold["order_id"].is_unique
assert gold["bad_review"].isin([0, 1]).all()
assert gold["bad_review"].isna().sum() == 0

# Distancia em km (nao graus)
assert gold["seller_customer_distance_km"].max() > 100
assert gold["seller_customer_distance_km"].max() < 6000

# Ainda ha dados suficientes
assert len(gold) > 50000, "menos de 50k pedidos — verificar filtros"

print("SMOKE TEST PASSOU — tabela gold validada")
print(gold.shape)
```
</verification>

<success_criteria>
1. `data/gold/olist_gold.parquet` existe — `pd.read_parquet("data/gold/olist_gold.parquet")` retorna DataFrame sem erro
2. DataFrame tem colunas: bad_review, seller_customer_distance_km, estimated_days, freight_ratio, product_volume_cm3, actual_delay_days, product_category_name_english
3. `gold["bad_review"].isin([0,1]).all() == True`
4. `gold["seller_customer_distance_km"].max() > 100` (confirma km, nao graus)
5. `gold["order_id"].is_unique == True`
6. `docs/data_quality.md` existe com valores reais (nulos, duplicatas, CEPs, datas, target)
7. Qualquer notebook pode fazer `pd.read_parquet("data/gold/olist_gold.parquet")` — sem joins adicionais necessarios
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/02-data-foundation/02-03-SUMMARY.md` com:
- Path e shape final do parquet exportado
- Resultado do smoke test (passou/falhou)
- Valores chave do checklist de qualidade (taxa de bad_review, nulos em distancia, range temporal)
- Confirmacao de que docs/data_quality.md foi criado com valores reais
</output>
