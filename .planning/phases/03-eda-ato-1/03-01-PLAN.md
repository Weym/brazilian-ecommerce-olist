---
phase: 03-eda-ato-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/FASE3-P3-eda-metricas.ipynb
  - reports/figures/eda01_atraso_vs_nota_boxplot.png
  - reports/figures/eda01_atraso_vs_nota_scatter.png
  - reports/figures/eda02_frete_vs_nota.png
  - reports/figures/eda04_categorias_ruins.png
autonomous: true
requirements:
  - EDA-01
  - EDA-02
  - EDA-04

must_haves:
  truths:
    - "Existe um boxplot de dias_atraso por review_score (1-5) com linhas de referencia em zero, exportado como PNG 150 DPI"
    - "Mann-Whitney U test esta no notebook com p-value impresso — p << 0.05 confirma que pedidos com bad_review=1 tem maior atraso"
    - "Existe um grafico duplo (frete absoluto + frete % do pedido) por nota, mostrando direcao e magnitude do efeito"
    - "Existe um grafico de barras horizontais com top-15 categorias por contagem de avaliacoes 1-2 estrelas, exportado como PNG"
    - "Todos os PNGs estao em reports/figures/ com tamanho > 0 bytes"
  artifacts:
    - path: "notebooks/FASE3-P3-eda-metricas.ipynb"
      provides: "Notebook EDA — atraso, frete, categorias (Pessoa 3)"
    - path: "reports/figures/eda01_atraso_vs_nota_boxplot.png"
      provides: "Boxplot atraso vs nota para slides"
    - path: "reports/figures/eda01_atraso_vs_nota_scatter.png"
      provides: "Scatter atraso vs nota (amostrado) para slides"
    - path: "reports/figures/eda02_frete_vs_nota.png"
      provides: "Frete absoluto e % vs nota para slides"
    - path: "reports/figures/eda04_categorias_ruins.png"
      provides: "Top-15 categorias problematicas para slides"
  key_links:
    - from: "notebooks/FASE3-P3-eda-metricas.ipynb"
      to: "data/gold/olist_gold.parquet"
      via: "pd.read_parquet"
      pattern: "read_parquet.*olist_gold"
    - from: "notebooks/FASE3-P3-eda-metricas.ipynb"
      to: "reports/figures/*.png"
      via: "fig.savefig(..., dpi=150, bbox_inches='tight')"
      pattern: "savefig|write_image"
---

<objective>
Produzir as evidencias visuais quantitativas do Ato 1: atraso degrada nota, frete degrada nota, categorias concentram insatisfacao.

Purpose: Fornecer os graficos prontos para slides que provam o impacto da logistica na satisfacao, com significancia estatistica validada.
Output: notebooks/FASE3-P3-eda-metricas.ipynb + 4 PNGs em reports/figures/
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-eda-ato-1/03-RESEARCH.md

<interfaces>
<!-- Contrato de entrada: olist_gold.parquet -->
<!-- Colunas obrigatorias para este plano: -->

Colunas esperadas em data/gold/olist_gold.parquet:
- order_id: str
- review_score: int (1-5)
- bad_review: int (0 ou 1) — target binario
- order_estimated_delivery_date: datetime ou str
- order_delivered_customer_date: datetime ou str
- freight_value: float (R$)
- price: float (R$)
- product_category_name_english: str (pode estar ausente — ver Task 1)

Se dias_atraso nao existir na gold, deriva:
  df["dias_atraso"] = (pd.to_datetime(df["order_delivered_customer_date"])
                       - pd.to_datetime(df["order_estimated_delivery_date"])).dt.days
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: EDA-01 — Atraso vs Nota (boxplot + scatter + Mann-Whitney)</name>
  <files>notebooks/FASE3-P3-eda-metricas.ipynb, reports/figures/eda01_atraso_vs_nota_boxplot.png, reports/figures/eda01_atraso_vs_nota_scatter.png</files>
  <action>
Criar notebooks/FASE3-P3-eda-metricas.ipynb com as seguintes secoes em ordem:

**Celula 1 — Setup:**
```python
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt
from scipy import stats
from pathlib import Path

sns.set_theme(style="whitegrid", context="talk")
FIGURES = Path("reports/figures")
FIGURES.mkdir(parents=True, exist_ok=True)
GOLD = Path("data/gold/olist_gold.parquet")
```

**Celula 2 — Load e derivacao defensiva de dias_atraso:**
```python
df = pd.read_parquet(GOLD)

# Derivacao defensiva — idempotente mesmo se coluna ja existir
df["dias_atraso"] = (
    pd.to_datetime(df["order_delivered_customer_date"])
    - pd.to_datetime(df["order_estimated_delivery_date"])
).dt.days

# Sanidade: range esperado -30 a +90 dias
print(f"dias_atraso: min={df['dias_atraso'].min()}, max={df['dias_atraso'].max()}, nulls={df['dias_atraso'].isna().sum()}")
print(f"Shape: {df.shape}, bad_review rate: {df['bad_review'].mean():.2%}")
```

**Celula 3 — Mann-Whitney U:**
```python
bad  = df.loc[df["bad_review"] == 1, "dias_atraso"].dropna()
good = df.loc[df["bad_review"] == 0, "dias_atraso"].dropna()
stat, p = stats.mannwhitneyu(bad, good, alternative="greater")
print(f"Mann-Whitney U={stat:.0f}, p={p:.2e}")
print("Conclusao: pedidos com avaliacao ruim tem MAIOR atraso (p < 0.05)" if p < 0.05 else "ATENCAO: resultado nao significativo")
```

**Celula 4 — Boxplot por review_score (slide export):**
```python
fig, ax = plt.subplots(figsize=(9, 5))
sns.boxplot(
    data=df, x="review_score", y="dias_atraso",
    palette="RdYlGn", order=[1, 2, 3, 4, 5], ax=ax,
    showfliers=False,  # outliers extremos omitidos para legibilidade
)
ax.axhline(0, color="gray", linestyle="--", linewidth=0.8, label="Entrega no prazo")
ax.set_title("Atraso vs. Nota de Avaliacao", fontsize=16, fontweight="bold")
ax.set_xlabel("Nota (estrelas)", fontsize=13)
ax.set_ylabel("Dias de Atraso (+ = atrasado)", fontsize=13)
ax.legend(fontsize=11)
fig.tight_layout()
fig.savefig(FIGURES / "eda01_atraso_vs_nota_boxplot.png", dpi=150, bbox_inches="tight")
plt.close(fig)
print("Exportado: eda01_atraso_vs_nota_boxplot.png")
```

**Celula 5 — Scatter amostrado (alpha baixo para evitar overplotting):**
```python
sample = df.sample(min(5000, len(df)), random_state=42)
fig, ax = plt.subplots(figsize=(9, 5))
ax.scatter(
    sample["dias_atraso"],
    sample["review_score"] + (pd.Series(range(len(sample))).apply(lambda _: __import__('random').uniform(-0.2, 0.2)).values),
    alpha=0.05, s=4, color="steelblue",
)
ax.set_title("Atraso vs. Nota (amostra 5k, jitter)", fontsize=16, fontweight="bold")
ax.set_xlabel("Dias de Atraso", fontsize=13)
ax.set_ylabel("Nota (estrelas)", fontsize=13)
ax.set_yticks([1, 2, 3, 4, 5])
fig.tight_layout()
fig.savefig(FIGURES / "eda01_atraso_vs_nota_scatter.png", dpi=150, bbox_inches="tight")
plt.close(fig)
print("Exportado: eda01_atraso_vs_nota_scatter.png")
```

Alternativa mais simples para o jitter (usar numpy):
```python
import numpy as np
jitter = np.random.uniform(-0.2, 0.2, size=len(sample))
ax.scatter(sample["dias_atraso"], sample["review_score"] + jitter, alpha=0.05, s=4)
```
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; f1=Path('reports/figures/eda01_atraso_vs_nota_boxplot.png'); f2=Path('reports/figures/eda01_atraso_vs_nota_scatter.png'); assert f1.exists() and f1.stat().st_size > 10000, f'Boxplot missing or tiny: {f1}'; assert f2.exists() and f2.stat().st_size > 10000, f'Scatter missing or tiny: {f2}'; print('EDA-01 PNGs OK')"</automated>
  </verify>
  <done>eda01_atraso_vs_nota_boxplot.png e eda01_atraso_vs_nota_scatter.png existem em reports/figures/ com tamanho > 10KB. Mann-Whitney p-value esta impresso no notebook e e menor que 0.05.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EDA-02 — Frete vs Nota (absoluto + percentual)</name>
  <files>notebooks/FASE3-P3-eda-metricas.ipynb, reports/figures/eda02_frete_vs_nota.png</files>
  <action>
Adicionar ao mesmo notebook FASE3-P3-eda-metricas.ipynb as seguintes celulas (continuando apos Task 1):

**Celula 6 — Derivar frete_pct_pedido com protecao contra divisao por zero:**
```python
# Protecao contra price + freight_value == 0
denominador = (df["price"] + df["freight_value"]).replace(0, float("nan"))
df["frete_pct_pedido"] = df["freight_value"] / denominador

# Sanidade
print(f"frete_pct_pedido: min={df['frete_pct_pedido'].min():.3f}, max={df['frete_pct_pedido'].max():.3f}")
print(f"Mediana por nota:\n{df.groupby('review_score')['frete_pct_pedido'].median().round(3)}")
```

**Celula 7 — Grafico duplo (slide export):**
```python
fig, axes = plt.subplots(1, 2, figsize=(14, 6))

# Painel esquerdo: frete absoluto
sns.boxplot(
    data=df, x="review_score", y="freight_value",
    palette="RdYlGn", order=[1, 2, 3, 4, 5],
    showfliers=False, ax=axes[0],
)
axes[0].set_title("Frete Absoluto vs. Nota", fontsize=14, fontweight="bold")
axes[0].set_xlabel("Nota (estrelas)", fontsize=12)
axes[0].set_ylabel("Valor do Frete (R$)", fontsize=12)

# Painel direito: frete como % do pedido
sns.boxplot(
    data=df, x="review_score", y="frete_pct_pedido",
    palette="RdYlGn", order=[1, 2, 3, 4, 5],
    showfliers=False, ax=axes[1],
)
axes[1].set_title("Frete como % do Pedido vs. Nota", fontsize=14, fontweight="bold")
axes[1].set_xlabel("Nota (estrelas)", fontsize=12)
axes[1].set_ylabel("Frete / (Preco + Frete)", fontsize=12)
axes[1].yaxis.set_major_formatter(plt.FuncFormatter(lambda y, _: f"{y:.0%}"))

fig.suptitle("Impacto do Frete na Avaliacao do Cliente", fontsize=16, fontweight="bold", y=1.01)
fig.tight_layout()
fig.savefig(FIGURES / "eda02_frete_vs_nota.png", dpi=150, bbox_inches="tight")
plt.close(fig)
print("Exportado: eda02_frete_vs_nota.png")
```

**Celula 8 — Correlacao (Spearman) para magnitude:**
```python
from scipy.stats import spearmanr
corr_abs, p_abs = spearmanr(df["freight_value"].dropna(), df["review_score"].dropna())
corr_pct, p_pct = spearmanr(df["frete_pct_pedido"].dropna(), df["review_score"].dropna())
print(f"Spearman frete_abs vs nota: r={corr_abs:.3f}, p={p_abs:.2e}")
print(f"Spearman frete_pct vs nota: r={corr_pct:.3f}, p={p_pct:.2e}")
print("Direcao negativa esperada (maior frete = menor nota)")
```
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; f=Path('reports/figures/eda02_frete_vs_nota.png'); assert f.exists() and f.stat().st_size > 10000, f'Frete PNG missing: {f}'; print('EDA-02 PNG OK')"</automated>
  </verify>
  <done>eda02_frete_vs_nota.png existe em reports/figures/ com tamanho > 10KB. Grafico tem dois paineis: frete absoluto (R$) e frete percentual. Correlacao de Spearman esta impressa no notebook com direcao negativa confirmada.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: EDA-04 — Segmentacao por Categoria de Produto</name>
  <files>notebooks/FASE3-P3-eda-metricas.ipynb, reports/figures/eda04_categorias_ruins.png</files>
  <action>
Adicionar ao mesmo notebook FASE3-P3-eda-metricas.ipynb as seguintes celulas:

**Celula 9 — Verificar disponibilidade de product_category_name_english:**
```python
if "product_category_name_english" not in df.columns:
    # Fallback: fazer merge da translation table
    translation = pd.read_csv(
        "data/raw/product_category_name_translation.csv",
        usecols=["product_category_name", "product_category_name_english"],
    )
    df = df.merge(translation, on="product_category_name", how="left")
    print("Merge com translation table realizado")
else:
    print("product_category_name_english ja disponivel na gold table")

print(f"Categorias distintas: {df['product_category_name_english'].nunique()}")
```

**Celula 10 — Agregacao top-15 categorias por contagem de avaliacoes ruins:**
```python
cat_agg = (
    df[df["bad_review"] == 1]
    .groupby("product_category_name_english")["order_id"]
    .count()
    .nlargest(15)
    .reset_index(name="bad_review_count")
)
print(cat_agg)
```

**Celula 11 — Grafico de barras horizontais (slide export via plotly + kaleido):**
```python
import plotly.express as px

fig = px.bar(
    cat_agg.sort_values("bad_review_count"),
    x="bad_review_count",
    y="product_category_name_english",
    orientation="h",
    title="Top 15 Categorias com Mais Avaliacoes 1-2 Estrelas",
    labels={
        "bad_review_count": "Contagem de Avaliacoes Ruins",
        "product_category_name_english": "Categoria",
    },
    color="bad_review_count",
    color_continuous_scale="Reds",
)
fig.update_layout(
    yaxis={"categoryorder": "total ascending"},
    coloraxis_showscale=False,
    font=dict(size=13),
    title_font_size=16,
    margin=dict(l=200, r=40, t=60, b=60),
)
fig.write_image(
    str(FIGURES / "eda04_categorias_ruins.png"),
    format="png", scale=2, width=900, height=550,
)
print("Exportado: eda04_categorias_ruins.png")
```

Nota: se kaleido nao estiver instalado, usar fallback seaborn:
```python
# Fallback seaborn (se fig.write_image falhar)
fig2, ax2 = plt.subplots(figsize=(10, 7))
sns.barplot(data=cat_agg.sort_values("bad_review_count"), x="bad_review_count",
            y="product_category_name_english", palette="Reds_r", ax=ax2)
ax2.set_title("Top 15 Categorias — Avaliacoes 1-2 Estrelas", fontsize=15, fontweight="bold")
ax2.set_xlabel("Contagem", fontsize=12); ax2.set_ylabel("Categoria", fontsize=12)
fig2.tight_layout()
fig2.savefig(FIGURES / "eda04_categorias_ruins.png", dpi=150, bbox_inches="tight")
plt.close(fig2)
```
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; f=Path('reports/figures/eda04_categorias_ruins.png'); assert f.exists() and f.stat().st_size > 10000, f'Categorias PNG missing: {f}'; print('EDA-04 PNG OK')"</automated>
  </verify>
  <done>eda04_categorias_ruins.png existe em reports/figures/ com tamanho > 10KB. Grafico mostra top-15 categorias com mais avaliacoes 1-2 estrelas em barras horizontais ordenadas. Notebook FASE3-P3-eda-metricas.ipynb esta completo com todas as celulas das Tasks 1-3.</done>
</task>

</tasks>

<verification>
Verificacao final do plano — rodar apos todas as tasks:

```bash
python -c "
from pathlib import Path
figs = [
    'reports/figures/eda01_atraso_vs_nota_boxplot.png',
    'reports/figures/eda01_atraso_vs_nota_scatter.png',
    'reports/figures/eda02_frete_vs_nota.png',
    'reports/figures/eda04_categorias_ruins.png',
]
nb = Path('notebooks/FASE3-P3-eda-metricas.ipynb')
assert nb.exists(), f'Notebook ausente: {nb}'
for f in figs:
    p = Path(f)
    assert p.exists() and p.stat().st_size > 10000, f'PNG ausente ou vazio: {p}'
print('PLANO 03-01 OK: notebook + 4 PNGs verificados')
"
```
</verification>

<success_criteria>
1. notebooks/FASE3-P3-eda-metricas.ipynb existe e cobre EDA-01, EDA-02, EDA-04
2. Mann-Whitney U test impresso com p < 0.05 (atraso e significativamente maior em bad_review=1)
3. Spearman correlation impressa para frete vs nota (direcao negativa esperada)
4. 4 PNGs exportados em reports/figures/ a 150 DPI, cada um > 10KB
5. Nenhum path hardcoded absoluto — usar pathlib.Path relativo ao notebook
</success_criteria>

<output>
Apos conclusao, criar .planning/phases/03-eda-ato-1/03-01-SUMMARY.md com:
- Figuras exportadas e tamanhos em bytes
- Mann-Whitney U e p-value obtidos
- Spearman r para frete (absoluto e percentual)
- Categorias mais problematicas (top 3)
- Qualquer desvio do plano (ex: fallback seaborn usado para EDA-04)
</output>
