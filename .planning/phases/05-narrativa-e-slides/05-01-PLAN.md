---
phase: 05-narrativa-e-slides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/FASE2-P2-data-foundation.ipynb
  - notebooks/FASE3-P3-eda.ipynb
  - notebooks/FASE4-P4-ml-pipeline.ipynb
autonomous: true
requirements:
  - PRES-02

must_haves:
  truths:
    - "Qualquer revisor tecnico pode abrir qualquer dos tres notebooks e entender o 'porque' de cada decisao metodologica sem consultar o autor"
    - "Nenhum path absoluto (C:/, /home/, /Users/) existe em nenhum dos tres notebooks"
    - "Outputs intermediarios verbosos estao ausentes dos notebooks; metricas finais importantes estao documentadas em celulas Markdown, nao apenas como output de celula"
    - "A metrica-ancora 'Precision >= 0.40' e 'Recall >= 0.60' esta explicitamente documentada na celula do threshold do notebook ML"
  artifacts:
    - path: "notebooks/FASE2-P2-data-foundation.ipynb"
      provides: "Notebook de data foundation documentado com decisoes metodologicas"
      contains: "celulas Markdown explicando joins, Haversine, tagging de colunas"
    - path: "notebooks/FASE3-P3-eda.ipynb"
      provides: "Notebook de EDA documentado com justificativas de teste estatistico"
      contains: "celulas Markdown explicando Mann-Whitney, choropleth, heatmap rotas"
    - path: "notebooks/FASE4-P4-ml-pipeline.ipynb"
      provides: "Notebook ML documentado com criterios de threshold e metricas finais"
      contains: "celulas Markdown explicando class_weight, PR-AUC, threshold operacional"
  key_links:
    - from: "notebooks/FASE4-P4-ml-pipeline.ipynb"
      to: "docs/report.md (Plan 05-02)"
      via: "metricas extraidas das celulas Markdown de resultado"
      pattern: "PR-AUC.*baseline|XGBoost|Precision.*0.40|Recall.*0.60"
    - from: "notebooks/FASE2-P2-data-foundation.ipynb"
      to: "data/gold/olist_gold.parquet"
      via: "Path.cwd() / 'data' / 'gold' / 'olist_gold.parquet'"
      pattern: "Path.cwd()"
---

<objective>
Documentar os tres notebooks das Phases 2, 3 e 4 com celulas Markdown explicando o "porque" de cada decisao metodologica, corrigir paths hardcoded para pathlib relativo, e garantir que outputs intermediarios verbosos estejam ausentes.

Purpose: A coerencia narrativa do deck e do relatorio depende de metricas rastreadas a notebooks auditaveis. Este plano cria essa base de auditabilidade. Sem notebooks documentados, o relatorio corre risco de usar numeros inconsistentes entre artefatos.
Output: Tres notebooks com headers de secao, celulas Markdown de decisao metodologica, paths relativos com pathlib, e outputs limpos.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-narrativa-e-slides/05-CONTEXT.md
@.planning/phases/05-narrativa-e-slides/05-RESEARCH.md
@.planning/phases/04-ml-ato-2/04-CONTEXT.md

<interfaces>
<!-- Convencoes estabelecidas nas phases anteriores que este plano deve honrar -->

Convencao de nomes de notebooks (Phase 1 CONTEXT.md):
  FASE{N}-P{N}-descricao.ipynb
  Notebooks das phases a documentar:
  - notebooks/FASE2-P2-data-foundation.ipynb
  - notebooks/FASE3-P3-eda.ipynb
  - notebooks/FASE4-P4-ml-pipeline.ipynb

Padrao de caminho relativo portavel (05-RESEARCH.md):
```python
from pathlib import Path
ROOT = Path.cwd()  # assume execucao da raiz do projeto
assert (ROOT / "data" / "gold").exists(), f"Execute da raiz. ROOT: {ROOT}"
gold_path = ROOT / "data" / "gold" / "olist_gold.parquet"
```

Padrao de celula Markdown de decisao metodologica (05-RESEARCH.md):
- Estrutura: "## N. Titulo da Secao\n\n**Por que [abordagem]?** [justificativa]\n\n**[Decisao especifica]:** [racional]"
- Nao descrever o que o codigo faz — explicar POR QUE aquela abordagem foi escolhida

Metricas e threshold do modelo (04-CONTEXT.md):
- Threshold: Precision >= 0.40 (primario), Recall >= 0.60 (secundario)
- Balanceamento: class_weight='balanced' (sem SMOTE)
- Divisao treino/test: 80/20 estratificado por bad_review
- Metrica primaria: PR-AUC e Recall (nao accuracy, nao ROC-AUC)
- Frase-ancora: "40% dos pedidos flagrados sao de fato risco real"

nbstripout: ja configurado via .gitattributes — outputs sao removidos automaticamente no commit.
Portanto outputs importantes de metricas finais DEVEM ser documentados em celulas Markdown de texto, nao apenas como output de celula.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documentar notebook de Data Foundation (FASE2-P2)</name>
  <files>notebooks/FASE2-P2-data-foundation.ipynb</files>
  <action>
    Abrir o notebook `notebooks/FASE2-P2-data-foundation.ipynb` e aplicar as seguintes transformacoes:

    **1. Primeira celula — setup de paths:**
    Garantir que a primeira celula de imports contenha o padrao de caminho relativo portavel:
    ```python
    from pathlib import Path
    ROOT = Path.cwd()
    print(f"ROOT: {ROOT}")
    assert (ROOT / "data" / "gold").exists(), f"Execute da raiz do projeto. ROOT atual: {ROOT}"
    ```
    Substituir qualquer path absoluto (strings com "C:/", "C:\\", "/home/", "/Users/") por `ROOT / "subpasta" / "arquivo"`.

    **2. Celulas Markdown de secao a inserir antes dos blocos de codigo correspondentes:**

    Antes do bloco de load dos CSVs:
    ```markdown
    ## 1. Load dos Dados Originais

    **Fonte:** 9 CSVs da Olist em `data/raw/` — nunca modificados diretamente (dados imutaveis).
    **Estrategia de join:** Cascata a partir de `orders` como tabela central — cada pedido tem exatamente uma linha na tabela gold resultante (1 linha por order_id).
    ```

    Antes do bloco de join chain:
    ```markdown
    ## 2. Join Chain Principal (1 linha por order_id)

    **Por que orders como ancora?** Orders e a entidade central do negocio — cada pedido tem exatamente um cliente, um conjunto de itens, uma avaliacao e coordenadas de origem/destino. Partir de orders garante granularidade correta.
    **Pre-agregacao de geolocation:** CEPs podem ter multiplos registros de lat/lng. Usamos a mediana por CEP antes do join para evitar multiplicacao de linhas.
    ```

    Antes do bloco de feature engineering (Haversine):
    ```markdown
    ## 3. Feature Engineering — Distancia Haversine em km

    **Por que Haversine e nao distancia euclidiana?** A Terra e esferica — coordenadas lat/lng nao podem ser tratadas como plano cartesiano. Haversine calcula a distancia de circulo maximo, que aproxima bem distancias de ate 4000 km (escala do Brasil).
    **Validacao de sanidade:** Valores esperados entre 0 e 4000 km (nao graus decimais). Se resultado estiver entre 0 e 10, a formula esta usando graus em vez de radianos.
    ```

    Antes do bloco de tagging de colunas:
    ```markdown
    ## 4. Tagging de Colunas — Contrato de Features

    **Por que tagear explicitamente?** O modelo ML so pode usar features `[pre-entrega]`. O tagging explícito impede vazamento de dados (data leakage) e torna o contrato auditavel para qualquer revisor.
    **Regra de corte temporal:** Ancora = `order_approved_at`. Qualquer coluna calculada com data posterior a aprovacao e `[pos-entrega]` e proibida na matriz X do modelo.
    ```

    Antes do bloco de target (bad_review):
    ```markdown
    ## 5. Target: bad_review (Variavel Binaria)

    **Definicao:** bad_review = 1 se review_score in [1, 2]; bad_review = 0 se review_score in [3, 4, 5].
    **Por que binario?** Simplifica o problema para classificacao binaria — o objetivo e identificar insatisfacao real, nao prever a nota exata. Estrelas 1-2 representam reclamacoes acionaveis.
    **Proporcao esperada:** ~15-20% de pedidos com bad_review=1 (classe minoritaria — dataset desbalanceado, tratar com class_weight no modelo).
    ```

    Antes do bloco de export para parquet:
    ```markdown
    ## 6. Export para Tabela Gold

    **Formato parquet:** Mais eficiente que CSV para leitura em pandas (colunas tipadas, compressao, metadados). Qualquer notebook downstream faz `pd.read_parquet(ROOT / "data" / "gold" / "olist_gold.parquet")` sem joins adicionais.
    **Imutabilidade:** Uma vez exportado, `olist_gold.parquet` e o contrato imutavel de dados — nao modificar sem documentar a mudanca e re-executar todos os notebooks downstream.
    ```

    **3. Remover ou limpar outputs verbosos:**
    - Manter outputs de: shape final da tabela gold, contagem de nulos, distribuicao do target bad_review
    - Limpar/remover outputs de: warnings de deprecacao, logs intermediarios de join, printouts de debug

    **4. Adicionar celula Markdown de metricas finais (texto, nao output de celula):**
    No final do notebook, adicionar celula Markdown com os resultados chave:
    ```markdown
    ## Resumo — Tabela Gold

    | Metrica | Valor |
    |---------|-------|
    | Total de pedidos | [N linhas] |
    | Colunas totais | [N colunas] |
    | bad_review = 1 | [N pedidos] ([X%]) |
    | Nulos criticos | [documentar resultado do checklist] |
    | Distancia min/max (km) | [0 - ~4000] |

    **Proxima etapa:** `FASE3-P3-eda.ipynb` consome este parquet para analise exploratoria.
    ```
  </action>
  <verify>
    <automated>python -c "
import json, re
nb = json.load(open('notebooks/FASE2-P2-data-foundation.ipynb'))
cells = nb.get('cells', [])

# Check no hardcoded absolute paths
source_all = ' '.join(''.join(c.get('source', [])) for c in cells)
for bad in ['C:/', 'C:\\\\', '/home/', '/Users/']:
    assert bad not in source_all, f'FAIL: hardcoded path found: {bad}'
print('OK: sem paths absolutos hardcoded')

# Check pathlib pattern present
assert 'Path.cwd()' in source_all or 'pathlib' in source_all, 'FAIL: pathlib nao encontrado'
print('OK: pathlib presente')

# Check markdown cells with 'Por que' exist
md_cells = [c for c in cells if c.get('cell_type') == 'markdown']
md_text = ' '.join(''.join(c.get('source', [])) for c in md_cells)
assert 'Por que' in md_text, 'FAIL: celulas Markdown de decisao nao encontradas'
print(f'OK: {len(md_cells)} celulas Markdown encontradas')

# Check Haversine documentation
assert 'Haversine' in md_text or 'haversine' in md_text.lower(), 'FAIL: Haversine nao documentado'
print('OK: Haversine documentado')

print('PASS: FASE2-P2 documentado')
"
    </automated>
  </verify>
  <done>
    - Notebook FASE2-P2 tem celulas Markdown antes de cada bloco de codigo nao-trivial explicando o "porque"
    - Primeira celula usa pathlib.Path com ROOT = Path.cwd()
    - Nenhum path absoluto no notebook
    - Celula Markdown de resumo no final com metricas da tabela gold
    - Decisao de Haversine, tagging de colunas e target bad_review documentadas
  </done>
</task>

<task type="auto">
  <name>Task 2: Documentar notebooks de EDA e ML (FASE3-P3 e FASE4-P4)</name>
  <files>notebooks/FASE3-P3-eda.ipynb, notebooks/FASE4-P4-ml-pipeline.ipynb</files>
  <action>
    Aplicar o mesmo padrao de documentacao (celulas Markdown de decisao metodologica + paths relativos + outputs limpos) aos notebooks FASE3-P3 e FASE4-P4.

    **FASE3-P3-eda.ipynb:**

    Primeira celula — padrao pathlib (mesmo que Task 1).

    Celulas Markdown a inserir:

    Antes da analise de atraso vs nota:
    ```markdown
    ## 1. Analise de Atraso vs. Nota de Avaliacao

    **Por que Mann-Whitney e nao t-test?** As notas (1-5 estrelas) sao ordinais e fortemente enviesadas para 5 estrelas — nao seguem distribuicao normal. Mann-Whitney e o teste nao-parametrico correto para comparar medianas entre grupos sem assumir normalidade.
    **Hipotese nula:** A distribuicao de notas e identica entre pedidos com e sem atraso. Rejeitamos H0 se p-value < 0.05.
    ```

    Antes da analise de frete:
    ```markdown
    ## 2. Analise de Frete vs. Nota de Avaliacao

    **Por que frete em valor absoluto E como % do pedido?** O valor absoluto captura o impacto direto no bolso do cliente. O percentual captura o efeito relativo — R$30 de frete em um pedido de R$50 (60%) e muito mais impactante que R$30 em um pedido de R$300 (10%).
    ```

    Antes do mapa geografico:
    ```markdown
    ## 3. Distribuicao Geografica — Avaliacoes 1-2 Estrelas por UF

    **Por que choropleth por UF e nao por municipio?** Municipios sao muitos (5570) e os dados de Olist nao cobrem todos uniformemente — UF da granularidade geografica com significado operacional (distribuicoes regionais, decisoes de parceiros logisticos por estado).
    **Arquivo exportado:** `data/processed/geo_aggregated.parquet` — consumido pelo Streamlit na Phase 6 sem reprocessamento.
    ```

    Antes da analise de rotas:
    ```markdown
    ## 4. Analise de Rotas Criticas — Origem x Destino

    **Por que heatmap de pares UF-origem x UF-destino?** Atrasos nao sao uniformes geograficamente — existem corredores criticos (ex.: SP → nordeste) onde a concentracao de atrasos e muito maior. O heatmap revela esses padroes de forma visual imediata.
    ```

    Antes da analise de categorias:
    ```markdown
    ## 5. Categorias com Maior Concentracao de Reviews Ruins

    **Objetivo:** Identificar se o problema de satisfacao e generalizado ou concentrado em categorias especificas. Categorias com alta proporcao de bad_review podem ter problemas de embalagem, fragilidade ou expectativas incorretas — informacao acionavel para operacoes.
    ```

    Celula Markdown de resumo no final do FASE3-P3:
    ```markdown
    ## Resumo — Achados do Ato 1

    | Analise | Achado Principal |
    |---------|-----------------|
    | Atraso vs nota | [resultado Mann-Whitney, p-value] |
    | Frete vs nota | [direcao e magnitude do efeito] |
    | Geografico | [UFs com maior concentracao de bad_review] |
    | Rotas criticas | [top corredor de atrasos] |
    | Categorias | [top categorias com bad_review] |

    **Figuras exportadas em `reports/figures/`:** [listar nomes dos PNGs]
    **Proxima etapa:** `FASE4-P4-ml-pipeline.ipynb` usa as mesmas features pre-entrega para prever risco.
    ```

    ---

    **FASE4-P4-ml-pipeline.ipynb:**

    Primeira celula — padrao pathlib (mesmo padrao).

    Celulas Markdown a inserir:

    Antes do load e feature matrix:
    ```markdown
    ## 1. Load e Feature Matrix

    **Allow-list de features:** Importamos `PRE_DELIVERY_FEATURES` de `src/features.py` — lista explícita de 13 colunas disponiveis antes da expedicao. Nenhuma feature pos-entrega entra na matriz X.
    **Ancora temporal:** `order_approved_at` — nenhum calculo usa datas posteriores como input. Violacoes desta regra constituem data leakage e invalidam o modelo.
    ```

    Antes do baseline logistico:
    ```markdown
    ## 2. Baseline Logistico (LogisticRegression)

    **Por que baseline obrigatorio antes de XGBoost?** O baseline estabelece o patamar minimo de performance. Se o XGBoost nao superar o baseline de forma significativa, nao justifica a complexidade adicional. Tambem serve de sanidade check — se o baseline tiver PR-AUC < 0.3, ha problema nos dados.
    **Balanceamento:** `class_weight='balanced'` — ajusta automaticamente os pesos inversamente proporcional as frequencias das classes. Sem SMOTE (sprint de 1 semana, dados sinteticos adicionam risco sem ganho garantido).
    **Metrica primaria:** PR-AUC e Recall para a classe positiva (bad_review=1). Accuracy e enganosa com ~15-20% de classe positiva.
    ```

    Antes do XGBoost:
    ```markdown
    ## 3. XGBoost com Balanceamento de Classes

    **Por que XGBoost apos baseline?** XGBoost captura interacoes nao-lineares entre features (ex.: distancia alta + prazo estimado alto juntos predizem muito mais risco do que cada um separado). A regressao logistica trata cada feature como independente.
    **Hiperparametros:** Defaults razoaveis sem GridSearchCV extenso — o ganho marginal de tuning nao justifica o risco de prazo em sprint de 1 semana.
    **Divisao treino/test:** 80/20 estratificado por `bad_review` para manter proporcao de classes em ambos os conjuntos.
    ```

    Antes do SHAP:
    ```markdown
    ## 4. Explicabilidade — SHAP TreeExplainer

    **Por que SHAP e nao feature_importances_?** SHAP (SHapley Additive exPlanations) atribui a cada feature sua contribuicao marginal para cada predicao individual, considerando interacoes entre features. Feature importance do XGBoost e baseada em ganho de split — menos interpretavel para stakeholders.
    **Escopo:** Test set, top-15 features, salvo em `reports/figures/shap_beeswarm.png` para uso nos slides do Ato 2.
    **Nota de performance:** SHAP TreeExplainer pode levar 10+ min em 100k+ linhas. Se necessario, calcular em amostra de 5000 registros do test set para viabilidade.
    ```

    Antes do threshold:
    ```markdown
    ## 5. Definicao do Threshold de Decisao

    **Criterio primario:** Precision >= 0.40 no threshold escolhido.
    **Criterio secundario:** Recall >= 0.60 no mesmo ponto.

    **Por que Precision como criterio primario?** O custo operacional de intervir em um pedido (contato com vendedor, monitoramento extra) nao e trivial. Um modelo que flaga 90% dos pedidos como "risco" e inutil — equipe operacional nao tem capacidade para agir em todos. Com Precision >= 0.40, garantimos que 40% dos pedidos flagrados sao de fato risco real — a frase-ancora operacional desta analise.

    **Por que Recall >= 0.60 como secundario?** Recall 0.60 significa capturar 60% dos pedidos que iriam gerar avaliacao ruim — suficiente para impacto operacional mensuravel sem exigir Precision muito baixa.
    ```

    Antes da tabela de vendedores:
    ```markdown
    ## 6. Score de Risco por Vendedor

    **Criterio de inclusao:** Minimo de 10 pedidos — vendedores com menos pedidos tem scores estatisticamente instáveis.
    **Top-20 vendedores:** Cabe numa tabela de apresentacao. Colunas: seller_id, score_medio_risco, total_pedidos, pedidos_alto_risco.
    **Uso operacional:** Esta tabela e acionavel diretamente — gestores de operacoes podem priorizar intervencoes nos vendedores do topo da lista.
    ```

    Antes da serializacao:
    ```markdown
    ## 7. Serializacao do Pipeline (.joblib)

    **O que esta serializado:** Pipeline sklearn completo (ColumnTransformer + modelo), nao apenas o estimador. Isso garante que o Streamlit carregue e prediga sem reconstruir transformacoes de pre-processamento.
    **Destino:** `models/final_pipeline.joblib` e `models/baseline_logreg.joblib`.
    **Verificacao:** Carregar o .joblib e chamar `.predict_proba()` em um sample — se retornar array de probabilidades sem erro, a serializacao esta correta.
    ```

    Celula Markdown de metricas finais (OBRIGATORIO — nbstripout apaga outputs de celula):
    ```markdown
    ## Metricas Finais — Resultados do Modelo

    | Metrica | Baseline (LogReg) | XGBoost |
    |---------|------------------|---------|
    | PR-AUC (test set) | [PREENCHER] | [PREENCHER] |
    | Recall @ threshold | [PREENCHER] | [PREENCHER] |
    | Precision @ threshold | - | [PREENCHER] |
    | Pedidos flagrados/semana (estimativa) | - | [PREENCHER] |

    **Threshold escolhido:** [PREENCHER — valor de probabilidade]
    **Frase-ancora operacional:** "40% dos pedidos flagrados pelo modelo sao de fato pedidos de alto risco de avaliacao ruim."

    *Nota: Preencher estes valores apos execucao do notebook. Estes numeros serao copiados para docs/report.md e slides — fonte unica de verdade.*
    ```

    **Para ambos os notebooks FASE3 e FASE4:**
    - Corrigir qualquer path absoluto para pathlib relativo
    - Verificar se ha strings com "C:/", "C:\\", "/home/", "/Users/" e substituir
    - Limpar outputs de celulas com mais de 50 linhas de log de treinamento XGBoost ou warnings de deprecacao
  </action>
  <verify>
    <automated>python -c "
import json, os

errors = []
for nb_path in ['notebooks/FASE3-P3-eda.ipynb', 'notebooks/FASE4-P4-ml-pipeline.ipynb']:
    if not os.path.exists(nb_path):
        errors.append(f'MISSING: {nb_path}')
        continue
    nb = json.load(open(nb_path))
    cells = nb.get('cells', [])
    source_all = ' '.join(''.join(c.get('source', [])) for c in cells)

    # Check no hardcoded paths
    for bad in ['C:/', 'C:\\\\\\\\', '/home/', '/Users/']:
        if bad in source_all:
            errors.append(f'{nb_path}: hardcoded path {bad}')

    # Check pathlib present
    if 'Path.cwd()' not in source_all and 'pathlib' not in source_all:
        errors.append(f'{nb_path}: pathlib nao encontrado')

    # Check Markdown cells with Por que
    md_cells = [c for c in cells if c.get('cell_type') == 'markdown']
    md_text = ' '.join(''.join(c.get('source', [])) for c in md_cells)
    if 'Por que' not in md_text:
        errors.append(f'{nb_path}: sem celulas Markdown de decisao metodologica')

    print(f'Verificado: {nb_path} — {len(md_cells)} celulas Markdown')

# Specific check for FASE4 — threshold documentation
nb4 = json.load(open('notebooks/FASE4-P4-ml-pipeline.ipynb'))
cells4 = nb4.get('cells', [])
md4 = ' '.join(''.join(c.get('source', [])) for c in cells4 if c.get('cell_type') == 'markdown')
if '0.40' not in md4 and 'Precision' not in md4:
    errors.append('FASE4: threshold Precision >= 0.40 nao documentado em celula Markdown')

if errors:
    for e in errors: print(f'FAIL: {e}')
    raise SystemExit(1)
else:
    print('PASS: ambos os notebooks documentados corretamente')
"
    </automated>
  </verify>
  <done>
    - FASE3-P3-eda.ipynb tem celulas Markdown antes de cada analise explicando escolha do teste estatistico (Mann-Whitney), choropleth por UF, rotas criticas e categorias
    - FASE4-P4-ml-pipeline.ipynb tem celulas Markdown antes de cada secao (baseline, XGBoost, SHAP, threshold, vendedores, joblib)
    - Celula de metricas finais preenchida com valores reais de PR-AUC, Recall, Precision e pedidos/semana
    - Threshold de Precision >= 0.40 e Recall >= 0.60 explicitamente documentado em texto Markdown
    - Frase-ancora "40% dos pedidos flagrados" presente no notebook ML
    - Nenhum path absoluto em nenhum dos dois notebooks
  </done>
</task>

</tasks>

<verification>
Verificacao geral apos conclusao do plano:

```bash
# Verificar ausencia de paths absolutos nos 3 notebooks
python -c "
import json, os, glob
for nb_path in glob.glob('notebooks/FASE*.ipynb'):
    nb = json.load(open(nb_path))
    src = ' '.join(''.join(c.get('source',[])) for c in nb.get('cells',[]))
    for bad in ['C:/', 'C:\\\\\\\\', '/home/', '/Users/']:
        if bad in src:
            print(f'FAIL {nb_path}: {bad}')
            raise SystemExit(1)
    md = [c for c in nb['cells'] if c.get('cell_type')=='markdown']
    md_text = ' '.join(''.join(c.get('source',[])) for c in md)
    if 'Por que' not in md_text:
        print(f'FAIL {nb_path}: sem celulas de decisao metodologica')
        raise SystemExit(1)
    print(f'OK: {nb_path} — {len(md)} celulas Markdown')
print('PASS: todos os notebooks documentados')
"
```
</verification>

<success_criteria>
- Os tres notebooks (FASE2, FASE3, FASE4) tem celulas Markdown antes de cada bloco de codigo nao-trivial explicando o "porque" da abordagem, nao apenas o "o que"
- Nenhum path absoluto (C:/, /home/, /Users/) em nenhum notebook — todos usam Path.cwd()
- Notebook FASE4 tem celula Markdown de metricas finais com valores reais de PR-AUC baseline e XGBoost, Recall, Precision e estimativa de pedidos flagrados/semana
- Frase-ancora "40% dos pedidos flagrados" presente e explicada no notebook ML
- Threshold de Precision >= 0.40 (primario) e Recall >= 0.60 (secundario) documentado em texto Markdown (nao apenas como output de celula que sera apagado pelo nbstripout)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/05-narrativa-e-slides/05-01-SUMMARY.md`
</output>
