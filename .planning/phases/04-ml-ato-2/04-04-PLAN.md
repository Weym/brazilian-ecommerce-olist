---
phase: 04-ml-ato-2
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/FASE4-P4-ml-pipeline.ipynb
  - docs/ml_limitations.md
autonomous: true
gap_closure: true
requirements: [ML-05]

must_haves:
  truths:
    - "O notebook FASE4-P4-ml-pipeline.ipynb contem uma celula Markdown na Secao 5 explicando explicitamente que Recall=0.02 e uma limitacao esperada do modelo com PR-AUC=0.2283 — e a narrativa slide-ready esta escrita"
    - "O arquivo docs/ml_limitations.md existe e documenta a limitacao operacional do modelo com framing honesto para a apresentacao"
    - "A narrativa para o Ato 2 dos slides esta disponivel como referencia: modelo de alta precisao, baixo recall — 8 pedidos flagrados/semana, 40% sao risco real"
  artifacts:
    - path: "docs/ml_limitations.md"
      provides: "Documentacao da limitacao operacional para uso na Phase 5 (slides)"
      contains: "Recall=0.02, Precision=0.40, threshold=0.785, 8 flagged/week, framing narrativo"
    - path: "notebooks/FASE4-P4-ml-pipeline.ipynb"
      provides: "Celula Markdown de interpretacao na Secao 5 explicando a limitacao com contexto de negocio"
      contains: "Secao 5 com celula interpretativa Markdown pos-threshold"
  key_links:
    - from: "docs/ml_limitations.md"
      to: ".planning/phases/05-narrativa-e-slides"
      via: "Referencia direta na narrativa do deck — Phase 5 usa este doc como fonte de framing honesto"
      pattern: "ml_limitations"
    - from: "notebooks/FASE4-P4-ml-pipeline.ipynb"
      to: "docs/ml_limitations.md"
      via: "Mesma narrativa documentada nos dois lugares — consistencia entre codigo e documentacao"
      pattern: "Recall.*0.02|threshold.*0.785"
---

<objective>
Fechar o unico gap da verificacao da Phase 4: a observable truth #12 (Recall >= 0.60) nao e satisfeita porque o modelo XGBoost com PR-AUC=0.2283 nao consegue atingir simultaneamente Precision >= 0.40 e Recall >= 0.60. Este e um comportamento de modelo esperado, ja tratado com AVISO no notebook — o gap e de documentacao e narrativa, nao de codigo.

Purpose: Criar documentacao explicita da limitacao operacional para que a Phase 5 (slides) tenha framing honesto disponivel, e adicionar uma celula Markdown interpretativa no notebook que torna a limitacao auditavel e slide-ready.

Output:
- docs/ml_limitations.md — referencia estruturada para Phase 5
- Celula Markdown adicional em FASE4-P4-ml-pipeline.ipynb Secao 5 (apos threshold selection)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ml-ato-2/04-CONTEXT.md
@.planning/phases/04-ml-ato-2/04-VERIFICATION.md
@.planning/phases/04-ml-ato-2/04-03-SUMMARY.md
</context>

<interfaces>
<!-- Estado atual do notebook e artefatos — sem necessidade de explorar o codebase -->

Threshold atual: threshold=0.785, Precision=0.40, Recall=0.02
Estimativa operacional: 8 pedidos flagrados/semana, 40% sao risco real (Precision=0.40)
Criterio primario LOCKED (CONTEXT.md): Precision >= 0.40 — SATISFEITO
Criterio secundario original: Recall >= 0.60 — NAO SATISFEITO (modelo nao suporta ambos simultaneamente)
Fallback pre-especificado no plano: AVISO impresso se Recall < 0.60, execucao continua

Decisao de resolucao (Options 1+3 do VERIFICATION.md):
- Aceitar a limitacao operacional do modelo
- Documentar explicitamente no notebook e em docs/ml_limitations.md
- Fornecer narrativa honesta para Phase 5: "modelo de alta precisao, baixo recall"

Arquivos relevantes:
- notebooks/FASE4-P4-ml-pipeline.ipynb — Secao 5 tem 3 celulas; adicionar 1 celula Markdown apos a celula de threshold selection
- docs/ — pasta existe com metrics_agreement.md, kickoff.md, etc.
- docs/ml_limitations.md — NAO existe ainda; criar neste plano
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar celula Markdown interpretativa na Secao 5 do notebook</name>
  <files>notebooks/FASE4-P4-ml-pipeline.ipynb</files>
  <action>
Abrir notebooks/FASE4-P4-ml-pipeline.ipynb e adicionar UMA celula Markdown do tipo "cell_type": "markdown" na Secao 5, logo apos a celula que imprime "AVISO: Recall abaixo do criterio secundario" (Cell 21 no estado atual).

A celula Markdown deve conter exatamente este conteudo (nao alterar outras celulas):

```markdown
### Interpretacao Operacional do Threshold

**Threshold selecionado:** 0.785 | **Precision:** 0.40 | **Recall:** 0.02

O modelo opera em modo de **alta precisao, baixo recall**:
- De cada 100 pedidos flagrados, **40 sao verdadeiros riscos** (Precision = 0.40)
- O modelo captura apenas **2% dos pedidos que gerarao avaliacao ruim** (Recall = 0.02)
- Estimativa operacional: **8 pedidos flagrados por semana** no volume Olist (~940 pedidos em risco/semana)

**Por que esse tradeoff e aceitavel para o negocio?**
- O objetivo e precisao cirurgica: alertar apenas quando ha confianca alta — evitar "fadiga de alerta" no time de operacoes
- 8 intervencoes/semana e um volume humanamente gerenciavel para uma equipe de atendimento
- XGBoost PR-AUC = 0.2283 vs. baseline LogReg = 0.2207 — o modelo e a melhor opcao disponivel nas features pre-entrega

**Limitacao conhecida:** Com PR-AUC = 0.2283 em 13.9% de positivos, o modelo nao consegue atingir simultaneamente Precision >= 0.40 E Recall >= 0.60. Para atingir Recall >= 0.60 seria necessario baixar o threshold para ~0.14, reduzindo Precision para ~0.18 — flagrando ~540 pedidos/semana com 82% sendo falsos alarmes. A operacao escolheu precisao.

*Narrativa para slides: "Modelo de alerta precoce de alta precisao — 40% dos alertas sao pedidos de fato em risco."*
```

Instrucoes de implementacao no JSON do notebook:
- Localizar a celula com source contendo "AVISO" ou "Recall abaixo" na Secao 5
- Inserir nova celula Markdown APOS essa celula (nao antes, nao substituir)
- Formato JSON da nova celula:
  ```json
  {
    "cell_type": "markdown",
    "id": "interpretacao-threshold-gap-closure",
    "metadata": {},
    "source": ["### Interpretacao Operacional do Threshold\n", ...]
  }
  ```
- Preservar todos os outputs existentes das celulas de codigo — nao modificar celulas de codigo
- Salvar o arquivo .ipynb como JSON valido

NAO usar nbformat/jupyter para adicionar a celula se Python nao estiver disponivel — editar o JSON do .ipynb diretamente com o Read+Write tool, inserindo a celula no array "cells" na posicao correta.
  </action>
  <verify>
    <automated>python -c "import json; nb=json.load(open('notebooks/FASE4-P4-ml-pipeline.ipynb')); md_cells=[c for c in nb['cells'] if c['cell_type']=='markdown' and 'Interpretacao Operacional' in ''.join(c['source'])]; print(f'Celula interpretativa encontrada: {len(md_cells)==1}'); assert len(md_cells)==1, 'Celula nao encontrada'"</automated>
  </verify>
  <done>FASE4-P4-ml-pipeline.ipynb tem uma celula Markdown na Secao 5 com o titulo "Interpretacao Operacional do Threshold", contendo a narrativa de alta precisao/baixo recall e o framing para slides</done>
</task>

<task type="auto">
  <name>Task 2: Criar docs/ml_limitations.md com framing operacional para Phase 5</name>
  <files>docs/ml_limitations.md</files>
  <action>
Criar o arquivo docs/ml_limitations.md com o seguinte conteudo. Este arquivo e uma referencia estruturada para a Phase 5 (slides) usar ao narrar a limitacao do modelo com honestidade.

Conteudo do arquivo:

```markdown
# ML Model Limitations — Olist Pre-Delivery Risk

**Phase 4 documented limitation — referencia para Phase 5 (slides) e Phase 6 (Streamlit)**

---

## Limitacao Principal: Alta Precisao, Baixo Recall

| Metrica | Valor | Criterio | Status |
|---------|-------|----------|--------|
| PR-AUC (XGBoost) | 0.2283 | > baseline (0.2207) | SATISFEITO |
| PR-AUC (Baseline LogReg) | 0.2207 | — | referencia |
| Precision no threshold | 0.40 | >= 0.40 (primario) | SATISFEITO |
| Recall no threshold | 0.02 | >= 0.60 (secundario) | NAO ATINGIDO |
| Threshold selecionado | 0.785 | primeiro ponto Precision >= 0.40 | — |

**Estimativa operacional (volume Olist ~2 anos, 97.456 pedidos):**
- Pedidos flagrados por semana: **8**
- Percentual de risco real entre os flagrados: **40%** (Precision = 0.40)
- Percentual de pedidos em risco capturados: **2%** (Recall = 0.02)

---

## Por que o Modelo Nao Atinge Recall >= 0.60

O modelo XGBoost treinado nas 13 features pre-entrega tem PR-AUC = 0.2283 em um dataset com 13.9% de positivos. Esta performance reflete que **as features disponiveis antes da entrega tem poder preditivo limitado** — a satisfacao do cliente e determinada por muitos fatores pos-entrega que o modelo nao pode observar por definicao (resultado da entrega, estado do produto, atendimento).

Para atingir Recall = 0.60 com as features atuais seria necessario usar threshold ~0.14:
- Precision cairia para ~0.18 (82% de falsos alarmes)
- Pedidos flagrados por semana: ~540 (vs. 8 atual)
- Operacionalmente inviavel para qualquer time de atendimento

**A escolha de Precision >= 0.40 e uma decisao operacional, nao uma falha do modelo.**

---

## Framing para Slides (Ato 2)

### Narrativa recomendada

> "O modelo de alerta precoce opera em modo cirurgico: **40% dos pedidos que ele alerta sao de fato riscos reais**. Com 8 alertas por semana, o time de operacoes tem uma carga de trabalho humanamente gerenciavel para intervir antes que o problema chegue ao cliente."

### O que NAO dizer

- NAO: "O modelo detecta 60% dos pedidos em risco" — isso e falso (Recall = 0.02)
- NAO: "O modelo e impreciso" — Precision = 0.40 e bem superior ao baseline aleatório (~0.14)
- NAO omitir o recall baixo — audiencia tecnica perceberá; honestidade constrói credibilidade

### Slide de limitacoes (recomendado no Apendice Tecnico)

Incluir um slide honesto com o tradeoff Precision/Recall visualizado na curva PR:
- Mostrar onde o threshold 0.785 esta na curva (extremo direito, alta precisao)
- Mostrar o tradeoff: mover o threshold para a esquerda aumenta recall mas explode os falsos alarmes
- Mensagem: "Escolhemos cirurgia, nao rastreio em massa"

---

## Impacto na Phase 6 (Streamlit Demo)

O predictor no Streamlit deve exibir:
- Score de risco (probabilidade bruta do modelo) — sempre util
- Indicacao de "ALTO RISCO" apenas quando score > threshold (0.785)
- Aviso contextual: "Este modelo alerta com 40% de precisao — 8 casos/semana em media"

Evitar exibir Recall na UI — e tecnicamente correto mas confuso para audiencia nao-tecnica.

---

## Referencias Tecnicas

- Notebook: `notebooks/FASE4-P4-ml-pipeline.ipynb` — Secao 5 (Threshold + Operational Estimate)
- Curva PR: `reports/figures/pr_curve.png` — ponto vermelho em Precision=0.40, Recall=0.02
- SHAP beeswarm: `reports/figures/shap_beeswarm.png` — top features explicam o modelo
- Joblib: `models/final_pipeline.joblib` — pipeline completo para Streamlit

---

*Gerado em Phase 4 gap closure — 2026-03-01*
*Referencia para: Phase 5 (docs/slides_outline.md) e Phase 6 (app/pages/2_Preditor.py)*
```
  </action>
  <verify>
    <automated>python -c "import os; content=open('docs/ml_limitations.md').read(); checks=['0.2283','0.40','Recall','threshold','Precision','Phase 5','Phase 6','Framing para Slides']; missing=[c for c in checks if c not in content]; print(f'Checks passados: {len(checks)-len(missing)}/{len(checks)}'); assert not missing, f'Faltando: {missing}'"</automated>
  </verify>
  <done>docs/ml_limitations.md existe com pelo menos 100 linhas, tabela de metricas, framing narrativo para slides, e secoes dedicadas para Phase 5 e Phase 6</done>
</task>

</tasks>

<verification>
Apos completar as 2 tasks:

1. Verificar notebook JSON valido: `python -c "import json; json.load(open('notebooks/FASE4-P4-ml-pipeline.ipynb')); print('JSON valido')"`
2. Verificar celula Markdown adicionada: `python -c "import json; nb=json.load(open('notebooks/FASE4-P4-ml-pipeline.ipynb')); md=[c for c in nb['cells'] if 'Interpretacao Operacional' in ''.join(c.get('source',[]))]; print(f'Celula encontrada: {bool(md)}')" `
3. Verificar docs/ml_limitations.md existe e tem conteudo: `python -c "f=open('docs/ml_limitations.md').read(); print(f'Linhas: {len(f.splitlines())}')"`
4. Confirmar que nenhuma celula de codigo do notebook foi modificada (outputs preservados): `python -c "import json; nb=json.load(open('notebooks/FASE4-P4-ml-pipeline.ipynb')); code_with_output=[c for c in nb['cells'] if c['cell_type']=='code' and c.get('outputs')]; print(f'Celulas codigo com output: {len(code_with_output)}')"`

Nenhuma re-execucao do notebook e necessaria — a celula adicionada e Markdown (nao codigo).
</verification>

<success_criteria>
- docs/ml_limitations.md existe com framing narrativo completo para Phase 5 e Phase 6
- notebooks/FASE4-P4-ml-pipeline.ipynb contem celula Markdown "Interpretacao Operacional do Threshold" na Secao 5
- O gap da VERIFICATION.md (observable truth #12) esta endereçado: a limitacao e documentada explicitamente como comportamento esperado e aceito, com narrativa honesta disponivel
- Nenhum artefato de Phase 4 foi re-computado ou alterado — apenas documentacao adicionada
- Phase 5 pode referenciar docs/ml_limitations.md para framing honesto do modelo no deck
</success_criteria>

<output>
Apos completar as tasks, criar `.planning/phases/04-ml-ato-2/04-04-SUMMARY.md` seguindo o template padrao do projeto.

Atualizar .planning/STATE.md:
- status: gap_closed
- Adicionar em Decisions: "[Phase 04 gap closure]: Recall=0.02 aceito como limitacao operacional — modelo opera em modo alta precisao (40%); docs/ml_limitations.md criado como referencia para Phase 5"
- Current focus: Phase 5 — Narrativa e Slides
</output>
