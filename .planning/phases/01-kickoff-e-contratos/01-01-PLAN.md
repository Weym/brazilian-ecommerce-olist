---
phase: 01-kickoff-e-contratos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/raw/.gitkeep
  - data/gold/.gitkeep
  - data/processed/.gitkeep
  - notebooks/.gitkeep
  - src/.gitkeep
  - models/.gitkeep
  - reports/figures/.gitkeep
  - app/.gitkeep
  - docs/.gitkeep
  - .gitattributes
  - requirements.txt
  - README.md
autonomous: true
requirements:
  - KICK-03

must_haves:
  truths:
    - "Todas as pastas do projeto existem e estao vazias com .gitkeep para o git rastreá-las"
    - "O arquivo .gitattributes registra o filtro nbstripout para *.ipynb"
    - "O requirements.txt lista as dependencias pinadas do projeto"
    - "O README.md tem secoes de visao geral, estrutura de pastas e instrucoes de setup com nbstripout"
  artifacts:
    - path: ".gitattributes"
      provides: "Filtro nbstripout registrado para git"
      contains: "*.ipynb filter=nbstripout"
    - path: "requirements.txt"
      provides: "Dependencias pinadas do projeto"
    - path: "README.md"
      provides: "Documentacao de entrada do projeto"
    - path: "data/raw/.gitkeep"
      provides: "Pasta de dados raw rastreada pelo git"
    - path: "src/.gitkeep"
      provides: "Pasta src rastreada (receberá features.py no Plan 02)"
  key_links:
    - from: "README.md"
      to: ".gitattributes"
      via: "instrucoes de setup (nbstripout --install)"
      pattern: "nbstripout"
    - from: ".gitattributes"
      to: "*.ipynb"
      via: "git filter"
      pattern: "filter=nbstripout"
---

<objective>
Criar o scaffold completo do repositorio: todas as pastas, regras de git para notebooks (nbstripout), dependencias pinadas e README inicial.

Purpose: Nenhum trabalho paralelo pode comecar sem a estrutura de pastas e as convencoes de git estabelecidas. Este plano e o desbloqueador de todos os outros planos da Phase 1.
Output: Esqueleto de repositorio com todas as pastas, .gitattributes, requirements.txt e README.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kickoff-e-contratos/01-CONTEXT.md
@.planning/phases/01-kickoff-e-contratos/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar estrutura de pastas e .gitkeep</name>
  <files>
    data/raw/.gitkeep
    data/gold/.gitkeep
    data/processed/.gitkeep
    notebooks/.gitkeep
    src/.gitkeep
    models/.gitkeep
    reports/figures/.gitkeep
    app/.gitkeep
    docs/.gitkeep
  </files>
  <action>
    Criar todas as pastas do projeto conforme a estrutura definida em CONTEXT.md (decisao bloqueada):

    ```
    data/raw/          <- CSVs originais da Olist (nunca modificados)
    data/gold/         <- olist_gold.parquet (contrato imutavel)
    data/processed/    <- agregacoes intermediarias
    notebooks/         <- notebooks por fase/pessoa
    src/               <- modulos Python reutilizaveis
    models/            <- artefatos serializados (.joblib)
    reports/figures/   <- imagens exportadas para slides
    app/               <- codigo Streamlit
    docs/              <- documentacao
    ```

    Em cada pasta criar um arquivo `.gitkeep` vazio para que o git rastreie a pasta (git nao rastreia diretorios vazios). O conteudo do `.gitkeep` deve ser vazio.

    Importante: nao criar `src/__init__.py` nem `src/features.py` aqui — isso e responsabilidade do Plan 02.
  </action>
  <verify>
    <automated>python -c "import os; dirs=['data/raw','data/gold','data/processed','notebooks','src','models','reports/figures','app','docs']; [print(f'OK: {d}') if os.path.isdir(d) else print(f'MISSING: {d}') for d in dirs]"</automated>
  </verify>
  <done>Todas as 9 pastas existem, cada uma com um .gitkeep vazio</done>
</task>

<task type="auto">
  <name>Task 2: Criar .gitattributes, requirements.txt e README.md</name>
  <files>
    .gitattributes
    requirements.txt
    README.md
  </files>
  <action>
    **`.gitattributes`** — Registrar filtro nbstripout em modo git filter (nao pre-commit hook — decisao de pesquisa: o modo hook modifica a copia de trabalho, o modo filter nao):

    ```
    # .gitattributes
    # nbstripout git filter — strips Jupyter output before git sees the file.
    # Working copy is NOT modified (use git filter mode, not pre-commit hook mode).
    # Each contributor must run once after cloning:
    #   pip install nbstripout
    #   nbstripout --install --attributes .gitattributes
    # Verify with: nbstripout --status
    *.ipynb filter=nbstripout
    *.ipynb diff=ipynb
    ```

    **`requirements.txt`** — Listar dependencias do projeto pinadas. Usar `requirements.txt` (nao pyproject.toml — decisao de discricao do Claude: mais simples para sprint de 1 semana):

    ```
    # Core data science
    pandas>=2.0,<3.0
    numpy>=1.26,<2.0
    scikit-learn>=1.4,<2.0
    xgboost>=2.0,<3.0
    shap>=0.44,<1.0

    # Data storage
    pyarrow>=14.0,<16.0
    fastparquet>=2023.0

    # Visualization
    matplotlib>=3.8,<4.0
    seaborn>=0.13,<1.0
    plotly>=5.18,<6.0

    # Geospatial
    geopy>=2.4,<3.0

    # App
    streamlit>=1.32,<2.0

    # Notebook hygiene
    nbstripout>=0.7,<1.0
    jupyter>=1.0
    ipykernel>=6.0

    # Utilities
    joblib>=1.3,<2.0
    python-dotenv>=1.0,<2.0
    ```

    **`README.md`** — Documento de entrada do projeto. Incluir:
    - Titulo e descricao do projeto (Olist — Sistema de Alerta Precoce de Risco Pre-Entrega)
    - Estrutura de pastas (copiar da CONTEXT.md)
    - Secao "Setup" com instrucoes claras:
      1. Clonar repositorio
      2. `pip install -r requirements.txt`
      3. `nbstripout --install --attributes .gitattributes` (OBRIGATORIO para todos os contribuidores)
      4. Verificar: `nbstripout --status`
    - Convencao de nomes de notebooks: `FASE{N}-P{N}-descricao.ipynb`
    - Link para `docs/feature_contract.md` e `docs/metrics_agreement.md`
    - Secao "Reproducao" explicando como rodar as fases em ordem
    - Secao "Dados" explicando que CSVs ficam em `data/raw/` (nunca modificados)
  </action>
  <verify>
    <automated>python -c "
import os
files = ['.gitattributes', 'requirements.txt', 'README.md']
for f in files:
    if os.path.exists(f):
        content = open(f).read()
        print(f'OK: {f} ({len(content)} chars)')
    else:
        print(f'MISSING: {f}')
# Check .gitattributes has nbstripout filter
content = open('.gitattributes').read()
assert 'filter=nbstripout' in content, 'FAIL: .gitattributes missing nbstripout filter'
print('OK: .gitattributes has nbstripout filter')
# Check requirements.txt has key packages
req = open('requirements.txt').read()
for pkg in ['pandas', 'scikit-learn', 'xgboost', 'streamlit', 'nbstripout']:
    assert pkg in req, f'FAIL: requirements.txt missing {pkg}'
    print(f'OK: requirements.txt has {pkg}')
"
    </automated>
  </verify>
  <done>
    - .gitattributes existe com `*.ipynb filter=nbstripout` e `*.ipynb diff=ipynb`
    - requirements.txt lista pandas, scikit-learn, xgboost, shap, streamlit, nbstripout e demais dependencias
    - README.md tem titulo, estrutura de pastas, instrucoes de setup com nbstripout e convencao de notebooks
  </done>
</task>

</tasks>

<verification>
Verificacao geral apos conclusao do plano:

```python
import os

# Todas as pastas existem
dirs = ['data/raw','data/gold','data/processed','notebooks','src','models','reports/figures','app','docs']
for d in dirs:
    assert os.path.isdir(d), f"Pasta ausente: {d}"

# Arquivos criticos existem
files = ['.gitattributes', 'requirements.txt', 'README.md']
for f in files:
    assert os.path.exists(f), f"Arquivo ausente: {f}"

# .gitattributes tem filtro correto
assert 'filter=nbstripout' in open('.gitattributes').read()
assert 'diff=ipynb' in open('.gitattributes').read()

print("PASS: scaffold completo")
```
</verification>

<success_criteria>
- 9 pastas do projeto existem, cada uma com .gitkeep
- .gitattributes registra o filtro nbstripout para *.ipynb (modo git filter, nao hook)
- requirements.txt lista dependencias do projeto incluindo nbstripout, pandas, scikit-learn, xgboost, shap, streamlit
- README.md tem instrucoes de setup com passo obrigatorio de `nbstripout --install --attributes .gitattributes`
- Nenhum arquivo de codigo Python criado neste plano (src/features.py vem no Plan 02)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-kickoff-e-contratos/01-01-SUMMARY.md`
</output>
