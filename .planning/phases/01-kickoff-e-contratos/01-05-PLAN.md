---
phase: 01-kickoff-e-contratos
plan: 05
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - notebooks/FASE2-P1-data-foundation.ipynb
  - notebooks/FASE3-P2-geo-analysis.ipynb
  - notebooks/FASE3-P3-eda.ipynb
  - notebooks/FASE4-P4-ml-pipeline.ipynb
  - docs/ownership.md
autonomous: true
requirements:
  - KICK-03

must_haves:
  truths:
    - "Cada notebook placeholder existe com a celula de importacao padrao e caminhos via pathlib"
    - "Os nomes dos notebooks seguem a convencao FASE{N}-P{N}-descricao.ipynb"
    - "docs/ownership.md mapeia cada area para seu responsavel e seu(s) notebook(s)"
    - "Nenhum notebook tem outputs commitados (os placeholders tem apenas codigo, sem outputs)"
    - "A celula padrao de setup inclui sys.path para que from src.features import ... funcione"
  artifacts:
    - path: "notebooks/FASE2-P1-data-foundation.ipynb"
      provides: "Placeholder do notebook de data foundation (P1)"
    - path: "notebooks/FASE3-P2-geo-analysis.ipynb"
      provides: "Placeholder do notebook de analise geografica (P2)"
    - path: "notebooks/FASE3-P3-eda.ipynb"
      provides: "Placeholder do notebook de EDA (P3)"
    - path: "notebooks/FASE4-P4-ml-pipeline.ipynb"
      provides: "Placeholder do notebook de ML (P4)"
    - path: "docs/ownership.md"
      provides: "Mapa de ownership: pessoa -> area -> notebooks"
  key_links:
    - from: "notebooks/FASE4-P4-ml-pipeline.ipynb"
      to: "src/features.py"
      via: "from src.features import PRE_DELIVERY_FEATURES — celula no placeholder"
      pattern: "PRE_DELIVERY_FEATURES"
    - from: "docs/ownership.md"
      to: "notebooks/"
      via: "tabela de ownership por pessoa"
      pattern: "FASE"
---

<objective>
Criar os notebooks placeholders para cada area do projeto e o documento de ownership. Cada placeholder estabelece o arquivo no git (previne conflitos futuros) e inclui a celula de configuracao padrao com imports e caminhos via pathlib.

Purpose: Notebooks criados agora ficam no git como arquivos separados por responsavel. Isso previne o pitfall de dois contribuidores editando o mesmo arquivo sob pressao de sprint. O placeholder tambem serve como template com a celula de importacao correta.
Output: 4 notebooks placeholder + docs/ownership.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kickoff-e-contratos/01-CONTEXT.md
@.planning/phases/01-kickoff-e-contratos/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar notebooks placeholder com celula padrao</name>
  <files>
    notebooks/FASE2-P1-data-foundation.ipynb
    notebooks/FASE3-P2-geo-analysis.ipynb
    notebooks/FASE3-P3-eda.ipynb
    notebooks/FASE4-P4-ml-pipeline.ipynb
  </files>
  <action>
    Criar 4 notebooks Jupyter placeholder no formato JSON valido. Cada notebook deve ter:
    - Metadata de kernel Python 3
    - Exatamente duas celulas:
      1. Celula markdown com titulo, responsavel, fase e descricao
      2. Celula de codigo com o template padrao de importacoes (do RESEARCH.md)

    A celula de codigo padrao (identica para todos os notebooks, com ajuste do titulo):

    ```python
    import sys
    from pathlib import Path

    # Add project root to path so `from src.features import ...` works
    # Adjust depth if notebook is in a subdirectory
    PROJECT_ROOT = Path.cwd().parent  # notebooks/ is one level below root
    if str(PROJECT_ROOT) not in sys.path:
        sys.path.insert(0, str(PROJECT_ROOT))

    import pandas as pd
    import numpy as np
    import matplotlib.pyplot as plt
    import seaborn as sns

    # Project-specific imports (from src/features.py — created in Phase 1)
    from src.features import PRE_DELIVERY_FEATURES, FORBIDDEN_FEATURES, TARGET_COLUMN

    # Display settings
    pd.set_option("display.max_columns", 50)
    pd.set_option("display.float_format", "{:.4f}".format)
    %matplotlib inline

    # Data paths (pathlib — no hardcoded strings)
    DATA_RAW = PROJECT_ROOT / "data" / "raw"
    DATA_GOLD = PROJECT_ROOT / "data" / "gold"
    DATA_PROCESSED = PROJECT_ROOT / "data" / "processed"
    MODELS_DIR = PROJECT_ROOT / "models"
    FIGURES_DIR = PROJECT_ROOT / "reports" / "figures"

    print(f"Project root: {PROJECT_ROOT}")
    print(f"PRE_DELIVERY_FEATURES ({len(PRE_DELIVERY_FEATURES)} columns): {PRE_DELIVERY_FEATURES}")
    print(f"TARGET_COLUMN: {TARGET_COLUMN}")
    ```

    Criar cada notebook como JSON valido com estrutura minima de .ipynb:

    ```json
    {
     "cells": [
      {
       "cell_type": "markdown",
       "metadata": {},
       "source": ["# FASE{N} — {DESCRICAO}\n", "\n", "**Responsavel:** P{N} — {AREA}\n", "**Fase:** {N}\n", "**Objetivo:** {OBJETIVO_DA_FASE}\n"]
      },
      {
       "cell_type": "code",
       "execution_count": null,
       "metadata": {},
       "outputs": [],
       "source": ["{CELULA_PADRAO_ACIMA}"]
      }
     ],
     "metadata": {
      "kernelspec": {
       "display_name": "Python 3",
       "language": "python",
       "name": "python3"
      },
      "language_info": {
       "name": "python",
       "version": "3.11.0"
      }
     },
     "nbformat": 4,
     "nbformat_minor": 5
    }
    ```

    Parametros especificos por notebook:

    **FASE2-P1-data-foundation.ipynb:**
    - Titulo: "FASE2 — Data Foundation"
    - Responsavel: "P1 — Data Lead"
    - Objetivo: "Construir a tabela gold (olist_gold.parquet) com joins, colunas tagueadas e qualidade validada"

    **FASE3-P2-geo-analysis.ipynb:**
    - Titulo: "FASE3 — Analise Geografica e Logistica"
    - Responsavel: "P2 — Geo/Logistica"
    - Objetivo: "Mapa de avaliacoes ruins por UF, analise de rotas e corredores de atraso"

    **FASE3-P3-eda.ipynb:**
    - Titulo: "FASE3 — EDA: Impacto da Logistica na Satisfacao"
    - Responsavel: "P3 — EDA & Metricas"
    - Objetivo: "Visualizacoes de atraso vs nota, frete vs nota, analise por categoria de produto"

    **FASE4-P4-ml-pipeline.ipynb:**
    - Titulo: "FASE4 — ML Pipeline: Modelo de Risco Pre-Entrega"
    - Responsavel: "P4 — ML Lead"
    - Objetivo: "Baseline logistico + XGBoost + SHAP + curva PR + score por vendedor"
    - Adicionar na celula markdown: "**Referencia obrigatoria:** Ler docs/metrics_agreement.md e docs/feature_contract.md antes de comecar"

    IMPORTANTE: Os notebooks devem ter `"outputs": []` (lista vazia) — nenhum output commitado. Isso e o estado correto antes do nbstripout ser instalado por cada contribuidor.
  </action>
  <verify>
    <automated>python -c "
import json, os

notebooks = [
    'notebooks/FASE2-P1-data-foundation.ipynb',
    'notebooks/FASE3-P2-geo-analysis.ipynb',
    'notebooks/FASE3-P3-eda.ipynb',
    'notebooks/FASE4-P4-ml-pipeline.ipynb',
]

for nb_path in notebooks:
    assert os.path.exists(nb_path), f'MISSING: {nb_path}'
    # Validar JSON
    nb = json.load(open(nb_path))
    assert nb.get('nbformat') == 4, f'FAIL: {nb_path} nao e nbformat 4'
    assert len(nb['cells']) >= 2, f'FAIL: {nb_path} tem menos de 2 celulas'
    # Sem outputs
    for cell in nb['cells']:
        if cell['cell_type'] == 'code':
            assert cell.get('outputs', []) == [], f'FAIL: {nb_path} tem outputs commitados'
    # Tem importacao de features
    code_cells = [c for c in nb['cells'] if c['cell_type'] == 'code']
    code_content = ''.join(''.join(c['source']) for c in code_cells)
    assert 'PRE_DELIVERY_FEATURES' in code_content, f'FAIL: {nb_path} nao importa PRE_DELIVERY_FEATURES'
    assert 'pathlib' in code_content or 'Path' in code_content, f'FAIL: {nb_path} nao usa pathlib'
    print(f'OK: {nb_path}')

print('ALL NOTEBOOK CHECKS PASSED')
"
    </automated>
  </verify>
  <done>
    - 4 notebooks existem como JSON valido em notebooks/
    - Nomes seguem convencao FASE{N}-P{N}-descricao.ipynb
    - Cada notebook tem celula markdown com titulo/responsavel/objetivo e celula de codigo com template padrao
    - Todos os notebooks tem outputs vazios (sem outputs commitados)
    - A celula de codigo importa PRE_DELIVERY_FEATURES de src.features e usa pathlib para caminhos
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar docs/ownership.md</name>
  <files>docs/ownership.md</files>
  <action>
    Criar o documento de ownership de notebooks. Deve ser uma referencia rapida de quem e responsavel por que durante o sprint.

    Estrutura obrigatoria:

    ```markdown
    # Ownership de Notebooks — Sprint Olist

    ## Convencao de Nomes

    `FASE{N}-P{N}-descricao.ipynb`

    - `FASE{N}`: Numero da fase (2, 3, 4...)
    - `P{N}`: Numero da pessoa (P1, P2, P3...)
    - `descricao`: nome descritivo em kebab-case

    Exemplos validos:
    - FASE2-P1-data-foundation.ipynb
    - FASE3-P2-geo-analysis.ipynb
    - FASE3-P3-eda.ipynb
    - FASE4-P4-ml-pipeline.ipynb

    ## Mapa de Ownership

    | Pessoa | Area | Notebook(s) | Fase |
    |--------|------|-------------|------|
    | P1 — Data Lead | Data Foundation | FASE2-P1-data-foundation.ipynb | 2 |
    | P2 — Geo/Logistica | Analise Geografica | FASE3-P2-geo-analysis.ipynb | 3 |
    | P3 — EDA & Metricas | EDA | FASE3-P3-eda.ipynb | 3 |
    | P4 — ML Lead | ML Pipeline | FASE4-P4-ml-pipeline.ipynb | 4 |
    | P5 — NLP/Reviews | Reviews NLP | FASE3-P5-nlp-reviews.ipynb (opcional) | 3 |
    | P6 — Storytelling | Apresentacao | Coordena deck em docs/ e app/ | 5-6 |

    ## Regras de Git para Notebooks

    1. **Nunca commitar outputs** — rodar `nbstripout --install --attributes .gitattributes` uma vez apos clonar
    2. **Verificar antes do commit**: `nbstripout --status` deve mostrar filtro ativo
    3. **Sem paths hardcoded** — usar sempre `pathlib.Path` relativo ao `PROJECT_ROOT`
    4. **Cada pessoa so edita seus proprios notebooks** — conflitos em .ipynb sao muito dificeis de resolver
    5. **Nao renomear notebooks apos criados** — o nome e o owner

    ## Como Verificar que nbstripout Esta Funcionando

    ```bash
    # Instalar (uma vez por maquina, apos clonar)
    pip install nbstripout
    nbstripout --install --attributes .gitattributes

    # Verificar
    nbstripout --status
    # Deve mostrar: nbstripout is installed in repository ...
    ```

    Se `nbstripout --status` mostrar "not installed", rodar o install novamente.

    ## Sinal de Alerta

    Se `git diff` em um arquivo .ipynb mostrar linhas como `"output_type": "display_data"`,
    os outputs NAO foram removidos. Verificar nbstripout e rodar `nbstripout <arquivo.ipynb>` manualmente antes do commit.
    ```

    Rodape:
    ```
    ---
    *Criado na Phase 1 (Kickoff). Referencia: README.md (setup) | docs/feature_contract.md (features)*
    ```
  </action>
  <verify>
    <automated>python -c "
content = open('docs/ownership.md').read()

checks = {
    'convencao de nomes': 'FASE' in content and 'P{N}' in content or 'P1' in content,
    'tabela de ownership': 'Data Lead' in content or 'P1' in content,
    'P2 geo': 'P2' in content and ('Geo' in content or 'geo' in content),
    'P3 EDA': 'P3' in content and 'EDA' in content,
    'P4 ML': 'P4' in content and 'ML' in content,
    'regras de git para notebooks': 'commit' in content.lower() or 'git' in content.lower(),
    'nbstripout mencionado': 'nbstripout' in content,
    'sem paths hardcoded': 'pathlib' in content or 'hardcoded' in content.lower() or 'hardcode' in content.lower(),
}

all_ok = True
for check, result in checks.items():
    status = 'OK' if result else 'FAIL'
    if not result:
        all_ok = False
    print(f'{status}: {check}')

assert all_ok, 'Alguns checks falharam'
print(f'OK: docs/ownership.md tem {len(content)} chars')
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <done>
    - docs/ownership.md existe com tabela de ownership mapeando P1-P6 para suas areas e notebooks
    - Convencao de nomes FASE{N}-P{N}-descricao.ipynb documentada com exemplos
    - Regras de git para notebooks documentadas (nbstripout, sem outputs, sem paths hardcoded)
    - Instrucoes de verificacao de nbstripout incluidas
  </done>
</task>

</tasks>

<verification>
Verificacao integrada apos conclusao:

```python
import json, os

# Notebooks existem e sao validos
for nb in ['FASE2-P1-data-foundation', 'FASE3-P2-geo-analysis', 'FASE3-P3-eda', 'FASE4-P4-ml-pipeline']:
    path = f'notebooks/{nb}.ipynb'
    assert os.path.exists(path)
    data = json.load(open(path))
    assert data['nbformat'] == 4
    for cell in data['cells']:
        if cell['cell_type'] == 'code':
            assert cell['outputs'] == [], f"Outputs em {path}"

# Ownership existe
assert os.path.exists('docs/ownership.md')

print("PASS: notebooks e ownership completos")
```
</verification>

<success_criteria>
- 4 notebooks placeholder existem com nomes seguindo FASE{N}-P{N}-descricao.ipynb
- Cada notebook e JSON valido, tem celula de setup com pathlib e importacao de PRE_DELIVERY_FEATURES
- Nenhum notebook tem outputs commitados
- docs/ownership.md mapeia P1-P6 para areas e notebooks com regras de git
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-kickoff-e-contratos/01-05-SUMMARY.md`
</output>
