---
phase: 03-eda-ato-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/FASE3-P2-eda-geo.ipynb
  - docs/brazil_states.geojson
  - data/processed/geo_aggregated.parquet
  - reports/figures/eda03_choropleth_bad_reviews_uf.png
  - reports/figures/eda03_choropleth_static.html
  - reports/figures/eda05_rotas_heatmap.png
autonomous: true
requirements:
  - EDA-03
  - EDA-05

must_haves:
  truths:
    - "data/processed/geo_aggregated.parquet existe com colunas: customer_state, total_orders, bad_reviews, avg_dias_atraso, avg_freight_value, bad_review_rate"
    - "Existe um mapa coropleto por UF mostrando bad_review_rate (nao contagem bruta) — SP nao domina por volume"
    - "Existe um heatmap de atraso medio por corredor origem (seller_state) x destino (customer_state), filtrado para top-15 origens"
    - "eda03_choropleth_bad_reviews_uf.png e eda05_rotas_heatmap.png existem em reports/figures/ com tamanho > 10KB"
  artifacts:
    - path: "notebooks/FASE3-P2-eda-geo.ipynb"
      provides: "Notebook EDA geo — mapa UF + heatmap rotas (Pessoa 2)"
    - path: "data/processed/geo_aggregated.parquet"
      provides: "Agregacao geografica pre-computada para Streamlit (Phase 6)"
      exports: ["customer_state", "total_orders", "bad_reviews", "avg_dias_atraso", "avg_freight_value", "bad_review_rate"]
    - path: "reports/figures/eda03_choropleth_bad_reviews_uf.png"
      provides: "Mapa estatico por UF para slides"
    - path: "reports/figures/eda05_rotas_heatmap.png"
      provides: "Heatmap corredores origem x destino para slides"
  key_links:
    - from: "notebooks/FASE3-P2-eda-geo.ipynb"
      to: "data/gold/olist_gold.parquet"
      via: "pd.read_parquet"
      pattern: "read_parquet.*olist_gold"
    - from: "geo_aggregated.parquet"
      to: "data/processed/"
      via: "geo_agg.to_parquet"
      pattern: "to_parquet.*geo_aggregated"
    - from: "folium.Choropleth"
      to: "docs/brazil_states.geojson"
      via: "gpd.read_file + key_on sigla"
      pattern: "read_file.*geojson"
---

<objective>
Produzir a evidencia geografica do Ato 1: onde no Brasil a logistica falha mais, e quais corredores de rota concentram os atrasos.

Purpose: Fornecer mapa coropleto por UF (normalizado por taxa, nao volume) e heatmap de rotas — os dois graficos geograficos que contextualizam o problema para lideranca.
Output: notebooks/FASE3-P2-eda-geo.ipynb + data/processed/geo_aggregated.parquet + 2 PNGs em reports/figures/
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-eda-ato-1/03-RESEARCH.md

<interfaces>
<!-- Contrato de entrada: olist_gold.parquet -->
Colunas obrigatorias para este plano:
- order_id: str
- bad_review: int (0 ou 1)
- customer_state: str (2 letras UF: SP, RJ, MG, ...)
- seller_state: str (2 letras UF — origem do vendedor)
- order_estimated_delivery_date: datetime ou str
- order_delivered_customer_date: datetime ou str
- freight_value: float

Se dias_atraso nao existir:
  df["dias_atraso"] = (pd.to_datetime(df["order_delivered_customer_date"])
                       - pd.to_datetime(df["order_estimated_delivery_date"])).dt.days

<!-- Schema de saida: geo_aggregated.parquet -->
Colunas obrigatorias:
- customer_state: str
- total_orders: int
- bad_reviews: int
- avg_dias_atraso: float
- avg_freight_value: float
- bad_review_rate: float (0.0 a 1.0)

<!-- GeoJSON source -->
URL primaria: https://raw.githubusercontent.com/codeforamerica/click_that_hood/master/public/data/brazil-states.geojson
Salvar em: docs/brazil_states.geojson
Campo esperado: sigla (verificar com br_states.columns apos load)
Alternativas: abbrev_state, UF, CD_UF
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: EDA-03 — Agregacao geo + GeoJSON + Choropleth por UF</name>
  <files>notebooks/FASE3-P2-eda-geo.ipynb, data/processed/geo_aggregated.parquet, docs/brazil_states.geojson, reports/figures/eda03_choropleth_bad_reviews_uf.png, reports/figures/eda03_choropleth_static.html</files>
  <action>
Criar notebooks/FASE3-P2-eda-geo.ipynb com as seguintes secoes em ordem:

**Celula 1 — Setup:**
```python
import pandas as pd
import geopandas as gpd
import folium
import matplotlib.pyplot as plt
import requests
from pathlib import Path

FIGURES = Path("reports/figures")
FIGURES.mkdir(parents=True, exist_ok=True)
PROCESSED = Path("data/processed")
PROCESSED.mkdir(parents=True, exist_ok=True)
DOCS = Path("docs")
DOCS.mkdir(parents=True, exist_ok=True)
GOLD = Path("data/gold/olist_gold.parquet")
```

**Celula 2 — Load gold e derivacao defensiva dias_atraso:**
```python
df = pd.read_parquet(GOLD)

df["dias_atraso"] = (
    pd.to_datetime(df["order_delivered_customer_date"])
    - pd.to_datetime(df["order_estimated_delivery_date"])
).dt.days

print(f"Shape: {df.shape}")
print(f"customer_state valores unicos: {sorted(df['customer_state'].dropna().unique())}")
```

**Celula 3 — Agregacao geo com schema completo:**
```python
geo_agg = (
    df.groupby("customer_state")
    .agg(
        total_orders=("order_id", "count"),
        bad_reviews=("bad_review", "sum"),
        avg_dias_atraso=("dias_atraso", "mean"),
        avg_freight_value=("freight_value", "mean"),
    )
    .assign(bad_review_rate=lambda x: x["bad_reviews"] / x["total_orders"])
    .reset_index()
)
print(geo_agg.sort_values("bad_review_rate", ascending=False).head(10))

# Export para Streamlit (Phase 6)
geo_agg.to_parquet(PROCESSED / "geo_aggregated.parquet", index=False)
print(f"Exportado: data/processed/geo_aggregated.parquet ({len(geo_agg)} UFs)")
```

**Celula 4 — Download e load do GeoJSON do Brasil:**
```python
GEOJSON_PATH = DOCS / "brazil_states.geojson"
GEOJSON_URL = "https://raw.githubusercontent.com/codeforamerica/click_that_hood/master/public/data/brazil-states.geojson"

if not GEOJSON_PATH.exists():
    r = requests.get(GEOJSON_URL, timeout=30)
    r.raise_for_status()
    GEOJSON_PATH.write_bytes(r.content)
    print(f"GeoJSON baixado: {GEOJSON_PATH}")
else:
    print(f"GeoJSON ja existe: {GEOJSON_PATH}")

br_states = gpd.read_file(str(GEOJSON_PATH))

# CRITICO: verificar campo de sigla do estado
print("Colunas do GeoJSON:", br_states.columns.tolist())
print("Primeira feature:", br_states.iloc[0].to_dict())
# Ajustar SIGLA_COL conforme o output acima
```

**Celula 5 — Identificar campo de sigla e fazer merge:**
```python
# Detectar automaticamente o campo de sigla
possible_fields = ["sigla", "abbrev_state", "UF", "CD_UF", "SIGLA_UF"]
SIGLA_COL = None
for field in possible_fields:
    if field in br_states.columns:
        SIGLA_COL = field
        print(f"Campo de sigla encontrado: {SIGLA_COL}")
        break

if SIGLA_COL is None:
    print("ATENCAO: nenhum campo de sigla padrao encontrado. Colunas disponíveis:", br_states.columns.tolist())
    raise ValueError("Ajuste SIGLA_COL manualmente para o campo correto")

br_merged = br_states.merge(geo_agg, left_on=SIGLA_COL, right_on="customer_state", how="left")
print(f"Merge: {br_merged['bad_review_rate'].notna().sum()} UFs com dados")
```

**Celula 6 — Choropleth interativo com folium:**
```python
m = folium.Map(location=[-15, -55], zoom_start=4, tiles="CartoDB positron")
folium.Choropleth(
    geo_data=br_merged.__geo_interface__,
    data=geo_agg,
    columns=["customer_state", "bad_review_rate"],
    key_on=f"feature.properties.{SIGLA_COL}",
    fill_color="YlOrRd",
    fill_opacity=0.75,
    line_opacity=0.3,
    legend_name="Taxa de Avaliacoes 1-2 Estrelas",
    nan_fill_color="lightgray",
).add_to(m)
folium.LayerControl().add_to(m)
m.save(str(FIGURES / "eda03_choropleth_static.html"))
print("Exportado: eda03_choropleth_static.html")
```

**Celula 7 — Choropleth estatico para slides via plotly + kaleido (fallback para PNG):**
```python
import plotly.express as px

fig = px.choropleth(
    geo_agg,
    geojson=br_merged.__geo_interface__,
    locations="customer_state",
    featureidkey=f"properties.{SIGLA_COL}",
    color="bad_review_rate",
    color_continuous_scale="YlOrRd",
    title="Taxa de Avaliacoes 1-2 Estrelas por UF",
    labels={"bad_review_rate": "Taxa", "customer_state": "UF"},
    scope="south america",
)
fig.update_geos(fitbounds="locations", visible=False)
fig.update_layout(
    margin={"r": 0, "t": 50, "l": 0, "b": 0},
    coloraxis_colorbar=dict(title="Taxa", tickformat=".0%"),
    font=dict(size=13),
    title_font_size=16,
)
fig.write_image(
    str(FIGURES / "eda03_choropleth_bad_reviews_uf.png"),
    format="png", scale=2, width=900, height=650,
)
print("Exportado: eda03_choropleth_bad_reviews_uf.png")
```

Fallback seaborn caso kaleido/plotly falhem (usar apenas como contingencia):
```python
# Fallback: mapa estatico via geopandas plot
fig, ax = plt.subplots(figsize=(10, 12))
br_merged.plot(
    column="bad_review_rate", cmap="YlOrRd", legend=True,
    missing_kwds={"color": "lightgray"}, ax=ax, edgecolor="white", linewidth=0.3,
)
ax.set_title("Taxa de Avaliacoes 1-2 Estrelas por UF", fontsize=15, fontweight="bold")
ax.axis("off")
fig.tight_layout()
fig.savefig(FIGURES / "eda03_choropleth_bad_reviews_uf.png", dpi=150, bbox_inches="tight")
plt.close(fig)
print("PNG exportado via fallback geopandas")
```
  </action>
  <verify>
    <automated>python -c "
import pandas as pd
from pathlib import Path

# Verificar geo_aggregated.parquet
p = Path('data/processed/geo_aggregated.parquet')
assert p.exists(), f'geo_aggregated.parquet ausente: {p}'
df = pd.read_parquet(p)
required = ['customer_state', 'total_orders', 'bad_reviews', 'avg_dias_atraso', 'avg_freight_value', 'bad_review_rate']
missing = [c for c in required if c not in df.columns]
assert not missing, f'Colunas ausentes: {missing}'
assert len(df) >= 20, f'Poucos estados: {len(df)}'
assert (df.bad_review_rate.between(0, 1)).all(), 'bad_review_rate fora do range 0-1'
print(f'geo_aggregated.parquet OK: {len(df)} UFs, colunas: {df.columns.tolist()}')

# Verificar PNG
png = Path('reports/figures/eda03_choropleth_bad_reviews_uf.png')
assert png.exists() and png.stat().st_size > 10000, f'Choropleth PNG ausente: {png}'
print('EDA-03 tudo OK')
"</automated>
  </verify>
  <done>data/processed/geo_aggregated.parquet existe com 6 colunas obrigatorias e >= 20 UFs. bad_review_rate esta entre 0 e 1. eda03_choropleth_bad_reviews_uf.png existe com tamanho > 10KB. Mapa usa taxa (nao contagem bruta).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EDA-05 — Heatmap de Rotas Origem x Destino (top-15 origens)</name>
  <files>notebooks/FASE3-P2-eda-geo.ipynb, reports/figures/eda05_rotas_heatmap.png</files>
  <action>
Adicionar ao mesmo notebook FASE3-P2-eda-geo.ipynb as seguintes celulas (continuando apos Task 1):

**Celula 8 — Verificar disponibilidade de seller_state:**
```python
assert "seller_state" in df.columns, "seller_state ausente na gold table — verificar Phase 2"
assert "dias_atraso" in df.columns, "dias_atraso ausente — deve ter sido derivado na celula 2"

# Remover linhas sem dados de atraso ou estados
route_data = df.dropna(subset=["dias_atraso", "seller_state", "customer_state"]).copy()
print(f"Linhas com dados de rota: {len(route_data):,} de {len(df):,}")
print(f"seller_state valores unicos: {route_data['seller_state'].nunique()}")
```

**Celula 9 — Filtrar top-15 origens por volume (evita 27x27 ilegivel):**
```python
import seaborn as sns

top_origins = (
    route_data.groupby("seller_state")["order_id"]
    .count()
    .nlargest(15)
    .index
)
print(f"Top-15 origens (seller_state): {sorted(top_origins.tolist())}")

route_filtered = route_data[route_data["seller_state"].isin(top_origins)]
```

**Celula 10 — Pivot table de atraso medio por corredor:**
```python
pivot = route_filtered.pivot_table(
    values="dias_atraso",
    index="seller_state",    # origem (vendedor)
    columns="customer_state",  # destino (cliente)
    aggfunc="mean",
)

print(f"Pivot shape: {pivot.shape} (origens x destinos)")
print(f"Atraso medio geral: {pivot.values.mean():.1f} dias")
```

**Celula 11 — Heatmap seaborn (slide export):**
```python
sns.set_theme(style="white", context="talk")

fig, ax = plt.subplots(figsize=(18, 8))
sns.heatmap(
    pivot,
    cmap="RdYlGn_r",      # vermelho = atraso, verde = adiantado
    center=0,
    linewidths=0.3,
    linecolor="white",
    annot=False,           # Sem anotacao para 15x27 — muitos numeros
    cbar_kws={"label": "Atraso Medio (dias)", "shrink": 0.8},
    ax=ax,
)
ax.set_title(
    "Atraso Medio por Corredor Vendedor (Origem) x Cliente (Destino)\n"
    f"Top-{len(top_origins)} origens por volume de pedidos",
    fontsize=14, fontweight="bold", pad=12,
)
ax.set_xlabel("UF Destino (cliente)", fontsize=12)
ax.set_ylabel("UF Origem (vendedor)", fontsize=12)
plt.xticks(rotation=45, ha="right", fontsize=10)
plt.yticks(rotation=0, fontsize=10)
fig.tight_layout()
fig.savefig(FIGURES / "eda05_rotas_heatmap.png", dpi=150, bbox_inches="tight")
plt.close(fig)
print("Exportado: eda05_rotas_heatmap.png")
```

**Celula 12 — Top 5 corredores mais problematicos (insight de narrativa):**
```python
# Corredores com maior atraso medio (para mencao nos slides)
corridor_avg = (
    route_filtered.groupby(["seller_state", "customer_state"])["dias_atraso"]
    .agg(["mean", "count"])
    .reset_index()
    .rename(columns={"mean": "atraso_medio", "count": "volume"})
    .query("volume >= 50")  # minimo 50 pedidos para ser representativo
    .sort_values("atraso_medio", ascending=False)
    .head(10)
)
print("Top 10 corredores mais atrasados (minimo 50 pedidos):")
print(corridor_avg.to_string(index=False))
```
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; f=Path('reports/figures/eda05_rotas_heatmap.png'); assert f.exists() and f.stat().st_size > 10000, f'Heatmap rotas PNG ausente: {f}'; print('EDA-05 PNG OK')"</automated>
  </verify>
  <done>eda05_rotas_heatmap.png existe em reports/figures/ com tamanho > 10KB. Heatmap mostra top-15 origens (seller_state) x todos os destinos (customer_state), com escala de cores RdYlGn_r. Top-10 corredores mais atrasados estao impressos no notebook para uso na narrativa dos slides.</done>
</task>

</tasks>

<verification>
Verificacao final do plano — rodar apos todas as tasks:

```bash
python -c "
import pandas as pd
from pathlib import Path

# 1. geo_aggregated.parquet com schema correto
geo = pd.read_parquet('data/processed/geo_aggregated.parquet')
required_cols = ['customer_state', 'total_orders', 'bad_reviews', 'avg_dias_atraso', 'avg_freight_value', 'bad_review_rate']
missing = [c for c in required_cols if c not in geo.columns]
assert not missing, f'Colunas ausentes em geo_aggregated: {missing}'
assert len(geo) >= 20, f'Apenas {len(geo)} UFs — esperado 26+'
assert geo['bad_review_rate'].between(0, 1).all(), 'bad_review_rate fora do range'

# 2. PNGs
pngs = [
    'reports/figures/eda03_choropleth_bad_reviews_uf.png',
    'reports/figures/eda05_rotas_heatmap.png',
]
for f in pngs:
    p = Path(f)
    assert p.exists() and p.stat().st_size > 10000, f'PNG ausente ou vazio: {p}'

# 3. Notebook
nb = Path('notebooks/FASE3-P2-eda-geo.ipynb')
assert nb.exists(), f'Notebook ausente: {nb}'

print('PLANO 03-02 OK: notebook + geo_aggregated.parquet + 2 PNGs verificados')
print(f'UFs no geo_aggregated: {len(geo)}')
print(f'bad_review_rate range: {geo.bad_review_rate.min():.3f} - {geo.bad_review_rate.max():.3f}')
"
```
</verification>

<success_criteria>
1. data/processed/geo_aggregated.parquet existe com schema de 6 colunas e >= 20 UFs
2. bad_review_rate no parquet esta entre 0.0 e 1.0 (taxa, nao contagem)
3. eda03_choropleth_bad_reviews_uf.png exportado a 150 DPI — mapa do Brasil com escala YlOrRd
4. eda05_rotas_heatmap.png exportado a 150 DPI — heatmap top-15 origens x destinos
5. GeoJSON salvo em docs/brazil_states.geojson para reuso (nao baixar multiplas vezes)
6. notebooks/FASE3-P2-eda-geo.ipynb completo e sem paths hardcoded absolutos
</success_criteria>

<output>
Apos conclusao, criar .planning/phases/03-eda-ato-1/03-02-SUMMARY.md com:
- UFs no geo_aggregated.parquet e range de bad_review_rate
- Campo de sigla do GeoJSON encontrado (sigla vs abbrev_state vs outro)
- Top 5 corredores mais atrasados (seller_state -> customer_state, atraso_medio)
- UFs com maior bad_review_rate (top 3)
- Metodo usado para PNG do choropleth (plotly/kaleido ou fallback geopandas)
- Qualquer desvio do plano
</output>
